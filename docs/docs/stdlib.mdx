---
sidebar_position: 5
title: Standard library
description: Every built-in function available today, grouped, with live examples.
---

# Standard library

`wavelet:std/core` is implicitly imported and open in every file. Because identifiers are WIT labels, there is no `+` or `<` — the names are short and boring, a deliberate cost of "the syntax is WAVE, no exceptions".

Every function below is **implemented and runnable today**. A call bundles its arguments — `add(2 3)` passes two — so most functions read several arguments positionally; a few take a single value. Edit any example and press Run.

:::note[What "function" means here]
These are the core *builtins*. The bundled **macros** are `If` and `Do` (see [Control-flow macros](#control-flow-macros)); other conveniences (`And`, `Or`, `TryLet`) are **not** shipped — define them yourself with [`DefMacro`](./language/macros). Two pre-bound *values* also live in core: `none` (the empty option) and `pi`.
:::

<Playground id="std-pi" autoRun />

## Control-flow macros

`If` and `Do` are the two **bundled macros**. They are not core special forms —
each expands to [`Match`](./language/special-forms#match), the sole core
branching construct — but they are always in scope, exactly like the builtins.

### `If`

`If cond then else` — the everyday conditional. The condition must evaluate to a
`bool` (there is no truthiness); a non-`bool` condition is a **compile-time
error**. Only the taken branch is evaluated, and both branches are in
[tail position](./language/tail-calls). It expands to a two-clause `Match`:

```wavelet
If c t e   ==>   Match The bool c [(true t) (false e)]
```

<Playground id="sf-if" />

A non-boolean condition is a compile-time error:

<Playground id="sf-if-nonbool" />

### `Do`

`Do [exprs]` — evaluate each expression in order; the value is the last one.
Useful for side effects. Takes a single **list**. It expands to nested `Match`
clauses over a wildcard, so every statement runs and the last is the result:

```wavelet
Do [a b c]   ==>   Match a [(_ Match b [(_ c)])]
```

<Playground id="sf-do" />

## Predicates

| Function | Shape | Result |
|---|---|---|
| `eq` | `eq(a b)` | structural equality |
| `lt` `le` `gt` `ge` | `lt(a b)` | ordered comparison (numbers, strings, chars) |
| `not` | `not(bool)` | boolean negation |

`eq` is structural across all data types. The ordered comparisons work on numbers, strings, and chars.

<Playground id="std-predicates" />

## Arithmetic

| Function | Shape | Notes |
|---|---|---|
| `add` `sub` `mul` `div` `rem` | `add(a b)` | int+int stays int (overflow & ÷0 trap); a float operand promotes to float |
| `neg` | `neg(x)` | negation |
| `abs` | `abs(x)` | absolute value |
| `min` `max` | `min(a b)` | smaller / larger of two |

<Playground id="std-arith" />

Integer division by zero (and overflow) is an error, not a silent `NaN`:

<Playground id="std-div-zero" />

A float operand promotes the result to a float:

<Playground id="std-div-float" />

## Sequences

| Function | Shape | Result |
|---|---|---|
| `len` | `len(seq)` | length of a list, tuple, or string |
| `empty` | `empty(seq)` | is it empty? |
| `get` | `get(seq i)` | element at index `i` (list/tuple) |
| `put` | `put(list i v)` | list with index `i` replaced by `v` |
| `push` | `push(list v)` | list with `v` appended |
| `concat` | `concat(a b)` | two lists joined |
| `head` | `head(list)` | first element |
| `tail` | `tail(list)` | all but the first |
| `reverse` | `reverse(list)` | reversed list |
| `range` | `range(lo hi)` | `[lo … hi-1]` |
| `map` | `map(f list)` | apply `f` to each element |
| `filter` | `filter(pred list)` | keep elements where `pred` is `true` |
| `fold` | `fold(f init list)` | left fold; `f` receives `[acc elem]` |
| `zip` | `zip(a b)` | list of `(a-elem, b-elem)` tuples |

<Playground id="std-seq-basics" />

`get`, `put`, `push`, `concat`:

<Playground id="std-seq-mutate" />

`map`, `filter`, `fold`, `zip` take functions:

<Playground id="std-map" />

<Playground id="std-filter" />

<Playground id="std-fold" />

<Playground id="std-zip" />

:::note[`get` is for sequences]
`get` indexes lists and tuples; it does not look up record fields (use [pattern matching](./language/pattern-matching) or `rec-key`/`rec-val` for records).
:::

## Tuples

| Function | Shape | Result |
|---|---|---|
| `tuple0` … `tuple16` | `tuple2(a b)` | the tuple of exactly those arguments |

One constructor per size, from `tuple0()` (the empty tuple) through `tuple16(…)`, each taking **exactly** that many arguments — there is no variadic form, so every constructor has a WIT-expressible fixed-arity shape and works as an ordinary function value. The result type is the tuple of the argument types; no `The` ascription is needed. (In evaluation position parens are calls, which is why tuples get constructors rather than a paren literal — see [Values & types](./language/values#compound-values).)

<Playground id="std-tuple" />

## Strings

| Function | Shape | Result |
|---|---|---|
| `str-cat` | `str-cat(a b …)` | concatenate strings/chars (its arguments, or a single list of them) |
| `upper` `lower` | `upper(s)` | case conversion |
| `split` | `split(s sep)` | split on a separator → list of strings |
| `join` | `join(parts sep)` | join a list of strings with a separator |
| `contains` | `contains(s sub)` | does `s` contain `sub`? |

<Playground id="std-strcat" />

<Playground id="std-strings" />

`str-cat` takes strings and chars only — convert other values first with [`to-string`](#conversion):

<Playground id="std-strcat-tostring" />

## Conversion

| Function | Shape | Result |
|---|---|---|
| `to-string` | `to-string(v)` | canonical WAVE text for any value |
| `read` | `read(s)` | parse a string to a form → `ok(form)` / `err(msg)` |
| `to-u8` … `to-u64`, `to-s8` … `to-s64` | `to-u8(n)` | range-checked integer narrowing (a char gives its codepoint) |
| `to-f32` `to-f64` | `to-f64(n)` | to floating point |
| `to-char` | `to-char(n)` | int codepoint → char (must be a Unicode scalar value) |

<Playground id="std-tostring" />

`read` is the inverse of `to-string` for code — it parses WAVE/Wavelet text into a form value:

<Playground id="std-read" />

Numeric conversions range-check:

<Playground id="std-conv" />

<Playground id="std-conv-bad" />

Chars convert to and from their Unicode codepoints — `to-u32` (or any integer
conversion wide enough) reads a char's codepoint, and `to-char` builds a char
back from an int. `to-char` rejects ints that are not Unicode scalar values
(surrogates, or anything past `0x10FFFF`):

<Playground id="std-char-conv" />

<Playground id="std-char-conv-bad" />

## Input / output

The standard library has **no** I/O builtins. The language and compiler know
nothing about WASI: a program that wants to write to stdout, read its arguments,
or talk to the host imports the relevant WIT interface (e.g. `wasi:cli/stdout`,
`wasi:cli/environment`, `wasi:io/streams`) and calls it like any other
[imported component](./components). The `cli` template scaffolded by
[`wavelet new`](./cli#new) shows the pattern: it imports `wasi:cli/stdout` and
`wasi:io/streams`, builds the greeting, and writes it through the stream.

Because these are host calls, they only run in a compiled component on a wasm
runtime — not in the in-browser playground or under [`wavelet run`](./cli#run),
which evaluate pure programs only.

## Meta & reflection

| Function | Shape | Result |
|---|---|---|
| `apply` | `apply(f payload)` | call a function value with a payload |
| `gensym` | `gensym()` | a fresh, unique name |
| `expand` | `expand(form)` | run one macro-expansion step on a form |
| `form-kind` | `form-kind(v)` | the kind tag of a value-as-form |
| `rec-key` | `rec-key(record)` | the first field's key, as a name |
| `rec-val` | `rec-val(record)` | the first field's value |

<Playground id="std-apply" />

`form-kind` classifies a value the way the reader classifies a form (`"int"`, `"str"`, `"sym"`, `"tup"`, `"lst"`, `"rec"`, …). A quoted call is a tuple, so it reports `"tup"`; `"call"` is reserved for a runtime variant carrying a payload (`ok(1)`):

<Playground id="std-form-kind" />

`rec-key` / `rec-val` pull apart a single-field record — the workhorses of binding macros like `TryLet` ([see Macros](./language/macros#error-propagation-without-early-return)):

<Playground id="std-rec-key-val" />

## Constructors

| Name | Shape | Builds |
|---|---|---|
| `some` | `some(v)` | `option` — present |
| `none` | `none` | `option` — absent (a bound value, not a call) |
| `ok` | `ok(v)` | `result` — success |
| `err` | `err(v)` | `result` — failure |

<Playground id="std-constructors" />

## Cells

A `cell` is the standard library's mutable reference — the one place mutation lives, since [the core has none](./language/evaluation#no-loops-no-mutation-no-early-return). A cell is also the usual **representation** behind a [user-declared resource](./language/type-system#user-declared-resources): a `DefResource`'s `New:` constructor typically returns a `cell`, and its methods mutate through it.

| Function | Shape | Effect |
|---|---|---|
| `cell-new` | `cell-new(v)` | a new cell holding `v` |
| `cell-get` | `cell-get(c)` | read the current value |
| `cell-set` | `cell-set(c v)` | overwrite the value (returns unit) |
| `drop` | `drop(h)` | release a resource handle |

<Playground id="std-cells" />
