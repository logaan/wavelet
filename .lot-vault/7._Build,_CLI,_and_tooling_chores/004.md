---
status: work
update-id: lot:033l1PXx0gRmk9dF9ia3li
work-at: 2026-07-06T22:55:14.379907+00:00
---
## 7.1 REPL readline — DONE (commit 12a41d1)

Interactive `wavelet repl` now uses rustyline (native-only dep, under the
existing wasm-excluded target table): arrow-key line editing, persistent history
(`$XDG_STATE_HOME/wavelet/history`), Ctrl-C abandons the entry, Ctrl-D exits.
Design: gated on `stdin().is_terminal()` — piped input keeps the original plain
line-reader so the exact-match repl_session tests stay byte-identical (verified);
both front-ends share `eval_forms`. Drove the interactive path through a real pty
to confirm editing + history file write. README + cli.mdx + CHANGELOG updated.
Full `cargo test` green. No regen-examples needed (no language/example change).

Next: 7.3 VS Code tooling end-to-end (static validation + smoke doc).
