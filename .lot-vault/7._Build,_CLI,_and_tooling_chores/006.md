---
status: work
update-id: lot:033l1e3MbXrQ83c7XlB9Fn
work-at: 2026-07-06T23:04:46.430652+00:00
---
# Goal 7 — SESSION 1 CHECKPOINT

Branch `goal-7-build-cli` (from ae8425b), 4 commits, pushed, tree clean, green.
All five children addressed. Three engineering chores landed; 7.4 resolved
pragmatically with the single-import form recorded as a future language slot;
7.5 blocked upstream.

## Commits (origin/main..HEAD)
- d49cd3a  7.2 shebang: `.wlt` files run via `#!/usr/bin/env wavelet`
- 12a41d1  7.1 REPL readline (rustyline, native-only)
- 7371738  7.3 VS Code tooling headless grammar+manifest check
- 67a63f0  7.4 two-import docs + wit.rs/build.rs comment refinement

## Per child
- **7.1 readline — DONE.** Interactive `wavelet repl` uses rustyline: line
  editing, persistent history ($XDG_STATE_HOME/wavelet/history), Ctrl-C/Ctrl-D.
  Gated on stdin().is_terminal() so piped input keeps the plain reader — the
  exact-match repl_session tests are byte-identical. rustyline is under the
  wasm-excluded native target table. Drove the pty path to confirm. README +
  cli.mdx + CHANGELOG.
- **7.2 shebang — DONE.** Lexer skips a leading `#!` line (byte 0 only); bare
  `wavelet <file.wlt> [args...]` runs via the compiled-with-fallback path.
  Mirrored the token class into all THREE grammars (Prism, VS Code, wavelet.nvim
  submodule pushed to branch `goal-7-shebang`, pointer bumped). tests/shebang.rs
  (3). README + cli.mdx + CHANGELOG. Did NOT add "top-level as run" wrapper
  (semantics — flagged, not preempted).
- **7.3 VS Code — DONE.** scripts/check-grammar.mjs tokenizes via
  vscode-textmate + vscode-oniguruma (VS Code's own engine) and asserts 12 scope
  cases (incl. shebang) + manifest/grammar consistency; `npm run check` green.
  extension.js parses; wordPattern compiles. Manual GUI smoke checklist + the
  check documented in the vscode README. Dev deps gitignored.
- **7.4 dual macro+runtime import — RESOLVED pragmatically; single-import form
  DEFERRED.** A single import can't be both (needs a new import surface = a
  language-semantics change, out of this session's scope). The capability works
  today by importing the package twice (one macros:true, one runtime) — verified
  structurally sound, documented in macros.mdx, comments refined. Full design
  sketch on the 7.4 Thing (lot:033iXOAGSg0UwwTcBZA5O5) for a future language slot.
- **7.5 registry fetch (wavelet add) — DEFERRED (blocked upstream).** wkg fetches
  dep WIT but NOT components; a macros:true import needs the compiled .wasm. No
  registry-fetch possible until component-registry tooling exists. Recorded on
  the 7.5 Thing (lot:033iXOACqsHOWi7nwORVpU). Local `from:`/conventional path is
  the supported way today.

## Verification
- `cargo test`: FULLY GREEN (incl. new tests/shebang.rs; repl_session unchanged).
- Conformance matrix: NOT re-run — no emit/build logic changed. build.rs/wit.rs
  edits are comment-only; the lexer shebang-skip is read-stage and only affects
  sources starting with `#!` (none in the suite). Result is definitionally the
  ae8425b baseline. Tree clean; conformance/wavelet/wit/wavelet.wit untouched.
- CHANGELOG [Unreleased]: Added entries for readline + shebang. No release cut
  (Cargo.toml / wavelet-lsp versions untouched). No regen-examples needed (no
  locked example output changed; mdx prose isn't tracked by gen-examples).
- Downstream surfaces: all three grammars updated for the shebang token class;
  wavelet.nvim committed+pushed (branch goal-7-shebang) + pointer bumped. LSP
  diagnostics unaffected (no semantic change).

## Docket for session 2
Goal 7 is complete for this session. No goal-7 remainder except the two recorded
deferrals (7.4 single-import surface = future language slot; 7.5 = blocked on
component-registry tooling) — neither is build/CLI-chore work. Next per plan:
the record-only post-goal-7 code review, lot:033kscUZLMS3Ouvf2KdFeo (not mine).

## Process
Worktree throughout. Edit/Write blocked (parent not isolated) — all writes via
Bash/python patches. Committed + pushed after every slice. No PR (per brief). No
main commits. lot updates via stdin only.
