---
status: work
update-id: lot:033l1YmYyAmaxJlEDcT0hf
work-at: 2026-07-06T23:01:18.576975+00:00
---
## 7.5 assessment (goal-7 session 1) — DEFERRED (blocked upstream)

Confirmed the blocker stands: `wkg` fetches dependency WIT into wit/deps but
does NOT fetch components, and a `macros: true` import needs the compiled
component (the .wasm that exports wavelet:meta/macros), not just its WIT. So a
real `wavelet add` / registry fetch of macro components cannot be built until the
component-registry tooling exists. Not attempted this session.

Today's supported path (unchanged, documented in macros.mdx): point a macro
import at a locally-built .wasm via `from:` or the conventional
`wit/macros/<ns>-<name>.wasm` location (macrodep.rs resolution strategy). The
stale doc reference the note flagged (a non-existent dev-notes/decouple-wasi.md)
is moot now that dev-notes/ is deleted and planning lives in this vault.

Remains blocked-on-tooling; revisit when component-registry fetch (wkg or a
successor) lands.
