---
status: work
update-id: lot:033l0efNR7rn5Cfwa5OStx
work-at: 2026-07-06T22:24:25.719341+00:00
---
# Goal 7 — session 1 execution brief

Worktree `goal-7-build-cli` off main ae8425b. Read the goal-7 Thing + all five
children, plus the tail of goals 5/6 and the coordination handoff. CLI surface
today: `main.rs` dispatches read/expand/repl/wit/new/run/build/compose/--version;
`repl.rs` is the compiled-first accumulate-and-recompile REPL over
`stdin().lock().lines()`; `runner::run` is compiled-first with interpreter
fallback (goal 6). Source extension is `.wlt` (the notes' `.wvl` is a typo).

## Docket (ordered; green slices)

1. **7.2 shebang support** (lot:033iXOAR5CVhVhuhgs4QgL) — FIRST. Small + safe.
   (a) lexer skips a leading `#!` line at byte 0 only (`//` is the only comment
   today; `#` stays an error elsewhere); (b) dispatch so a bare `wavelet
   <file.wlt>` runs the file (what `#!/usr/bin/env wavelet` yields on
   `./script.wlt`). The note's "treat top-level as run" wrapper is a SEMANTICS
   change (scripts still need `Export run` today); I wire only the plumbing and
   flag the wrapper as a language decision rather than preempt it.

2. **7.1 REPL readline** (lot:033iXOANTp236L2utwRQhh) — line-editing + history
   via `rustyline`, native-only (gated under the existing
   `cfg(not(target_arch = "wasm32"))` table so it never reaches the wasm
   playground cdylib). Piped-stdin REPL tests (repl_session.rs) stay green.

3. **7.3 VS Code tooling end-to-end** (lot:033iXOAUUfB6G5qswzc9wx) — can't drive
   live VS Code from here: validate tmLanguage.json + language-configuration.json
   + package.json well-formed and consistent with the lexer token classes, add a
   repeatable check, document the manual in-editor smoke steps.

4. **7.4 dual runtime-dep + macro-library import** (lot:033iXOAGSg0UwwTcBZA5O5)
   — real gap needing two surfaces (world runtime import PLUS compile-time
   instantiation) at wit.rs:803 / build.rs:81. Assess; land a scoped slice or
   record precisely why deferred.

5. **7.5 registry fetch `wavelet add`** (lot:033iXOACqsHOWi7nwORVpU) — DEFERRED
   upstream: `wkg` fetches dep WIT but not components. Record as
   blocked-on-tooling, not attempted.

None of the five were satisfied by goals 2–6; no overlap with the six pending
decisions. Semantics changes + the post-goal-7 review are out of scope. Per
slice: `cargo test` green, CHANGELOG [Unreleased], commit + push.
