---
status: work
update-id: lot:033l1Ua9XX1Lz0i9vAOS1y
work-at: 2026-07-06T22:58:32.948410+00:00
---
## 7.3 VS Code tooling — DONE (commit 7371738)

Added tooling/vscode/scripts/check-grammar.mjs: loads the TextMate grammar with
vscode-textmate + vscode-oniguruma (VS Code's own tokenizer) and asserts 12
representative snippets tokenize to the right scopes — including the new shebang
line — plus package.json / language-configuration.json / grammar agree on
language id, `.wlt`, and `source.wavelet`. `npm run check` is green. Two initial
"failures" were my bad test cases and actually confirmed correct behaviour
(qualified call vs qualified value). extension.js parses (node --check),
wordPattern compiles. Documented the check + a manual GUI-only smoke checklist
(bracket matching, auto-close, word selection, LSP wiring) in the vscode README.
Dev deps are gitignored (node_modules, package-lock).

Now assessing 7.4 (dual runtime-dep + macro-library import) at wit.rs / build.rs.
