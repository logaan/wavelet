---
status: work
update-id: lot:033l0ri0uy6q7lFB6O3XW9
work-at: 2026-07-06T22:33:00.034738+00:00
---
## 7.2 shebang — DONE (commit d49cd3a)

Landed shebang support: lexer skips a leading `#!` line (byte 0 only; `#` stays
an error elsewhere), and a bare `wavelet <file.wlt> [args...]` runs the script
through the existing compiled-with-fallback run path. Mirrored the new token
class into all three grammars — Prism (docs/src/prism/wavelet.js), VS Code
TextMate (tooling/vscode/…tmLanguage.json), and the wavelet.nvim submodule
(pushed to branch `goal-7-shebang` on logaan/wavelet.nvim; pointer bumped here).
README + cli.mdx + CHANGELOG updated. New tests/shebang.rs (3 tests) green; full
`cargo test` green. Did NOT implement the notes' "treat top-level as run"
wrapper — that's a semantics change (scripts still need `Export run`); flagged
as a language decision rather than preempted.

Next: 7.1 REPL readline (rustyline, native-only).
