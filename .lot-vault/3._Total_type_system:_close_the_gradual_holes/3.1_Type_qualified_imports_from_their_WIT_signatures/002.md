---
status: info
update-id: lot:033itm4lUsI2kX74WRFeIp
info-at: 2026-07-03T05:31:17.501811+00:00
---
3.1 implemented on branch goal-3-total-types (commit 63e168e): check::ImportSigs + check_program_with_imports feed resolved import WIT signatures (parsed from witdep/sibling FuncSigs and functor SET_OPS) into the checker keyed 'alias/fn'; qualified calls are checked in all §4.2 call shapes and typed by their declared results. build_files re-checks each unit with its resolved deps before emit (pure local check still runs first, preserving error ordering). DefType aliases resolve transparently; the generic bridge's string->list<u8> widening is admitted at call boundaries. New build-path regressions in tests/checker_all_paths.rs. Residuals noted: (a) wavelet run's interpreter stand-in path and eval_snippet still see Qsym as gradual (no dep resolution there — revisit at the 3.9 strict flip); (b) wit.rs export-result inference still cannot see through dep calls (pre-existing; the documented Export-record-form workaround stands — wit::collect would need two-phase dep-aware collection).
