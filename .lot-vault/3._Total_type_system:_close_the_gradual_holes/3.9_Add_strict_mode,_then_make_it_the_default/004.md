---
status: work
update-id: lot:033iunneiq6RQn3M4TAcEb
work-at: 2026-07-03T06:13:10.375348+00:00
---
3.9 progress on branch goal-3-total-types (commit 772111e), per the accepted option-1 gate: --strict is implemented and opt-in (wavelet run|build|wit --strict; check::set_strict). Under strict, any expression without a concrete static type is a compile error; declaration heads and unexpanded macro calls are exempt (macro OUTPUT is checked strictly post-expansion). Burn-down so far, landed as DEFAULT behaviour since the 2.2 drops are decided: non-bool If conditions are static errors; The is a pure static ascription (all annotation shapes propagate); heterogeneous lists rejected (list<t> only); out-of-range literals static; value defs, flags literals, and rec-val typed. Backlog under strict: 17 of 84 documented examples, listed by the ignored strict_backlog diagnostic in tests/strict_mode.rs — the categories are (a) untyped function values / higher-order builtins (map, filter, fold, apply, closures as values, untyped def params) which need function types (Phase D) or param inference in the checker, (b) cells (need a static resource/cell type), (c) macro-trylet (expansion contains an option-typed hole pattern... to re-derive at flip time). THE DEFAULT IS NOT FLIPPED: the gate (examples + conformance green under strict) is structurally unreachable until function values are typeable, which is beyond this goal's checker phases (Phase D explicitly future work in check.rs's charter). Riders honoured: all new/rewritten examples are strict-clean except the inherently higher-order ones.
