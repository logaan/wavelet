---
status: info
update-id: lot:033iuurFNyNhi9WCiCiav7
info-at: 2026-07-03T06:17:48.695682+00:00
---
3.11 implemented on branch goal-3-total-types (commit b6235e3): the oracle relationship is inverted — the checker runs before the interpreter on every path (eval_snippet source+expanded, run, build with import sigs, wit), and the runtime type-error paths it makes unreachable are DELETED from interp.rs: the runtime The conformance check (The now jumps to its expression — pure static ascription, tail-transparent), bind_one's typed-parameter conformance check, check_type entirely, and Param.ty (runtime closures keep only names). Deleting these also removed an interpreter/backend divergence: the wasm backend never emitted them. Deliberately retained: If's bool destructure (structurally selects a branch), Match's no-clause error (reachable via gradual Unknown scrutinees until the strict flip), and dynamic bind-arity errors (closures called through map/apply are untyped until function types land). Full suite green (21 binaries).
