---
status: info
update-id: lot:033iuy065uV90jhzbP23Vr
info-at: 2026-07-03T06:19:52.625658+00:00
---
3.13 implemented on branch goal-3-total-types (commit beb3c89): wavelet-lsp diagnostics now run the full pipeline on every edit — read, best-effort macro expansion (local macros; foreign macro components are not instantiated on the keystroke path), then wavelet::check::check_program — so the new total-checker errors (ill-typed calls, non-exhaustive Match, heterogeneous lists, If-bool, static The, literal ranges) surface in the editor with the compiler's exact message. Tests added in analysis.rs. Known limitation recorded in-code: checker errors carry no spans yet, so diagnostics anchor to the first line — span-carrying checker errors are the follow-up that makes them precise. Grammars: NO lexer token classes changed in goal 3 (verified git diff against main for lexer.rs is empty), so Prism, wavelet.nvim, and VS Code grammars are current — no submodule bump needed. The /// doc-comment token class mentioned in update 002 did NOT land this goal (wit.rs doesn't emit docs yet — that work remains with the 2.4 doc-comment wiring, later goal); when it lands, the three-grammar mirror and LSP doc hover go with it.
