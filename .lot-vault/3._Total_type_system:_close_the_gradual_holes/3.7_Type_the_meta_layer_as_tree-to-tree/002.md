---
status: info
update-id: lot:033itUGrjYZxuD8nWHziQK
info-at: 2026-07-03T05:19:35.391941+00:00
---
3.7 implemented on branch goal-3-total-types: Type::Tree is the static type of the meta layer, matching the wavelet:meta 'tree' WIT record. Quote/Quasi -> tree; Quasi holes (Unquote/Splice) are checked as ordinary expressions with interp's exact depth rules while template data stays opaque; Splice holes must be list-shaped. DefMacro templates now check as tree -> tree (body checked with params bound at tree — an unbound name or a param used as a scalar in a template is now a compile error, even if the macro is never called). gensym/expand/rec-key -> tree; read -> result<tree, string>. rec-val stays gradual (it projects a field *value*, which on runtime records is arbitrary). 5 new regressions in tests/type_system.rs; full suite green. Homoiconicity intact: quoted data round-trips unchanged, only the type got a name.
