---
status: work
update-id: lot:033itPSyTUNBjk9nYo3rk8
work-at: 2026-07-03T05:16:25.924840+00:00
---
3.4 progress (commit e1ef03d): Type::IntLit now carries the literal's value and unification with a concrete int type range-checks it, so every literal that meets a concrete int context (typed parameter — positional, by-name record, or single payload — variant payload, overload filtering, to-* conversions) resolves and range-checks at COMPILE time, with byte-identical messages to the old runtime checks ('parameter `a`: 300 does not conform to type `u8`', '`to-u8`: 999 out of range', '`to-char`: 55296 is not a Unicode scalar value'). The typed-byte-add-bad differential SKIP entry is retired — interpreter and compiled artifact now agree. Still open for 3.4: unconstrained literals still default at s64/f64 only in wit.rs inference (that's the strict-mode defaulting rule, lands with 3.9), and full u64 range beyond i64::MAX remains blocked on the boxed i64 runtime representation (5.2), as recorded in update 002.
