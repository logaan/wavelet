---
status: info
update-id: lot:033iv1NrV5FtksDYNfX68e
info-at: 2026-07-03T06:22:06.041397+00:00
---
3.4 closing status (branch goal-3-total-types): literals now resolve statically wherever a concrete context exists — Type::IntLit carries its value and unification with an int type range-checks it (typed params in all call shapes, variant payloads, overload filtering, The, to-* conversions), with byte-identical messages to the old runtime checks. The runtime checks themselves are deleted per 3.11 (bind_one conformance, The, to-* literal path still range-checks non-literal values at runtime — that residue is by design until strict flips). Unconstrained literals default s64/f64 at the boundary via check::type_to_wit (the defaulting rule, single place). Deferred as recorded in update 002: full u64 range (u64::MAX literals) stays clamped by the boxed-i64 runtime representation and the i64 form node — 5.2 finishes that at runtime; the lexer/form widening should land with it.
