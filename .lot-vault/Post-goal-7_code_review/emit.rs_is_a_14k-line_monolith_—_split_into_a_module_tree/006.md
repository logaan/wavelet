---
status: info
update-id: lot:033yWynPOJYG5ucidNVnYN
info-at: 2026-07-30T15:44:36.114481+00:00
---
**Recovered update** — originally posted 2026-07-07T11:12:03Z as lot:033lJUevg21iaofuSgOEC1 from a git worktree, where it sat uncommitted in the worktree's copy of the vault and never reached the main vault. Re-posted verbatim below before removing the merged worktrees.

Done — emit.rs is now a module tree. Draft PR: https://github.com/logaan/wavelet/pull/39 (branch `worktree-split-emit-module-tree`).

**Stacked on PR #38** (`worktree-split-emit-builtin`): that still-open draft splits `Emitter::builtin` — exactly the code this refactor moves — so basing on main would have guaranteed a nasty conflict. Merge #38 first; GitHub retargets #39 to main when #38's branch is deleted.

The split (pure code motion, no behaviour change): the root `src/emit.rs` drops from 14,594 to 775 lines, keeping the shared types (`Emitter`, `FnCtx`, `Helpers`, signature/binding types), consts, `expr` dispatch, `emit_component`, and module wiring. Fourteen submodules under `src/emit/`: `witty` (WIT types + ABI sizing), `scan`, `deps`, `boxes`, `call`, `pattern`, `canonical` (lower/lift/store/load), `mem` (canonical-memory path + scalar ops), `builtins`, `core_module`, `resources` (functor set + user resources, with tests), `macro_component` (strategy-B `mc_*`), `helpers` (helper bodies), `wit_synth`. Largest file is now ~2.1k lines. `Emitter` methods stay on one type via per-concern `impl` blocks made `pub(crate)`; the public `wavelet::emit::*` API paths are unchanged via root re-exports. Each file has a `//!` header.

Verification: `cargo test` 410 passed / 0 failed; clippy warning set identical to base (6 pre-existing, nothing new); and `wavelet build examples/main.wlt examples/shout.wlt` produces byte-identical wasm artifacts with the base and split compilers. Commits are staged so each one compiles. No CHANGELOG entry (internal refactor).

One housekeeping note: while comparing clippy baselines I accidentally `git stash pop`ed your pre-existing stash (`WIP on main: a924233 docs: rewrite WASI-decoupling plan…`) into my worktree; the pop conflicted, I reset my worktree, and the stash entry is still in `git stash list` untouched — nothing lost.
