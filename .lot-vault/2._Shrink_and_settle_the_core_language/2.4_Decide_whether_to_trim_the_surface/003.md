---
status: work
update-id: lot:033icVS27Lu2ZAzfbgnFgj
work-at: 2026-07-02T17:47:38.822524+00:00
---
Decisions:

1.  A. Drop the unit value.
2.  A. Keep and bless `///`
3.  B. Keep the f(x y) sugar.
4.  Break this out into its own decision note: 2.7.
    - I don't understand the value of having grouping at all.
    - I don't understand why it's needed in quoting or macros.
    - If `(a x y)` means call `a` with `x` and `y` as arguments thing I think
      `(a)` should mean call `a` with no arguments. Isn't that the normal lisp
      way?
    - Don't continue this discussion on this note, just use these comments to
      inform the options and recommendation you make on 2.7.
5.  Move do to stdlib.
    - Create a new decision file 2.8 that considers whether we can move `If`
      into stdlib if we're keeping `Match` in core.

Make sure any other tasks that depend on these decisions are updated.