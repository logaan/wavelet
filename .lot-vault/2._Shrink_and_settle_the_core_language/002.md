---
status: info
update-id: lot:033isgw3GDS7fV4SCJFtg9
info-at: 2026-07-03T04:47:09.902884+00:00
---
Goal-2 actionable work is complete. Ledger:

**Decided (by Logan, recorded and retired):**
- 2.2 drop list — all seven children done: heterogeneous lists, spontaneous structural records, tag-dispatch dynamism (all four behaviours), runtime The checks/boundary coercions, runtime eval, and the gradual escape hatch are all dropped; 2.2.7 (first-class functions) is moot because 2.3 went to defunctionalization. Every drop's implementation lands with 3.9/3.10, none earlier, by design.
- 2.3 closures — defunctionalize inside the component, expand-time combinators (5.7.1), resource-lifting extension parked at 5.8.1; 2.3.1 answered by the same decision. Both retired.
- 2.4 surface trims — unit value drops (3.9/3.10), /// blessed as docs (with wit.rs doc emission), f(x y) sugar stays, Do moves to stdlib (5.7). Retired; grouping and If spun out.
- 2.5 naming alignment — idea dropped; names stay as they are. Retired with no work.

**Implemented (branch goal-2-core-language, pushed; 4 commits, cargo test 230/230 green):**
- 2.6 extension switch .wvl -> .wlt (cde1167), all surfaces covered incl. docs, both grammars, wavelet.nvim (bb09900 pushed + pointer), LSP, CHANGELOG. Retired.
- 2.1 review — mechanical pass (clippy + repo-wide rustfmt, 068d464), stale dev-notes doc references removed (427dfbf), CLI project-root helper extracted (0d31b1a). Internal only, no CHANGELOG entries needed; no language behaviour changed, so no example regen was needed.

**Open for Logan (three yes/nos, all with options + recommendation written up):**
- 2.7 grouping/(a): recommend keeping the current semantics ((a) is Tup[a], a zero-arg call) — no language change.
- 2.8 If to stdlib: recommend yes, sequenced behind 3.6 + 3.12; Do moves without waiting.
- 2.1 carries two new proposals from the review: drop list-payload positional binding at calls (with 3.9/3.10), and reject float literals as Match patterns (with 3.12).

**Recorded as goal-5 inputs on 2.1:** emit.rs scale/split seams, the duplicated quasi special-head recognition (oracle/backend drift risk), the second WIT renderer in emit.rs, two structural clippy warnings left in place, and the @0.2.0 hardcode (goal 4).

2.1 stays in work status until Logan answers its two proposals; everything else under this goal is retired.
