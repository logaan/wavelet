---
status: work
update-id: lot:033isf7tbyKGNpefKRELrw
work-at: 2026-07-03T04:45:58.568023+00:00
---
**emit.rs findings — recorded as goal-5 inputs, not fixed in place** (per update 002's re-scope):

1. **Scale**: emit.rs is 8,812 lines — more than half of src/. The goal-5 rewrite should split at the seams that already exist informally: canonical-ABI layout (flags_size/align_up/field offsets), the Emitter expression walk, quoting/quasi codegen, the functor/resource machinery, and world-WIT synthesis.
2. **Two structural clippy warnings left in place**: `store_variant_case` takes 8 arguments (emit.rs:3317) and a function near emit.rs:982 returns the bare tuple `(Vec<(String, TypeDef)>, Vec<(String, String)>)` — both symptoms of missing named types (a `VariantCtx`, a named synthesis result) rather than lint problems.
3. **Duplicated special-head recognition**: `Emitter::quasi`/`quasi_seq` hand-duplicate the interpreter's depth-1 `unquote-MACRO`/`splice-MACRO`/nested-`quasi` recognition (interp.rs quasi/quasi_seq). Any future change to quasi semantics must be made twice, in structurally different code — a drift risk between the oracle and the backend. A shared form-classifier (even just "is this 2-tuple a quote-family head at this depth") would remove it.
4. **Second WIT renderer**: `synthesize_world_wit` (emit.rs ~8100) renders world text separately from wit.rs's `synthesize_info`; only the functor interface is shared (via `wit::functor_interface`). The goal-5 shape should have exactly one WIT renderer.
5. Minor: `Features::dep_calls` deduplicates via `Vec::contains` (O(n^2) scan in `scan()`); fine at current scale, worth an IndexSet if component graphs grow.
6. The TAG_FN boxed-closure representation and the uniform boxed heap are already condemned by the 2.3 decision (defunctionalization, 5.8) — no notes needed beyond what 2.3 records.
