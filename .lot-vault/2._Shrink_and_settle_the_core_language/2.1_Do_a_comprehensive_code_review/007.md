---
status: work
update-id: lot:033isedfSSGt4GP92SIdeQ
work-at: 2026-07-03T04:45:39.342965+00:00
---
Review complete over the update-003 scope (branch goal-2-core-language). Summary per file:

**lexer.rs / reader.rs / form.rs / printer.rs** — in good shape. Small, uniform reader rule set; the MacroTable collision policy is well-documented and well-tested; the `-inf` whole-word fix from the earlier pass holds. No further refactors warranted.

**expand.rs** — clean. The ForeignExpander seam is tidy and the Derive multi-root path is clearly fenced. Nothing to change.

**check.rs** — coherent Phase A/C implementation; the gradual-top discipline ("never preempt a runtime error") is consistently applied, and the memoised return-type-directed resolution is sound. Its heterogeneous-list and tag-dispatch hedges are exactly the code the 2.2 drops will delete at 3.9/3.10 — no point polishing them now.

**wit.rs** — sound; the SET_OPS single-source-of-truth pattern (one descriptor drives both the inference op-table and the emitted resource) is the right shape. Fixed: dangling `dev-notes/` doc references here and in macrodep.rs/tools.rs/witdep.rs (427dfbf). Noted for goal 4: `external_versioned` hardcodes `@0.2.0` for every wasi import.

**interp.rs** — small (630 lines) and readable; the Step::Jump tail-call loop and the quasi depth rules are clear. Two semantics quirks found -> posted as separate proposals below rather than fixed.

**CLI (main.rs)** — extracted the duplicated project-root derivation into one helper (0d31b1a). Otherwise fine.

**Mechanical pass** (068d464): clippy fixes across the tree + repo-wide rustfmt (the repo was not previously rustfmt-clean), an approx_constant fix in a macros test.

All work verified: cargo test passes, 230 tests, 0 failures. No language behaviour changed, so no example regeneration was needed. Remaining items on this Thing: the two proposals below await Logan's yes/no; emit.rs findings recorded next as goal-5 inputs.
