---
status: work
update-id: lot:033wpOPG8wSSrUR9h0t6yj
work-at: 2026-07-27T16:02:07.843813+00:00
---
Items 1, 4, and 5 are implemented, verified, and up for review:
https://github.com/logaan/wavelet/pull/41 (draft, branch
`worktree-tuple-constructors`).

- Item 1 (tuple construction, 0.1's answer): `tuple0`..`tuple16` in the
  oracle, checker (exact static arity, inferred `tuple(...)` type), and
  backend (boxed TAG_TUP + canonical in-place construction incl.
  variant/result/record payloads). First-class use works; variant cases
  shadow the names like `zip`. New suite `tests/tuple_constructors.rs`.
  Bonus oracle-parity fix: Sym-headed tuple patterns over statically-known
  tuple scrutinees now destructure on the boxed path (they took the
  variant-only reading before).
- Item 4 (values callee): builds as-written and PASSES `run-values()`
  against both rust seeds via the new
  `conformance/wavelet/test-values-callee.sh`; STATUS header + README
  updated.
- Item 5 (docs + tooling): values/syntax/evaluation/stdlib/roadmap prose
  corrected, two new regenerated examples (also differential-covered), LSP
  hover blurb, CHANGELOG Added+Fixed. No grammar changes were needed — none
  of the three grammars enumerate builtins (call heads highlight via the
  attachment rule) and the lexer token classes are unchanged, so no
  wavelet.nvim bump.
- Also fixed en route: `src/wit.rs` was cfg'd out of wasm32 builds while
  check.rs calls `wit::export_names`, which had broken
  `scripts/regen-examples.sh` on main; ungated (it is a pure arena walk).

Still open on this step: item 2 (record-payload closure ABI — blocked on
decision 0.7, unanswered), item 3 (values-atoms flags residue — open
engineering), item 6 (blocked on decision 0.4, unanswered).
