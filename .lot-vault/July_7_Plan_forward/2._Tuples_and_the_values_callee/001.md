---
status: note
task-id: lot:033wp6XHKDaUK8ahmoUNGJ
update-id: lot:033wp6XHKDaUK8ai9vUBEM
note-at: 2026-07-27T15:50:23.137966+00:00
---
# 2. Tuples and the values callee

The plan for closing the last language gaps in front of the conformance
values callee. Written by following "1.1 Write step 2"
(lot:033lMm9WbLtbNA93dWRMuU). Decisions consulted: 0.1
(lot:033lMg3lFvO9LLepj3efMs — ANSWERED: fixed-arity constructors
`tuple0`..`tuple16`, no variadics, every constructor first-class), 0.7
(lot:033lMgJ87OwkIyv1WJABPV — no answer yet), 0.4
(lot:033lMg3nyxOBIjqzRPRGN4 — no answer yet).

## Items

1. **Tuple construction (0.1's answer: `tuple0`..`tuple16`)** — one builtin
   per size, fixed arity, WIT-expressible shape, usable as a function value.
   Oracle in `src/builtins.rs` (+ `tuple_ctor_arity` helper), checker arm
   returning `Type::Tuple(arg_tys)` with exact static arity, backend boxed
   constructor (TAG_TUP) + canonical in-place construction incl. the
   `WitTy::Tuple` arms in `mem_field_ok`/`mem_field_into` so tuples work as
   canonical results and variant/result/record payloads. Nested-tuple tests
   and a differential example. IN FLIGHT (session c75d8dff, branch
   `worktree-tuple-constructors`): interp/checker/backend landed with tests;
   also fixed en route: the boxed matcher's Sym-headed tuple pattern now
   takes the tuple reading over a statically-known tuple scrutinee (was
   variant-only — silently diverged from the oracle).

2. **Record-payload closure ABI** — BLOCKED on 0.7's answer. When answered
   (recommendation is (a): teach the n-param closure wrapper to bind a
   record payload by name, mirroring `bind_params`), acceptance = the
   `std-apply` entry leaves the differential SKIP list.

3. **`values-atoms` flags-literal emit residue** — the differential SKIP
   entry `("values-atoms", FLAGS)` ("flag literals rejected on this emit
   path"). Plain engineering, no decision needed; diagnose and fix, or
   record precisely why it stays. OPEN.

4. **The values callee** — with item 1 in,
   `conformance/wavelet/src/roundtrip-values.wlt` builds as-written
   (`tup(...)` respelled `tuple2`/`tuple3`). Add
   `conformance/wavelet/test-values-callee.sh` (mirror
   `test-resources-callee.sh`), run `run-values()` against BOTH rust seeds,
   update the .wlt STATUS header and `conformance/wavelet/README.md`.
   IN FLIGHT (same session as item 1).

5. **Docs corrections + tooling surfaces** — `values.mdx` (drop the
   `("abc", 123)` tuple-value overclaim; document `tupleN`),
   `evaluation.mdx`/`syntax.mdx` tuple-provenance lines, `roadmap.mdx`
   claims, `stdlib.mdx` entries, examples regen; the three grammars' builtin
   lists (Prism, wavelet.nvim submodule commit+push+pointer bump, VS Code)
   and the LSP builtin set; CHANGELOG `Added`. IN FLIGHT (same session).

6. **Qualified-ctor outcome** — BLOCKED on 0.4's answer (recommendation is
   (a) drop: one docs line that cases are spelled bare or
   `importalias/case`).

## Related bugs raised out of 0.1's research (own Things, not this step)

- lot:033woU3CLSqzuuJ9bxZo3y — backend generic call path resolves builtins
  before local variant cases (silent divergence).
- lot:033woU8I57f0eM3YvNxo7D — multi-payload variant cases synthesize
  invalid WIT.

## Sequencing

This step runs FIRST among the implementation steps: it unblocks the values
callee and one fallback hole (`std-apply` — item 2). Step "4. Remove the
interpreter fallback" depends on items 1–3 landing.
