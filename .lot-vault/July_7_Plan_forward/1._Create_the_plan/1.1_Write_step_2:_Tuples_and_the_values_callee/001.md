---
status: note
task-id: lot:033lMm9WbLtbNA93dWRMuU
update-id: lot:033lMm9WbLtbNA95AOYI4q
note-at: 2026-07-07T13:25:48.478449+00:00
---
# 1.1 Write step 2: Tuples and the values callee

# 1.1 Write step "2. Tuples and the values callee"

Create the top-level Thing `2. Tuples and the values callee` (child of
lot:033lMZDLxdCrU7hCa8fyZC) — the plan for closing the last language gaps in
front of the conformance values callee.

## Decisions to read first (use the answers, not the recommendations)

- lot:033lMg3lFvO9LLepj3efMs — 0.1 tuple value construction
- lot:033lMgJ87OwkIyv1WJABPV — 0.7 record payloads → multi-param closures
- lot:033lMg3nyxOBIjqzRPRGN4 — 0.4 qualified case constructors

## Scope the step must cover

1. **Tuple construction** per 0.1's answer, end-to-end (for the recommended
   `tup` builtin: oracle in `src/builtins.rs`, checker arm returning
   `Type::Tuple(arg_tys)`, backend constructor + a `WitTy::Tuple` arm in
   `mem_field_ok` so tuples work as canonical payloads; deliberate 0/1-arity
   semantics; nested-tuple tests; a differential example).
2. **Record-payload closure ABI** per 0.7's answer (mirror
   `src/interp.rs::bind_params` record-by-name binding in the backend's
   closure wrapper; acceptance = the `std-apply` entry leaves the
   differential SKIP list in `tests/differential.rs`).
3. **`values-atoms` flags-literal emit residue**: the differential SKIP entry
   `("values-atoms", FLAGS)` — "flag literals rejected on this emit path".
   Plain engineering, no decision; diagnose and fix or record precisely why
   it stays.
4. **The values callee**: with tuples in,
   `conformance/wavelet/src/roundtrip-values.wlt` builds as-written — add
   `conformance/wavelet/test-values-callee.sh` (mirror the existing
   `test-resources-callee.sh`), run `run-values()` against BOTH rust seeds,
   update the .wlt STATUS header and `conformance/wavelet/README.md`.
5. **Docs corrections**: `docs/docs/language/values.mdx` (lists
   `("abc", 123)` as tuple syntax — wrong), `evaluation.mdx` / `syntax.mdx`
   ("a literal tuple value comes from Quote (or a builtin)"),
   `docs/docs/roadmap.mdx` (tuple claims); stdlib docs + the three grammars'
   builtin lists + LSP builtin set for any new builtin name.
6. **Qualified-ctor outcome** per 0.4's answer (for "drop": a one-line docs
   note that cases are spelled bare or `importalias/case`; for the fast-path
   or the feature: the corresponding work items from that decision Thing).

## Sequencing

This step should run FIRST among the implementation steps: it unblocks the
values callee and one fallback hole (`std-apply`), and steps 3/4 touch the
same files. Note in the step that step "4. Remove the interpreter fallback"
depends on items 1–3 landing.
