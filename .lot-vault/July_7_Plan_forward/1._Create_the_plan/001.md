---
status: note
task-id: lot:033lMhxdEjtW6T0QpLqMgk
update-id: lot:033lMhxdEjtW6T0Sj4TEm7
note-at: 2026-07-07T13:23:03.179685+00:00
---
# 1. Create the plan

# 1. Create the plan

**Run this once every decision under "0. Answer Open decisions" has an answer
update.** This Thing is the coordinator; it is meant to be sent to a Claude
session (`lot claude send`), which should:

1. Read "0. Answer Open decisions" (lot:033lMZg4j6rFwUavjQh4ZW) and confirm
   every child has an answer update from Logan. If any is unanswered, post a
   work update here listing the gaps and stop.
2. Run this Thing's children **in order** (1.1, 1.2, … 1.7), one subagent per
   child. Each child is a self-contained brief for writing ONE top-level plan
   step. Launch each subagent with the child's full body as its prompt,
   verbatim, plus the rules below. Run them sequentially (they read the same
   repo and create Things in the same vault; the later steps also reference
   sequencing against earlier ones).
3. When all children have run, post an `info` update here summarising the
   created steps, then leave a final index update on "July 7 Plan forward"
   (lot:033lMZDLxdCrU7hCa8fyZC) listing every step Thing by ID.

## Rules to relay verbatim to every subagent

- Create your step as a child of "July 7 Plan forward"
  (lot:033lMZDLxdCrU7hCa8fyZC) via
  `lot thing new --parent lot:033lMZDLxdCrU7hCa8fyZC "N. <name>" < body.md`.
- `lot` update/new bodies must be passed on **stdin** (`< file` or piped) —
  the `-- "body"` argument form hangs and wedges the vault lock. If a `lot`
  command hangs, kill the stuck process to release the lock before retrying.
- Your step Thing must be **self-contained**: restate everything an
  implementing session needs (context, file references, decision outcomes,
  verification gates). Do not reference archived Things; you may reference
  the decision Things under lot:033lMZg4j6rFwUavjQh4ZW and sibling steps.
- **Verify against the repository** (/Users/logaan/code/personal/rust/wavelet)
  before writing: confirm claimed file locations and whether any listed item
  has already landed on main. Record what you verified.
- You are writing a PLAN, not implementing it. No code changes; no worktree
  needed. Read-only repo access plus `lot thing new`/`lot update`.
- The step body should contain: a goal statement; ordered work items with
  enough detail to execute (oracle-first rule where language behaviour
  changes: src/interp.rs + src/builtins.rs define semantics; the backend
  must agree); per-item verification (cargo test, tests/differential.rs,
  conformance both seeds, ./scripts/regen-examples.sh when documented
  examples change, CHANGELOG.md [Unreleased], docs prose, the three grammars
  + wavelet.nvim submodule push + pointer bump + LSP when surface changes);
  and explicit dependencies on other steps.
- Post a short `info` update on YOUR brief Thing (the 1.x child) when done,
  linking the created step's ID.
