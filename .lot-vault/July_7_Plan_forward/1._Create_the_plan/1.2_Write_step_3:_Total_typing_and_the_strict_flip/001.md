---
status: note
task-id: lot:033lMm9Xrt37xYDK6FxSnO
update-id: lot:033lMm9Xrt37xYDLMZsARn
note-at: 2026-07-07T13:25:48.491632+00:00
---
# 1.2 Write step 3: Total typing and the strict flip

# 1.2 Write step "3. Total typing and the strict flip"

Create the top-level Thing `3. Total typing and the strict flip` (child of
lot:033lMZDLxdCrU7hCa8fyZC) — the plan for clearing the strict backlog,
flipping `--strict` to the default, deleting gradual mode, and landing the
representation riders that totality unlocks.

## Decision to read first

- lot:033lMg3n6ZxhIVXUDqjAXy — 0.3 strict-mode backlog & the flip (contains
  the full empirically-verified bucket breakdown of the 17 failing examples
  and the flip mechanics; use the ANSWER)

## Scope the step must cover (assuming the hybrid; adjust to the answer)

1. Typed parametric arrow signatures for the higher-order builtins
   (`map`/`filter`/`fold`/`apply`) in `src/check.rs`, replacing their
   hard-coded `Unknown` results.
2. Expected-arrow-directed param inference: push a call site's expected arrow
   onto an untyped `Fn` literal's params; extend to user-def higher-order
   params (clears `eval-twice`).
3. Bucket-C record-return rewrites of `eval-fn-by-name` / `eval-fn-by-order`
   in `docs/scripts/gen-examples.mjs` + `./scripts/regen-examples.sh`.
4. Bucket-D stragglers: recursion-aware result inference for self-recursive
   defs (prefer inference over new `Fn` result-annotation syntax); a static
   cell type (`cell-new(x): cell<T>`, `cell-get: T`, `cell(T)` ascriptions);
   the option-typed hole in the `macro-trylet` expansion; capturing-Fn-literal
   arrow typing; list-of-function-values typing (needs item 5).
5. **Riders gated on totality** (verify current state in the repo first):
   the multi-case variant+dispatch closure representation (distinct function
   values in one binding — deliberately deferred to land WITH retirement),
   TAG_* boxed-representation retirement (possible only when total typing
   removes the last boxed producer; TAG_FN/TAG_TUP were explicitly gated on
   the closure work), and collapsing boundary lift/lower to no-ops where
   producer and consumer layouts now agree.
6. **The flip**: default strict on, delete gradual mode (`Unknown`-as-top
   paths and the boxed-fallback branches that exist only for them), convert
   `tests/strict_mode.rs::strict_backlog` from ignored diagnostic to hard
   assertion (must print 0 of 86), confirm conformance under strict,
   CHANGELOG, prose sweep (`type-system.mdx`, `philosophy.mdx`,
   `pattern-matching.mdx`, `special-forms.mdx` for gradual wording).

## Sequencing

Depends on step 2 (shared files: `src/check.rs`, `src/interp.rs`,
`src/emit/`). This is the largest step — recommend the plan break it into
sessions with per-session green gates, inference work before riders, flip
last.
