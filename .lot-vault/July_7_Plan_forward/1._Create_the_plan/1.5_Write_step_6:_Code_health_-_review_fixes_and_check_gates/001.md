---
status: note
task-id: lot:033lMm9ab8gcQJKCbLtFAl
update-id: lot:033lMm9ab8gcQJKEKhKpSJ
note-at: 2026-07-07T13:25:48.519016+00:00
---
# 1.5 Write step 6: Code health - review fixes and check gates

# 1.5 Write step "6. Code health: review fixes and check gates"

Create the top-level Thing `6. Code health: review fixes and check gates`
(child of lot:033lMZDLxdCrU7hCa8fyZC) — the plan for the accepted
code-review findings and the static-analysis gates.

## Decisions to read first

- lot:033lMgJ9BtS8IZM2KpoBXi — 0.8 findings triage (contains the seven open
  findings F1–F7 restated in full: fallback double-execution, persist
  barrier cell-identity/cycles, differential-harness coverage, REPL
  recompile perf, temp-project harness dedup, unchecked neg/abs,
  node_types invariant). Plan ONLY the accepted ones.
- lot:033lMgJAAIYfJEv1ybh27v — 0.9 static analysis as part of check.

## Scope the step must cover

1. The accepted findings, in the priority order from the answer (default:
   F1 → F2 → F3 → F5 → F4 → F7 → F6), each with its "suggested change"
   sketch from 0.8 expanded into concrete work items and acceptance tests.
   Note the couplings: F1 and F5 share the typed-stage-error refactor (do
   together); F3's docs-independent differential fixture corpus +
   effect-stream comparison is what makes F1/F2 regressions visible — the
   step should land F3's fixtures alongside or before the F1/F2 fixes so
   the fixes are pinned by tests; F1 interacts with step 4 (fallback
   removal eventually deletes the fallback — the F1 rule is the correct
   interim behaviour and the typed stage error survives as the build/run
   error surface).
2. Static-analysis gates per 0.9's answer (default: clippy `too_many_lines`
   + `cognitive_complexity` with in-repo thresholds, a file-length
   structural check, a coverage `--fail-under-lines` gate reusing
   `scripts/coverage.sh`'s cargo-llvm-cov bootstrap, all wired into one
   `scripts/check.sh` and CI). The implementing session must MEASURE the
   current tree first and set initial thresholds that pass, recording them
   in the script — ratchet later, don't block on cleanup.

## Sequencing

F1/F5/F3 can start immediately (independent of the language steps). Suggest
running the check-gates item AFTER the big refactors in steps 2–4 merge, so
thresholds are measured against the settled tree.
