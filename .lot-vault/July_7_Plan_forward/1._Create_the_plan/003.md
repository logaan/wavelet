---
status: work
update-id: lot:033yXjQ4N7iRByUuC4eXgY
work-at: 2026-07-30T16:15:14.653797+00:00
---
# Gate check passed — proceeding to run briefs

Verified every child of "0. Answer Open decisions" (lot:033lMZg4j6rFwUavjQh4ZW). There are now 17 decisions (0.1–0.17; 0.12–0.17 were broken out of 0.8's findings F2–F7). All have answers:

- 0.1, 0.2 — answered and already EXECUTED (PRs #41 and #40, both merged); retired.
- 0.8 — resolved: F1 decided (remove the fallback entirely), F2–F7 broken out as 0.12–0.17; retired.
- 0.3 — drop the non-WIT-expressible constructs (implementation, not just examples).
- 0.4 — drop Qsyms; `/` in names becomes sugar for `-`; case ctors behave as functions.
- 0.5 — do all the DefResource tail items.
- 0.6 — drop `read` from the language (compiler-side instead).
- 0.7 — drop `apply` from the language.
- 0.9 — best analysis, deps OK if auto-installable; hard limits + ratcheting.
- 0.10 — user-defined functor templates: doubling down, must not be parked.
- 0.11 — dual-use import: keep, don't park.
- 0.12 — (A) short-term guard. 0.13 — accept, skip property/fuzzing. 0.14 — accept. 0.15 — accept. 0.16 — accept. 0.17 — do the structural fix.

Brief 1.1 already ran on 2026-07-27 (created "2. Tuples and the values callee", lot:033wp6XHKDaUK8ahmoUNGJ, info posted on 1.1) — skipping it.

Now running briefs 1.2 → 1.7 sequentially, one subagent per brief, each given its brief body verbatim, the relay rules verbatim, plus an addendum indexing ALL 17 decision answers (per Logan's instruction that the broken-out decisions must all be taken into account — several answers supersede assumptions the briefs were written with).
