---
status: note
task-id: lot:033lMm9ZiqDD9Of74wjdhI
update-id: lot:033lMm9ZiqDD9Of8T5TlPZ
note-at: 2026-07-07T13:25:48.510362+00:00
---
# 1.4 Write step 5: DefResource fidelity and docs polish

# 1.4 Write step "5. DefResource fidelity and docs polish"

Create the top-level Thing `5. DefResource fidelity and docs polish` (child
of lot:033lMZDLxdCrU7hCa8fyZC) — the plan for the DefResource tail.

## Decision to read first

- lot:033lMg3orDepAC2YGMdiYU — 0.5 DefResource tail (playground example;
  own-consumption; Drop bodies). It contains the full context and per-item
  execution sketches; turn the ANSWER into the step.

## Scope the step must cover (per the answer)

1. **Runnable `<Playground>` example**: author `E['res-counter']` (or
   similar) in `docs/scripts/gen-examples.mjs` — construct `counter(10)`,
   call `counter/next` twice, read `counter/value`; must not depend on
   own-consumption trapping or observable Drop. Run
   `./scripts/regen-examples.sh` (heavy: wasm-pack rebuild of the committed
   `docs/src/wasm` artifact + `examples.json` + `cargo test` re-lock);
   commit both artifacts; embed `<Playground id="…">` in
   `docs/docs/language/type-system.mdx`. No CHANGELOG (docs only).
2. **Own-consumption modelling** (if chosen): thread declared `own`/`borrow`
   param types onto closure params (`src/value.rs` `Param`/`Closure`
   currently erase them); set `consumed = true` in
   `src/interp.rs::bind_params` when binding an own-typed resource arg; the
   existing `unwrap_receiver` trap then fires on reuse. Borrow never
   consumes; `self` binds the rep (untouched). Oracle tests (own reuse
   traps, borrow doesn't) → backend agreement → conformance. CHANGELOG
   Changed. Makes `type-system.mdx`'s ownership-invalidation prose true.
3. **Running user `Drop:` bodies** (if chosen): fire each live resource's
   Drop at component teardown (end of `run()`), at-most-once, never for
   borrows, not user-callable. Oracle test with an observable Drop (log
   cell) → backend agreement (the backend already exports/runs the dtor) →
   conformance. CHANGELOG; regen only if a documented example gains a Drop.
4. If (ii)/(iii) are parked: the step records the divergence explicitly
   (backend enforces ownership and runs Drop; interpreter does neither) in
   a place a future totality/oracle audit will find — e.g. a tracked note in
   the step and a code comment at the dead `consumed` field.

## Sequencing

Independent of steps 2–4 (different files except `interp.rs`; merge
hygiene). The regen is stall-prone — recommend running it as its own
commit/session.
