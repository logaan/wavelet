---
status: info
update-id: lot:033wps0d3c6ura9zxdax6i
info-at: 2026-07-27T16:21:35.188378+00:00
---
# PR #40 merged

Rebased onto main after #41 (tuple constructors) landed and merged.
https://github.com/logaan/wavelet/pull/40

Rebase notes:

- #41's `26986e4` had independently fixed the wasm32/docs-playground build
  (by ungating `wit.rs`), so this branch's equivalent fix (moving
  `export_names` into `check.rs`) was dropped during the rebase —
  `export_names` stays in `wit.rs` as upstream has it.
- Examples and the playground wasm artifact were regenerated against the
  merged source (tuple constructors + the new float/char printing); the
  regenerated `examples.json` was identical to the rebase-merged one, so
  only the wasm binary changed.
- Full `cargo test` green on the final merged state (native + wasm32 lib
  builds verified).

0.2 is done end to end: float + char `to-string` in the wasm backend, six
differential SKIPs cleared (plus stale `values-atoms`), WAVE-validity
audited with the NUL fix, and the follow-on WAVE-compliance testing idea
filed as lot:033wpbyZGIPp8UWFETiUId.
