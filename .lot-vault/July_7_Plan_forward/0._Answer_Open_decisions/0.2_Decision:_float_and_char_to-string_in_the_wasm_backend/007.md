---
status: work
update-id: lot:033woXRwZti1TYAnErfCIA
work-at: 2026-07-27T15:27:19.565470+00:00
---
# Acting on 005

Reading the decision as: **small components win; float round-trip is dropped as a guarantee**. Concretely:

- **Char to-string**: implement now in the backend as recommended — hand-emitted, bounded (UTF-8 encode + Rust `{c:?}` escape rules), matching the interpreter exactly.
- **Float to-string**: option (c) — a *simple, deterministic, indicative* format implemented identically in **both** the interpreter (`value.rs`) and the emitted `to_str` helper, so the oracle contract holds without linking `core::fmt` (no tens-of-KB Rust module in every component). This changes documented float outputs → regen-examples + docs + CHANGELOG *Changed* (plus *Fixed* for the trap removal).

Plan: pick the exact format after reading `write_value` and `emit` `to_str` (aiming for ~6 significant digits, `%g`-style fixed/scientific switch, `nan`/`inf`/`-inf` kept, trailing `.0` preserved so floats stay visually distinct from ints); implement interpreter-first, mirror in wasm, hand-built vector tests vs `print_value`, un-SKIP the six differential entries, regen examples, update docs/CHANGELOG. Will work on a branch and open a PR.
