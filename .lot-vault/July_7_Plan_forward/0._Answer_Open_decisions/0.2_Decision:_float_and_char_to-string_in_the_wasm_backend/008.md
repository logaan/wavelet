---
status: info
update-id: lot:033wpG8X06nx3l18l4N9mN
info-at: 2026-07-27T15:56:41.734352+00:00
---
# Done — PR #40

https://github.com/logaan/wavelet/pull/40 (draft, branch `tostr-float-char`)

Executed 005 as: **char now, float as option (c)** — a simple indicative float format in *both* pipelines, no `core::fmt` module linked into components, round-trip guarantee dropped.

## What landed

- **Float**: `to-string` prints six significant digits — fixed notation for exponents `-4..=5` (`3.14159`, `123457.0`), scientific outside (`1e6`, `2.5e-7`), `nan`/`inf`/`-inf` kept, trailing `.0` so floats never read as ints; output still lexes as a Wavelet float. `value::format_dec` uses only fixed-order IEEE-754 f64 ops and the emitted `to_str` arm transcribes it op-for-op ⇒ bit-identical text in interpreter and wasm, verified by 27 boundary vectors (subnormals, `f64::MAX`, specials, rounding carries) plus the differential suite. Component size cost is a few hundred wasm instructions. Source literals and the form printer keep full precision — code round-trips unchanged.
- **Char**: `{c:?}` ported exactly for all of Latin-1 (named escapes, `\u{..}` non-printables incl. NBSP/soft-hyphen — empirically checked against Rust); printable Unicode passes through as UTF-8; beyond-Latin-1 non-printables are a documented divergence, same policy as the string branch.
- **All six TOSTR_TRAP differential SKIPs removed** — plus `values-atoms`, whose FLAGS reason turned out stale (the float/char in the record were the real trap; flags already compiled). SKIP list is down to 3 entries.
- **Bonus fix**: the wasm32/docs-playground build had been broken on main since 80b18b2 (check.rs called the native-only `wit` module); `export_names` moved into `check.rs`. regen-examples works again.
- Docs: examples regenerated (only `sf-let`/`std-pi` texts changed), stdlib.mdx notes the indicative format, CHANGELOG has Changed + Fixed entries. Grammars/LSP unaffected (no lexical change).

Full `cargo test` green. This closes six of the ten gates on removing the run/REPL interpreter fallback.
