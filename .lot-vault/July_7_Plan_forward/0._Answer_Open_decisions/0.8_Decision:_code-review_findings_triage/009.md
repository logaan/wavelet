---
status: info
update-id: lot:033wrFzCk0Ep3crxcarEnQ
info-at: 2026-07-27T17:18:05.579586+00:00
---
# Triage resolved: F1 decided, F2–F7 broken out

Per updates 004/006, nothing was implemented. Two outcomes:

## F1 — decision recorded: drop the fallback

The compiled→interpreter fallback in `runner::run` / `repl::eval_forms` is to
be **removed entirely**, not patched with a pre-execution-only rule. A
compiled-path failure will surface as an error instead of silently re-running
in the interpreter. This still needs a scheduled step in the plan; note the
interaction with decisions 0.6/0.7 — until those holes close, programs that
hit them will error rather than fall back. Removal rewrites the staging code
in `src/runner.rs`, so it pairs naturally with F5 (now 0.15).

## F2–F7 — each is now its own decision Thing under "0. Answer Open decisions"

- **0.12 Decision: persist write barrier — cell identity and cycles** (F2) —
  lot:033wrFDvDcEkwELqysmlOj
- **0.13 Decision: docs-independent differential corpus comparing stdout**
  (F3) — lot:033wrFDwIDUorK5CZZpeK6
- **0.14 Decision: REPL recompiles the whole session per expression** (F4) —
  lot:033wrFDxAjR6BZkQr3wzW1
- **0.15 Decision: shared temp-project build harness** (F5) —
  lot:033wrFDy2rNw0aBrNjTjep
- **0.16 Decision: checked neg and abs overflow** (F6) —
  lot:033wrFMiihsMx2AW4hMYHE (renamed from "neg/abs": names can't contain
  path separators)
- **0.17 Decision: node_types last-write-wins invariant** (F7) —
  lot:033wrFMjtDMGXzfFrkVCYo

Each is self-contained (problem, question, recommendation, how to answer, with
file:line receipts from the 2026-07-28 re-verification) — answerable without
referring back here. Where a finding's context changed because of the F1
decision (F3's stdout rationale, F5's pairing), the breakout says so.

This Thing is fully resolved by those breakouts; nothing else remains here.
