---
status: work
update-id: lot:033wop2TMsw2clW6wZmlko
work-at: 2026-07-27T15:38:53.171276+00:00
---
Progress: interpreter, checker, and wasm backend all construct tuples now.

- `tuple0`..`tuple16` are ordinary builtins in `builtins::NAMES`; `tupleN`
  returns the N-tuple of its arguments. Fixed arity means the §4.2 bundle is
  unambiguous at every N, so first-class use works: `apply(tuple2 pair)` and
  `map(tuple1 xs)` behave exactly like direct calls in the oracle.
- Checker: `tupleN` requires exactly N args and infers `tuple(t1 .. tN)` — no
  `The` ascription needed.
- Backend: boxed path builds a TAG_TUP box; canonical path (the one the
  values callee needs) constructs elements in place — verified via a real
  component: computed tuples as export results, a genuine 1-tuple, nested
  tuples, and a tuple as a result's ok-payload all round-trip through
  wasmtime with correct values. Variant cases / local bindings shadow the
  new names exactly like `zip` today (test locks this in the oracle, and a
  local `Let` shadow in the backend).

`cargo test` fully green (129 lib + all integration suites). New suite:
`tests/tuple_constructors.rs`. Next: the conformance values callee.
