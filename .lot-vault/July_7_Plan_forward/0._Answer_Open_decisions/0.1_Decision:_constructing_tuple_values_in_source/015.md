---
status: work
update-id: lot:033woUcGpu9Kvw3qnLDxPo
work-at: 2026-07-27T15:25:27.832354+00:00
---
Decision received: fixed-arity tuple constructors `tuple0` .. `tuple16` — no
variadics (WIT can't express them), and every constructor is an ordinary
builtin usable as a function value.

Raised the two pre-existing bugs from update 011 as their own Things under
"July 7 Plan forward":

- lot:033woU3CLSqzuuJ9bxZo3y — Bug: backend generic call path resolves
  builtins before local variant cases (the silent `not(5)` divergence).
- lot:033woU8I57f0eM3YvNxo7D — Bug: multi-payload variant cases synthesize
  invalid WIT.

Now executing the decision. Note the fixed-arity shape dissolves the earlier
warts cleanly: each `tupleN` knows its arity, so the §4.2 bundle is
unambiguous (N=0 → unit payload, N=1 → bare value, N≥2 → the bundle itself),
first-class use via `apply`/`map` just works, and variant cases shadow the
names exactly like `zip` today. Plan: interpreter first, then checker, then
backend (canonical constructor + `WitTy::Tuple` arm in `mem_field_ok`), tests
incl. nested constructors and result payloads, then the conformance values
callee as-written (switched to `tuple2`/`tuple3` spellings), then the
downstream surfaces (three grammars, LSP, stdlib + language docs, CHANGELOG,
regen-examples). Working in a worktree on a fresh branch; will link the PR
here when it's up.
