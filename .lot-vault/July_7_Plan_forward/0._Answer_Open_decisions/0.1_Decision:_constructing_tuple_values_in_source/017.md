---
status: work
update-id: lot:033wp9mQpqBG6sY2LkzMF8
work-at: 2026-07-27T15:52:31.076900+00:00
---
The conformance values callee BUILDS AND PASSES against both rust seeds:

    ./test-values-callee.sh
    PASS  rust-a caller  <- wavelet values callee
    PASS  rust-b caller  <- wavelet values callee

Getting there surfaced one more pre-existing oracle divergence, fixed en
route: the backend's BOXED matcher read every Sym-headed tuple pattern as a
variant case, so `Match p [((a b) ...)]` over a tuple-typed *param* bound
only `b` (the interpreter disambiguates by the value and destructures).
When the checker knows the scrutinee is a tuple the variant reading is
statically dead, so `pattern_top` now routes to the element-wise tuple
reading. (A runtime tag dispatch can't work there — the two readings bind
different names, but the clause body compiles once against a single scope.)

Also per your message: I've followed "1.1 Write step 2"
(lot:033lMm9WbLtbNA93dWRMuU) and created the plan Thing
"2. Tuples and the values callee" (lot:033wp6XHKDaUK8ahmoUNGJ) under the
plan root, with per-item status: items 1/4/5 (tuples, values callee, docs +
grammars) in flight in this session; item 2 (record-payload closure ABI)
blocked on decision 0.7 which has no answer yet; item 3 (values-atoms flags
residue) open; item 6 blocked on decision 0.4, also unanswered. I'll update
both Things when this session's work lands.

Remaining here: docs corrections, stdlib docs, three grammars + LSP builtin
lists, CHANGELOG, examples regen, then the PR.
