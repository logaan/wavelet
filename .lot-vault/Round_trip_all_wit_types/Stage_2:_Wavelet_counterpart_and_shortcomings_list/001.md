---
status: note
task-id: lot:033iNRSfQ6bJV5pGM2v1Ei
update-id: lot:033iNRSfQ6bJV5pH8LxWUE
note-at: 2026-07-02T07:33:48.162416+00:00
---
# Stage 2: Wavelet counterpart and shortcomings list

Write a Wavelet file that implements the same `roundtrip:suite` world as the
Stage 1 Rust component: it must both call and be callable by every function in
the suite (values, resources, runner).

Then run the full matrix and record results:

- wavelet as caller vs rust-a and rust-b as callee
- rust-a and rust-b as caller vs wavelet as callee
  (seeds only matter on the caller side; both directions x both seed files per
  the parent Thing)

Compile the list of implementation shortcomings — every WIT feature wavelet
cannot express, compile, or round-trip — with a per-feature failure note.
Wavelet's current capabilities must not shape the test: failures are expected
and are the point.

Deliverables: PR 2 (wavelet file + matrix harness wiring), plus the
shortcomings list posted as an update on this task and mirrored to Stage 3.
