---
status: work
update-id: lot:033iPOi5b7gB0cRNp2p9df
work-at: 2026-07-02T08:53:29.447533+00:00
---
RESUME CONTEXT (checkpoint requested by Logan):

Where things stand: steps 2.1/2.2 done (info updates on them), 2.3 done-
blocked (callee impossible today; detailed info update on the step), 2.4
folded into 2.5's resource checks (all pass as caller; callee resources
impossible — no resource DefType), 2.5 nearly done (runner.wvl builds+runs;
bisect results on the step), 2.6 partially done (wavelet->rust-a bisected;
full-matrix run pending), 2.7 not started.

Working tree: worktree .claude/worktrees/wit-roundtrip-conformance, branch
worktree-wit-roundtrip-conformance (PR #27 = stage 1, draft, includes the
runner run-values/run-resources split). UNCOMMITTED stage-2 files under
conformance/wavelet/: src/runner.wvl (builds), src/roundtrip.wvl (callee
attempt, does not build, kept as documentation), out/*.wasm artifacts,
wit/deps/roundtrip-suite/world.wit (already committed).

Immediate next actions:
1. Remove the trapping char-rt check from runner.wvl (trap kills the whole
   run); keep the three wrong-result checks (they report, not trap).
2. Rebuild; run full matrix (wavelet->rust-a, wavelet->rust-b; run,
   run-values, run-resources); record outputs on step 2.6.
3. Optionally one diagnostic build returning to-string(iv/list-u8-rt([1 2 3]))
   to classify wrong-result checks (corruption vs eq weakness).
4. Commit conformance/wavelet (runner + callee-attempt + a README noting the
   roles), push, open PR 2 as draft with base worktree-wit-roundtrip-
   conformance (stacked on stage 1).
5. Step 2.7: distill the shortcomings list (all items already recorded on
   steps 2.3/2.5) into Stage 2's ready-for-review info update; create one
   Stage 3 child per shortcoming.

Commands cheat-sheet: build: cd conformance/wavelet && wavelet build
src/runner.wvl -o out. Compose+run: wac plug ../dist/roundtrip-a.wasm
--plug ../dist/stub.wasm -o callee.wasm; wac plug out/conformance-wavelet.wasm
--plug callee.wasm -o c.wasm; wasmtime run --invoke 'run()' c.wasm.
lot MUST run from the main checkout root (not the worktree).
