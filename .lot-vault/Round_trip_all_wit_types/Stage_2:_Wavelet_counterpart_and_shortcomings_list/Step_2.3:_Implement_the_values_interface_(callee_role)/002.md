---
status: info
update-id: lot:033iPOhyTd0zq0WnWZ7bv2
info-at: 2026-07-02T08:53:29.374976+00:00
---
Callee role (values) is BLOCKED wholesale; findings are definitive and probed:

1. Exporting into a foreign interface is all-or-nothing (component encoder
   demands every function of roundtrip:suite/values@0.1.0; error names the
   first missing one). No partial callee is possible.
2. Export signatures do NOT come from the vendored dep WIT — they must be
   expressed locally (typed Fn params or Export record form), so every type
   must be spellable in wavelet source.
3. Inexpressible/broken pieces found (each probed in isolation):
   - Dep types can't be referenced in local sigs ("name `point` does not
     exist" — no cross-package use synthesis).
   - DefType DOES declare records {..}, flags {a b}, variants [case case(t)]
     (gaps.typ is stale on this), but case names are NOT bound as
     constructors -> variant/enum values cannot be CONSTRUCTED in source
     ("unbound name `north`"; also "`roundtrip:suite@0.1.0` does not export
     `circle` in `types`" when trying t/circle via the types import).
   - Payload-less ok()/err() at a bare-result boundary PANICS the compiler:
     emit.rs:2884 "internal error: never a single flat value" (ICE).
   - Flag literals: clean error "flag literals not supported by the wasm
     backend yet".
   - f32: "type `f32` not supported by the wasm backend yet" (also blocks
     every-primitive which has an f32 field).
   - Dep type ALIASES: "type `points` not supported by the wasm backend yet".
   - char transform inexpressible: no char<->int conversion anywhere in the
     stdlib (interpreter or backend).
   - u64 values above s64::MAX unrepresentable (wavelet ints are i64).
   - wasm backend builtin coverage is tiny: eq/not/lt-le-gt-ge/add-sub-mul-
     div-rem/neg/len/head/tail/str-cat/to-string/upper/lower/some/ok/err/
     form-kind/rec-key/rec-val/gensym/expand ONLY. Interpreter-only builtins
     (map/filter/fold/concat/push/reverse/range/zip/contains/get/put/tup/
     none/flg/...) fail with "unknown function `X` (wasm backend)".
   The full-callee attempt lives at conformance/wavelet/src/roundtrip.wvl
   (does not build; kept as documentation of the target).
