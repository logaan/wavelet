---
status: info
update-id: lot:033iVphuCNWaZuabnjNMQL
info-at: 2026-07-02T13:15:43.371447+00:00
---
Done and ready for review: https://github.com/logaan/wavelet/pull/29 (draft,
branch worktree-fix-byte-lift, stacked on PR #28).

The uncommitted fix found in the worktree was sound and nearly complete; I
verified it, tidied the stray wit probe files it left behind, and shipped it:

- WitTy::IntS/IntU now carry byte width (1/2/4); size_of/align_of/elem_size
  honour it, store_at picks I32Store8/16/32, load_at picks sign/zero-extending
  narrow loads.
- Acceptance met: the three red checks in conformance/wavelet/src/runner.wvl
  flip green — run(), run-values(), run-resources() all return `ok` against
  both rust-a and rust-b callees.
- New regression suite tests/backend_byte_width.rs. Two subtleties made naive
  tests pass on the broken code: a pure echo can't catch the stride bug (guest
  lifted and re-lowered at the same wrong stride — a byte-level bijection — so
  the host got its own bytes back), and the host masks flat u8 results (a
  corrupt head of 197121 masked back to 1). The tests assert with in-guest eq
  checks and an s64-result probe instead; 4/5 fail on the pre-fix emitter, all
  pass with the fix.
- Full cargo test: 224 pass. CHANGELOG updated under [Unreleased]/Fixed.
  Runner header + conformance README no longer document expected failures.
- No interpreter/syntax/docs surface changed, so no example regen or grammar
  updates needed.

Proceeding to the next Stage 3 shortcoming per the queue.
