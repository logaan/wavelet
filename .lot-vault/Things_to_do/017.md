---
status: info
update-id: lot:033lMoB8I2cbIpQzSIpLXA
info-at: 2026-07-07T13:27:08.368842+00:00
---
# Done: "July 7 Plan forward" created

Created lot:033lMZDLxdCrU7hCa8fyZC ("July 7 Plan forward") with the two
children you asked for, fully populated. The subtree is **self-contained**:
it references nothing outside itself, so everything else in the vault can be
archived without losing context. The root Thing's body carries the verified
state-of-the-world (what landed, what's gated, settled decisions that must
not be reopened, and working rules).

## 0. Answer Open decisions (lot:033lMZg4j6rFwUavjQh4ZW) — 11 children

Each has verified context, options + tradeoffs, a recommendation, an
execution sketch, and a "how to answer" line (one update each):

1. 0.1 Tuple value construction (rec: `tup` builtin) — gates the values callee
2. 0.2 Float/char to-string (rec: char now; park float to fallback removal)
3. 0.3 Strict backlog & the flip (rec: hybrid — inference + targeted stragglers)
4. 0.4 Qualified case constructors (rec: drop)
5. 0.5 DefResource tail: playground example + own-consumption + Drop bodies
6. 0.6 `read` on the compiled path (rec: confine statically or guest reader)
7. 0.7 Record-payload closure ABI (rec: extend the wrapper)
8. 0.8 Code-review triage — the 7 still-open findings restated in full
9. 0.9 Static analysis in check (rec: clippy lints + coverage gate in scripts/check.sh)
10. 0.10 User-defined functor templates (rec: park)
11. 0.11 Dual-use (runtime+macro) import (rec: park)

## 1. Create the plan (lot:033lMhxdEjtW6T0QpLqMgk) — coordinator + 7 children

The coordinator Thing contains the run instructions (verify all decisions
answered; one subagent per child, in order; rules to relay verbatim, incl.
the lot stdin rule and self-containment). Its children 1.1–1.7 each brief a
subagent to write one top-level step as a sibling of 0/1:
"2. Tuples and the values callee", "3. Total typing and the strict flip",
"4. Remove the interpreter fallback", "5. DefResource fidelity and docs
polish", "6. Code health: review fixes and check gates", "7. Backend
representation and performance residues", "8. Parked and future work" (a
register that re-records every parked/future item so archiving loses
nothing).

## Verification notes (checked against main, 2026-07-07)

While writing these I confirmed several "did it actually happen?" items from
your update thread, and the new Things reflect reality rather than stale
notes:

- Already IMPLEMENTED (not re-raised as decisions): If/Do to stdlib; both
  2.1 cleanups (list binding, float patterns); DefResource decisions 3–6 +
  docs prose; widening/result-type threading (Option A); backend `drop`;
  permissions-rt zero-surface flags Match; `expand`-inside-macro (resolved
  in code); and three of the ten review findings (emit.rs split into
  src/emit/, Emitter::builtin split, extensionless shebang — PR #37).
- Still OPEN and captured as decisions/steps: tuples (`tup` unbound; values
  callee blocked only on it), float/char to-string (6 SKIPs), std-apply
  record ABI, std-read, values-atoms flags emit residue, strict flip,
  DefResource interp fidelity (dead `consumed`, unrun Drop), 7 review
  findings, static analysis (your note), functor templates, dual-use import.

Vault changes are committed locally by `lot` on main; I have not pushed
origin/main per my session rules — push when you're happy (`git push`).

Next action is yours: answer the 11 decisions, then send
"1. Create the plan" to a Claude session.
