---
status: work
update-id: lot:033lKDmUMq2l5EdTwk47Wz
work-at: 2026-07-07T11:41:43.304445+00:00
---
lot:033i26jtolYsuD2hi5xwd1 left this note •  Proposal 1 (update 009) — option 1: DROP list-payload positional binding. A single list argument binds to one parameter like any other value; positional multi-binding is the tuple's job, by-name binding the record's. The empty-list/ flags/record "no arguments" acceptances go with it (() is the empty tuple and suffices).
•  Proposal 2 (update 010) — option 1: REJECT float literals as Match patterns. A Dec literal in pattern position becomes a checker error (check.rs), pointing at an explicit comparison. Int/bool/char/string literal patterns are unaffected.
Scheduling note: both were originally sequenced "with 3.9/3.10" (proposal 1) and "with 3.12" (proposal 2), but goal 3 is done and those tasks already landed, so these two are now standalone cleanups. Both are small and touch the same files goal-5 is actively in (interp.rs bind_params/match_pattern, check.rs, the backend), so they are being folded into a goal-5 session's docket as their own clearly-labelled commits (interpreter/checker first per the oracle rule, then the backend), rather than run as a conflicting parallel branch.