---
status: work
update-id: lot:033lJnb7BKyidw3wn9qtMc
work-at: 2026-07-07T11:24:30.794088+00:00
---
lot:033iWTZoQYJK01IjTkjC87 left these notes

NOTE / DECISION NEEDED (linchpin, gates the export fast-path payoff): a BARE exported def body is NOT checked against its declared result type (check_roots passes expected=None), so Def f Fn {} {perms: {read exec}} gets the LITERAL's 2-member flags layout (structural), which mismatches the declared type; the export then re-lowers from the declared type (correct output, NOT canonical fast-path). I implemented threading the declared result type (parse Export result types, check each Fn body against it) and it made the flags field take declared positions AND the layout match declared -- BUT it broke 8 backend_lists tests: Def sum Fn {} Match ... add(a b) declares result s64 with an s32 body, and the checker's unify does not widen s32->s64 ("type mismatch: expected S64, got S32"). match-MACRO threads expected into every arm (check.rs:1316), so the rejection is intrinsic. This is entangled with THREE language-semantics questions for Logan:
 1.  Should the boundary/checker allow SAFE int widening (s32 body for an s64-declared result)? Programs currently rely on this being unchecked.
 2.  Threading also makes overload resolution return-type-directed for export bodies (a runtime behavior change).
 3.  New sub-expression type errors could surface. I REVERTED the threading and kept the localized flags work, which is CORRECT for all currently-reachable paths: cross-origin canonical compound ops (5.6) are not implemented yet, so the literal-vs-declared layout divergence cannot manifest (a literal-layout record is self-consistent + oracle-faithful in source order, and the boundary re-lowers set-preservingly). The threading should be decided + landed BEFORE 5.6 makes cross-origin canonical compound ops reachable.
5.4 RESIDUE still open (behavior-preserving optimizations; boxed fallback already correct; the findings themselves prescribe instrumentation not diff tests -- deliberately NOT landed in a background session to avoid untestable codegen that could silently regress pattern/ctor logic):
 1.  Dep-case ctor calls via Qsym (t/east(5)) stay boxed: ctor_parts (emit.rs ~4731) matches only Node::Sym heads, not Node::Qsym; needs alias->case resolution against the target variant's cases.
 2.  Bare-Sym case patterns (none / nullary case) rebox: pattern_top_mem (emit.rs ~2641) routes them to the boxed _ arm; pattern_mem_var (~2774) already handles the (0,None) disc case, so route a bare nullary-case Sym there as a 1-element slice.
 3.  Large types (>256 cases / >32 flags): per-type disc/bitset sizing still unimplemented; canonical gates hard-refuse >256, store/load/pattern assume 1-byte disc. (flags_size already handles >32; store/load error >32.)
 4.  5.9 Sym-headed-tuple residue on the boxed path.