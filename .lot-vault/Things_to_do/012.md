---
status: work
update-id: lot:033lL0duYRGWgPSALuc2lF
work-at: 2026-07-07T12:13:50.099054+00:00
---
lot:033iXNJD5h2KcdEuOxFeOb ended with these notes:

Remaining downstream polish for 4.5 (not correctness; CLAUDE.md surfaces)
•  Docs prose for DefResource (docs/docs/) + a runnable <Playground> example via gen-examples.mjs, then ./scripts/regen-examples.sh to lock examples.json + the committed wasm artifact. Deferred by the stall; scheduling next.
•  Grammars: session 1 assessed no change needed (TitleCase member keys highlight generically; own/borrow are ordinary lowercase names) — re-confirm during docs.
•  LSP: no change expected for this slice.
Staged interp items (not exercised by the round-trip; recorded for later)
•  Own-consumption trap at free-function OWN params; running user Drop bodies.
Non-blocking question for Logan (already flagged on the coordination Thing)
Member results flowing through cell-get/constructor aren't structurally inferable, so the conformance source ascribes them with The (e.g. next: Fn {self: counter} The u32 …). Works and matches the target WIT. Fine as convention, or want dedicated member-result sugar?
▼ work · 2026-07-03 22:28:22.600366+00:00 · lot:033jIjc4iMyvVTER0NNVRs
Docs prose for DefResource written on branch defresource-docs (pushed, no PR — orchestrator integrates).
Added a "User-declared resources" section to docs/docs/language/type-system.mdx covering: member roles (New/Drop/Static/instance methods) + checker rules, use-site spellings (constructor / type/method / statics), ownership at the boundary (bare = own, own/borrow, handle invalidation), identity equality + opaque to-string + no form-serialization, exporting the resource block plus own/borrow free functions, and a worked counter example. Verified the worked example against wavelet wit — it emits exactly the documented WIT (including the mutating next: func() -> u32).
Also expanded the Resources section in components.mdx and cross-linked from DefType (special-forms) and cell (stdlib). Static wavelet code blocks only, no Playground/regen (out of scope).