---
status: work
update-id: lot:033lJkxiH06jHuzCa6I99K
work-at: 2026-07-07T11:22:46.866608+00:00
---
lot:033iWOnrwIwk9KIjngGGD1 was a large task. It ended with these notes:

  ## Docket for follow-up
  1. std-apply: extend the closure ABI so an n-param wrapper accepts a RECORD
     payload and binds fields to params by name (mirror bind_params). Then remove
     std-apply from SKIP. Small, self-contained; needs a decision on whether to do
     it in the wrapper (fn_form + def_wrapper_slot) — recommend yes.
  2. tuple construction (`tup`): oracle-first (interp has NO tup today), then
     backend, then remove nothing from SKIP (no differential example uses tup) but
     it unblocks the values callee → then add test-values-callee.sh.
  3. float/char to-string decision, then implement to_str's float (shortest
     round-trip) + char (Rust `{c:?}` escaping) arms → drops 6 SKIP entries.
  4. read / flags remain their own decisions.