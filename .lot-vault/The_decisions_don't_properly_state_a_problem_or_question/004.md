---
status: info
update-id: lot:033wqCjftC5MIjtCjv1RMu
info-at: 2026-07-27T16:35:12.512210+00:00
---
Done. Each of the six still-open decisions (0.6-0.11) now has a second note
update titled "Restated: the problem and the question" that leads with an
explicit problem statement, then the exact question you're being asked, the
options mapped to the same answer letters as before, and the recommendation.
The original updates (full context, file references, tradeoffs) are untouched
above them, and all six Things still have `note` status so they still read as
unanswered.

What each now asks, in one line:

- 0.6 (lot:033lMg3pjanyUSBpXM3b9t): should `read` work in compiled components
  at all — and if so, implemented in the guest (a), by the host (b), rejected
  statically (c), or parked (d)?
- 0.7 (lot:033lMgJ87OwkIyv1WJABPV): interpreter and backend disagree on
  record-payloads-to-multi-param-closures — extend the backend ABI (a),
  shrink the language (b), or park (c)?
- 0.8 (lot:033lMgJ9BtS8IZM2KpoBXi): which of code-review findings F1-F7 do
  you accept as scheduled work, and in what priority?
- 0.9 (lot:033lMgJAAIYfJEv1ybh27v): what mechanism enforces the
  size/complexity/coverage limits — in-repo clippy+coverage gates (a),
  dedicated analyzers (b), or advisory-only CI (c)?
- 0.10 (lot:033lMgJAwpQurf0H6fb95V): invest in generic containers now —
  park (a), more compiler-known templates when needed (b), or design
  user-defined functor templates (c)?
- 0.11 (lot:033lMgJBj45mdd3Y3wrQoU): unify the double runtime+macro import
  now (b) or park until a real dual-role library exists (a)?

Committed on local main as 8ed1ca7 ("Add note updates restating problem and
question on decisions 0.6-0.11"). Not pushed: local main has diverged from
origin/main (ahead 12, behind 16), so reconciling/pushing is left to you.

Note: this session's LOT_VAULT_PATH env var pointed at the batch-receipts
vault (stale from another session), so all lot commands here were run with it
unset, resolving the wavelet vault via .lot.toml.
