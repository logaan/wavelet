---
status: info
update-id: lot:033iaEAHSCvrYoDXhQMVhJ
info-at: 2026-07-02T16:14:47.381189+00:00
---
Done. Every Thing under "Decisions the plan needs from you" now carries a work
update stating the decision as a question, the options, and a recommendation —
16 updates in all: the 2.2 parent plus its seven children, 2.3, 2.4, 2.5, 2.1,
3.9, 4.6.1, 5.1, and 6.1.8. (The 8.3 parked questions were left alone, per
"decide only if/when they block something".)

The recommendations in brief:

- **2.2 (all seven children): drop.** Every feature on the ledger exists to
  spend runtime tags; the parent Thing has the one-line roll-up. 2.2.7 is moot
  on the recommended 2.3 path.
- **2.3 closures: defunctionalize.** Closed world is free (whole-file
  compilation), closures become plain WIT variants, 8.2 falls out, and 5.1 gets
  to be headerless. Risks to settle at the 5.8 review: deterministic naming of
  generated variants; function types in exported signatures (reject initially).
- **2.4 trim: drop unit value, bless /// docs (maps 1:1 onto WIT docs), drop
  f(x y) sugar, make (a) pure grouping, move Do to stdlib but keep Match
  special** (the checker needs it for exhaustiveness/narrowing).
- **2.5 naming: keep `Package`** — its argument is literally a CM package ID,
  so the sketched package→world rename appears backwards. Rule: each construct
  takes the name of the WIT construct it lowers to; reserve World/Interface for
  goal 4's forms. Deliverable is a small audit of the rest of the surface.
- **2.1 review: run it now**, before 2.2–2.5 are finalized; refactors just get
  done, language-change proposals arrive as question/options/recommendation
  updates on that Thing for your yes/no.
- **3.9 strict default: objective gate** — flip when examples + conformance
  pass under strict; author new tests strict-clean; delete (not just
  un-default) gradual mode after the flip.
- **4.6.1 functors: dedicated `Instantiate` form with named `with:` arguments;**
  Import stays unparameterized.
- **5.1 memory: arena-per-call, headerless everywhere** — contingent on 2.3
  defunctionalization (else wasm-GC for closures only). Unbounded-allocation-
  in-one-call accepted as out of scope; region resets remain addable later.
- **6.1.8 expand-inside-macro: reject with a clear diagnostic**, document
  host-callback as the future path if a real use case appears.

Each is now yours to bless or veto in place on its own Thing.
