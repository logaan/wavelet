---
status: work
update-id: lot:033iwdP4UssCfDK1wp5ghv
work-at: 2026-07-03T07:27:50.179540+00:00
---
Implemented (commit e8c6d59) — and it went further than the re-scoped representation-independent half: witdep now records named aliases (`type points = list<point>`) as name -> WIT-type-text on Dep.aliases; emit feeds them into the boundary TypeEnv where wit_ty already expands aliases recursively before lowering; the checker treats an unresolved dep nominal as gradual against any type (it may alias anything). Sibling .wlt deps also now carry their full non-record type surface (variants/enums/flags/aliases) so the build path matches parsed-WIT deps.

Result: the backend's "type `points` not supported" rejection is gone TODAY, not just at goal 5 — the acceptance check is already restorable and green: points-rt is back in the conformance runner and run() passes against both rust-a and rust-b callees. 5.12's matrix will re-verify under the type-directed representation.
