---
status: work
update-id: lot:033jEkxY9LuAgXHEME4WZY
work-at: 2026-07-03T19:46:16.138727+00:00
---
DefResource implementation session 1 started. Branch `defresource` off latest main (base da399d1). Plan this session: reader/form -> interp (oracle) -> check -> wit, each committed + unit-tested. emit.rs exported-resource intrinsics (generalizing the `set` functor machinery) + full 5.12 conformance are large and entangled with functor instantiation; expected to trail into a follow-on session.

Reader design confirmed against the code: TitleCase heads lex to Tok::Title("<lower>-MACRO"). Register defresource-MACRO (arity 2: name + braces) and static-MACRO (arity 1: wraps the member Fn) in MacroTable::core. New:/Drop: keys lex as Title tokens, so parse_braces record branch must accept a Title key (recover spelling via title_flag_name -> "New"/"Drop"). own(x)/borrow(x)/bare x in type position need no reader change. expand.rs leaves these core heads untouched (descends structurally).
