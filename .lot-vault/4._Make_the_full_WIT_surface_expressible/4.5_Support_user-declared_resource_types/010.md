---
status: work
update-id: lot:033jGZHWMFMyBwzwo2lL2j
work-at: 2026-07-03T21:00:05.413096+00:00
---
DefResource implementation — session 1 checkpoint.

BRANCH: defresource (pushed to origin). Base: da399d1 (latest main).
COMMITS (all pushed):
- 171120a read: parse DefResource forms + capitalized member keys
- 0978854 interp: DefResource semantics (the oracle)
- 148da8a check: member discipline, own/borrow, Derive rejection
- 09a0c26 wit: synthesize resource blocks + own/borrow signatures
- d8081bf docs: CHANGELOG entry

TEST STATUS: `cargo test` fully green (33 suites, 0 failures), including 12 new DefResource tests (3 interp oracle, 8 checker, 1 wit synthesis). Verified end-to-end at the CLI via `wavelet repl`: constructor `counter(10)`, `counter/next` mutates (11,12), `counter/value`->12, static alt-ctor `counter/sum([1 2 3 4])`->10, `to-string(c)`->"<counter>", `eq(c c)`->true, `eq(counter(1) counter(1))`->false. All per design.

LANDED PER LAYER:
1. READ (reader.rs): registered defresource-MACRO (arity 2) and static-MACRO (arity 1) in MacroTable::core; parse_braces now accepts a TitleCase token as a record key (recovered via title_flag_name) so New:/Drop: markers parse. No lexer change (own/borrow/Static/New/Drop are all generic TitleCase/ident tokens). expand.rs leaves these core heads untouched.
2. INTERP (interp.rs, value.rs): new Value variants Resource(Rc<ResourceInstance{name,rep,consumed}>), ResourceCtor, ResourceMethod. defresource-MACRO special form binds bare type name -> ctor (runs New, wraps rep in an identity handle) and each method/static under its qualified key. Methods unwrap the receiver handle so self binds to the rep (decision 2b); statics take no self; sibling-call self pass-through works. Identity equality (Rc::ptr_eq), opaque `<name>` to-string, non-serializable (value_to_form rejects). A method use of a consumed handle traps.
3. CHECK (check.rs, expand.rs): validate_defresources enforces New/Drop-not-Static, Static-no-self, method-must-take-self:<name>, Drop must be Fn{self:<name>}. from_form recognizes own()/borrow() (both reduce to the underlying type for inference). borrow rejected in result/stored (Export result, DefType payloads). Derive over a resource rejected in expand.rs (identity/opaque).
4. WIT (wit.rs): FileInfo.resources: Vec<ResourceDecl>; collect() parses each DefResource, infers member sigs via infer_sig (self dropped from methods). synthesize renders an exported `resource {…}` block into its placement interface. type_text renders borrow(x)->borrow<counter>, strips own(x) to bare. Unit test synthesizes the 5.12 conformance counter interface EXACTLY: constructor(start:u32); next/value: func()->u32; sum: static func(values:list<u32>)->counter; plus make/bump/take/round-trip free funcs with own/borrow spellings.

Note on member result inference: methods returning via cell-get(self) and statics returning counter(...) are NOT inferable structurally, so the conformance wavelet source must ascribe member results with `The` (e.g. `next: Fn {self: counter} The u32 Do[...]`, `sum: Static Fn {values: list(u32)} The counter counter(...)`). The wit test uses this pattern and it matches the target WIT. This is the expected authoring style, not a gap.

RESIDUE / NEXT STEPS (ordered):
A. EMIT (emit.rs) — the big remaining piece; generalize the `set` functor resource machinery. Precise map:
   - Resource type registration: emit.rs ~6092-6116 registers `set`->TypeDef::Resource so wit_ty maps bare/own/borrow to WitTy::Handle. Do the same for each user resource name (and note: no dotted `.set` alias needed unless a functor-style return-text appears).
   - Intrinsic imports: ~6222-6241 declares `[resource-new/rep/drop]set` under module `[export]<versioned iface>`. For a user resource in iface `resources`, module = `[export]<pkg>/resources`, fields `[resource-new/rep/drop]counter`.
   - Index reservation + ResourceFns: ~6275-6305 reserves ctor/add/contains/size/dtor indices per functor before internal/export bodies. Generalize ResourceFns (emit.rs:6849) to N methods+statics (the set case is fixed at add/contains/size); reserve one index per constructor/method/static/dtor.
   - Body emission: emit_set_resource (grep it) builds the ctor/method/dtor bodies. Constructor: box the rep (counter rep = cell of u32; reuse cell emission) then `[resource-new]counter`; each method: `[resource-rep]counter` then the method body with self=rep; dtor: run Drop body if present else no-op. Validate every body AGAINST interp.rs (the oracle).
   - Boundary export names: ~6670-6674 registers `{iface}#[constructor]set`, `{iface}#[method]set.add`, `{iface}#[dtor]set`. For counter: `resources#[constructor]counter`, `resources#[method]counter.next`, `resources#[method]counter.value`, `resources#[static]counter.sum`, `resources#[dtor]counter`. (Name<->boundary mapping helpers at emit.rs:1173-1216.)
   - own/borrow lifting/lowering at the boundary + own-consumption drop: the free funcs make/bump/take pass handles; borrow doesn't consume, own does (callee drops). Model matches interp; wire handle lower/lift.
   - dep_call routing (~3289-3400) routes alias/op to functor_fns; the user-resource analog routes counter(...)/counter/next(...) to the emitted resource funcs. Note: the interp binds these as bare `counter` and `counter/next`; emit must recognize a constructor call (bare resource name applied) and qualified member calls.
B. INTERP own-consumption at free-function own params: currently only method-use-after-consume traps. Full own-consumption (passing a handle to an own param invalidates the caller's) needs param-type info threaded into the interp (closures currently erase types). Not exercised by the conformance round-trip; implement for faithfulness.
C. INTERP running user Drop bodies at handle-drop (staged; acceptance counter has none).
D. CONFORMANCE (shared with 5.12): create the wavelet callee under conformance/wavelet/src exporting the counter + free funcs (target WIT already exists at conformance/wavelet/wit/deps/roundtrip-suite/world.wit interface `resources`), then pass rust-a/rust-b run-resources(). Blocked on A.
E. DOCS prose (docs/docs) — a DefResource section; deferred until emit lands (feature not yet end-to-end compilable). Syntax grammars (Prism/neovim/vscode) need NO change (verified Prism: all TitleCase heads highlight generically as keyword; no token class changed). LSP: no change needed for this slice.

NEEDS LOGAN: none. Design was fully settled and followed exactly. One authoring convention surfaced (member results use `The` ascriptions) — confirm that's acceptable for the conformance source or whether member result annotations warrant dedicated sugar.
