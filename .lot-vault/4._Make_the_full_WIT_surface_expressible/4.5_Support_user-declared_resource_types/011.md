---
status: work
update-id: lot:033jIYVyk3dLpgv0X9wiie
work-at: 2026-07-03T22:21:04.952712+00:00
---
Orchestrator: DefResource backend + 5.12 resources conformance VERIFIED and merged
to main (origin/main = b06357b). Session 2's agent stalled on the stream watchdog
right before checkpointing (last action: reading a test run), but all its work was
committed + pushed; I verified and integrated it.

## Landed this session (branch defresource, merged)
- 092515d emit: cell-new/cell-get/cell-set builtins (mutable cell, TAG_CELL) — the
  wasm backend for the reps user resources are built on.
- fce5aa2 emit: exported user resources (DefResource backend).
- f8c9ad8 emit: user-resource carriage (rep-based), boundary conversions, arena
  persistence (resource state survives across calls, like the set functor).
- d358a4e conformance: wavelet callee for the roundtrip:suite `resources` interface
  (conformance/wavelet/src/roundtrip-resources.wlt + test-resources-callee.sh).
- d9c743f conformance: restore wavelet.wit to the runner world.
- 8160d72 docs: CHANGELOG entries (backend + conformance).

The backend generalizes the `set` functor machinery: resource carried
guest-internally as its rep; handles minted/deref'd only at the component edge
(own via resource.new/resource.rep; a borrow arrives as the rep directly);
[constructor]/[method]/[static]/[dtor] boundary exports.

## Verification I ran (orchestrator)
- `cargo test` on merged main: **370 passed, 0 failed** (+4 over the prior
  integration; includes tests/backend_resource.rs).
- `conformance/wavelet/test-resources-callee.sh` with a fresh release `wavelet`:
  **PASS rust-a caller <- wavelet resources callee**, **PASS rust-b caller <-
  wavelet resources callee**. (`run-resources()` == "ok" both directions.) This is
  the 4.5/5.12 acceptance test — GREEN.

## Status of 4.5 and 5.12
- 4.5 DefResource: FUNCTIONALLY COMPLETE across read/interp/check/wit/emit +
  conformance.
- 5.12 resources callee: DONE (green both seeds). (5.12's `values` callee remains
  goal-5 representation work — separate.)

## Remaining downstream polish for 4.5 (not correctness; CLAUDE.md surfaces)
- **Docs prose** for DefResource (docs/docs/) + a runnable `<Playground>` example
  via gen-examples.mjs, then `./scripts/regen-examples.sh` to lock examples.json +
  the committed wasm artifact. Deferred by the stall; scheduling next.
- Grammars: session 1 assessed no change needed (TitleCase member keys highlight
  generically; own/borrow are ordinary lowercase names) — re-confirm during docs.
- LSP: no change expected for this slice.

## Staged interp items (not exercised by the round-trip; recorded for later)
- Own-consumption trap at free-function OWN params; running user `Drop` bodies.

## Non-blocking question for Logan (already flagged on the coordination Thing)
Member results flowing through cell-get/constructor aren't structurally inferable,
so the conformance source ascribes them with `The` (e.g.
`next: Fn {self: counter} The u32 …`). Works and matches the target WIT. Fine as
convention, or want dedicated member-result sugar?
