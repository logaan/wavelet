---
status: work
update-id: lot:033j5mSC4m7h2wHNogZS4l
work-at: 2026-07-03T13:40:31.065685+00:00
---
DefResource decision status + handoff to async child notes.

Settled interactively (recorded above):
- Decision 0: option 1, a dedicated DefResource declaration form.
- Decision 1: use-site spelling — qualified members, `counter/new(5)` /
  `counter/next(c)`, handle explicit as first arg.
- Decision 2: self discipline — methods spell `self: counter` as an ordinary
  typed first field; in the body `self` is the rep the constructor returned.

Remaining decisions posted as child notes of this Thing, each self-contained
(question, options with wavelet code, trade-offs, one recommendation), for
Logan to answer with updates on each child:
- Decision 3 (lot:033j5kGCR56ZRXqZcJoXOq): bare resource name in type position
  — recommended 3a: bare = own (WIT's reading), borrow(counter) explicit,
  own-consumption modelled in the interpreter.
- Decision 4 (lot:033j5kry0JxFkYeIVrUcGD): destructor — recommended 4a:
  reserved member name `drop`, Fn {self: counter} -> unit, absent = no-op;
  implementation may stage (counter needs none).
- Decision 5 (lot:033j5lUjxlZ78kkwA4cvVq): statics + export surface —
  recommended explicit Static marker with two-way checker cross-check, and
  bare `Export counter` exporting the whole resource as one WIT resource
  (record form for iface placement; unexported = internal).
- Decision 6 (lot:033j5lxbD1LPP8QOpF1Sr6): observable semantics — recommended
  identity equality (Derive on resources is an error) and opaque
  `<counter>` to-string (not form-serializable).

Once Logan answers on the children, a consolidated info update here should
capture the complete DefResource design plus consequences for the gated tasks
(5.12 conformance callee role, 5.8.1 resource-lifting). Implementation is NOT
part of this decision thread — it gets scheduled as its own work.
