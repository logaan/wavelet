---
status: work
update-id: lot:033iwmoDZvSLm3JUF0xIBq
work-at: 2026-07-03T07:34:01.076106+00:00
---
Implemented (commit 8c46a47): synthesize_world_wit now emits `use <pkg>/<iface>@<ver>.{names};` inside a local interface for every dep-declared type its rendered signatures (and local DefType fields) reference. Dep gained type_ifaces (type name -> declaring interface), filled by witdep for parsed WIT deps and by build.rs for sibling .wlt deps (api). The use path is versioned from the resolved dependency — the "name `point` does not exist" failure is gone, and the goal-5-flagged @0.2.0 hardcode is NOT used for these (it remains only the wasi fallback when no dep resolves). tests/cross_package_sigs.rs covers dep records/enums/variants/aliases referenced directly, inside list/option constructors, and from local record fields.

Two scope notes: (1) `wavelet wit` (the single-file CLI display) still prints without use lines — it has no dep resolution; the build path is what gates the callee role. (2) Full roundtrip.wlt callee compilation ("exporting into a foreign interface is all-or-nothing") remains gated on the goal-5 representation gaps (f32, flags literals, char arithmetic) and 4.5 resources — the type-reference gap this Thing tracked is closed.
