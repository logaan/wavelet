---
status: work
update-id: lot:033ixCKZhcwmusdm9YwnfU
work-at: 2026-07-03T07:50:47.498275+00:00
---
Implemented (commit 2dff510): the element record hoisting the note anticipated. When an export references a functor handle whose element is a local type, the element's declaration (plus any local types it references, transitively) is hoisted into a shared `types` interface that both `api` and the functor interface `use` — no cycle, and the rejection at the old emit.rs:7977-7988 site is deleted. The hoist computation lives in wit::hoisted_types and is shared by BOTH WIT renderers (wavelet wit and the encoder path), so they cannot disagree; the world exports `types`.

Verified end to end: tests/backend_functor.rs::record_element_handle_return_hoists_the_element builds the docs' nearest-set shape, calls the returned own<set> handle's size/contains over record values from the wasmtime host (dedup by structural equality, matching the interpreter), and drops it cleanly; tests/functor_runtime.rs's old rejection test now locks the hoisted WIT instead. type-system.mdx updated (worked example shows the hoisted synthesis; the "shapes that build" section no longer lists this as rejected).
