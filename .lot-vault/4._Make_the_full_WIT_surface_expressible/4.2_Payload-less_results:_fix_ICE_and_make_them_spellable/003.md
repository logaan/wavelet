---
status: work
update-id: lot:033iwVCOB1L0Ivf4TEaOPQ
work-at: 2026-07-03T07:22:26.643386+00:00
---
Implemented (branch goal-4-wit-surface, commits ad22606 + 8ff432f), interpreter first and honouring the 2.4 unit-drop — payload-less cases are genuinely payload-less end to end:

- Spelling: a standalone `_` now lexes (the WIT absent-arm placeholder), so `result(_ e)`, `result(t)`, and bare `result` are all writable type text; wit synthesis renders them as result<_, e> / result<t> / result.
- Construction: ok()/err() with no arguments build Variant(case, None) in the interpreter AND the backend (no unit/empty-tuple payload). (ok)/(err) call-shaped patterns destructure them; bare `ok`/`err` sym patterns stay binders (those names are the constructor builtins) — a new sym_matches_nullary_case keeps checker and interpreter on one rule.
- Checker: absent arm carried as Type::Unit inside Result across parsing (type forms + WIT text), rendering, ctor typing, case payloads, and exhaustiveness; a gradually-unknown payload is coverable by the nullary pattern shape (the runtime accepts it).
- The recorded ICE is fixed: a bare result flattens to a lone i32 discriminant, and lift() now handles single-flat variants (was `unreachable!("never a single flat value")`).
- Acceptance met on the caller side: the conformance runner now spells `result<_, list<string>>` honestly, returns payload-less ok(), and includes result-rt (both flips) plus the payload-less sides of result-u32-rt / result-string-err-rt. run()/run-values()/run-resources() pass against both rust-a and rust-b callees. tests/payloadless_results.rs locks evaluation, WIT synthesis, and a wasmtime host round-trip.

One incidental fix worth flagging: `Quote (1 2)` (the docs-blessed literal-tuple spelling) had been rejected by the checker at tuple-typed boundaries since 3.7 typed Quote as `tree` — the runner on main does NOT currently build because of it. `tree` now unifies gradually with tuple/list types. The callee-side acceptance (roundtrip.wlt exporting these) remains gated on 4.3.
