---
status: info
update-id: lot:033ixXb0or01iKqYblM6zP
info-at: 2026-07-03T08:04:46.060659+00:00
---
Goal-4 execution summary — branch goal-4-wit-surface, pushed to origin (9 commits, 82a8913..8b725fa), ready to merge. Interpreter first throughout; cargo test green (24 suites incl. new case_constructors / payloadless_results / cross_package_sigs); regen-examples run; CHANGELOG + docs + grammars + LSP updated.

IMPLEMENTED
- 4.1 Case constructors: DefType variants/enums bind their cases (nullary as values, payloaded as first-class constructors); dep cases reachable via alias (t/circle(1.0), t/north) in interpreter and backend; all-nullary DefTypes synthesize WIT `enum`; backend bare-sym case patterns now equality-match (a real interp/backend divergence removed).
- 4.2 Payload-less results: `_` lexes; result / result(t) / result(_ e) spellable; ok()/err() genuinely payload-less (2.4-compliant); (ok)/(err) patterns; checker carries absent arms as Unit end-to-end; the "never a single flat value" ICE fixed (bare result = lone i32 disc; lift handles single-flat variants).
- 4.3 Cross-package sigs: synthesize_world_wit emits `use <pkg>/<iface>@<ver>.{names};` for dep types referenced by local sigs/type decls (Dep.type_ifaces); versions come from the resolved dep, not the wasi @0.2.0 hardcode.
- 4.4 Dep type aliases: witdep records named aliases; TypeEnv expands them; sibling deps carry their full type surface; checker treats unresolved dep nominals gradually against any type. points-rt restored.
- 4.6/4.6.1 (decided option 1): dedicated `Instantiate {pkg: … with: {elem: t} as: …}` with by-name args; Import-of-functor is an actionable error. 4.6's *generalization* to user-defined functor templates deferred (needs 4.5's resource surface first — noted on the Thing).
- 4.7 Cycle fix: functor element records hoist into a shared `types` interface (wit::hoisted_types, shared by both renderers); the docs' nearest-set shape now builds and its returned own<set> handle works from the host.

CONFORMANCE: the wavelet runner now spells result<_, list<string>> honestly and adds shape-rt, direction-rt, option-shape-rt, result-rt both flips, payload-less result sides, result-tuple-direction-rt err, points-rt. run()/run-values()/run-resources() pass against rust-a AND rust-b. (Also fixed en route: Quote (1 2) tuples were falsely rejected at tuple boundaries since 3.7 — the runner didn't build on main.)

OPEN
- 4.5 user resources: the declaration spelling is a genuine surface decision with no decision note — question/options/recommendation posted on lot:033iXNJD5h2KcdEuOxFeOb (recommend a dedicated DefResource form). Implementation awaits your call.
- roundtrip.wlt callee role still blocked only by goal-5 representation gaps (f32, flags literals, char arithmetic) + 4.5.
