---
status: work
update-id: lot:033iwAG0Pv5epvlv3i8yXj
work-at: 2026-07-03T07:08:40.836857+00:00
---
Implemented on branch goal-4-wit-surface (commits 82a8913, 52fb589), interpreter first:

- Evaluating a DefType now binds its variant/enum cases in the value namespace: nullary cases as payload-less Variant values (like `none`), payloaded cases as first-class CaseCtor values. Works in eval_snippet (playground/docs), `wavelet run`, and the module runner; sibling-dep cases bind under the import alias (`alias/case`, plus bare with `open: true`).
- The checker types a payloaded case used as a value gradually (first-class constructor), and now rejects *calling* a nullary case (`north()`), matching the interpreter's "not callable".
- wit.rs type_decl synthesizes a WIT `enum` for an all-payload-less list DefType (variant otherwise); emit's local_non_record_types mirrors with TypeDef::Enum. Same lone-i32-discriminant ABI.
- The wasm backend constructs cases in agreement: bare local nullary case names -> payload-less variant boxes; payloaded case calls -> variant boxes; dep cases via `alias/case` (both call and bare-value positions, keyed off Dep.type_defs). Backend bare-sym patterns naming a local nullary case now match by equality (was: bound as a catch-all — a real interp/backend divergence this change removes).

Tests: tests/case_constructors.rs (9 interpreter + 1 boundary round-trip via wasmtime host: enum rotate, variant extend/mk-days). Full cargo test green. Remaining for this Thing: conformance-suite acceptance (shape-rt/direction-rt/option-shape-rt/result-tuple-direction-rt err side) is gated on 4.3 cross-package sigs for the callee role; ctor-as-value in the *backend* is still rejected with a clear error (closure wrapper needed — goal-5 material).
