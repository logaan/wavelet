---
status: info
update-id: lot:033iNqOq2RRKtaYN2PYGkn
info-at: 2026-07-02T07:50:11.569149+00:00
---
Stage 1 is complete and ready for review: https://github.com/logaan/wavelet/pull/27
(draft, branch worktree-wit-roundtrip-conformance).

What exists now, all under `conformance/` (own cargo workspace, zero wavelet
knowledge):

- `wit/world.wit` — package `roundtrip:suite@0.1.0` covering every feature on
  the WIT design page, with the normative transform table in doc comments.
  Validated by `wasm-tools component wit` on every harness run.
- `suite/` — the symmetric Rust artifact (exports the suite as callee, exports
  a `runner` driving the imported suite as caller). `seed-b` cargo feature
  selects the second seed table -> `dist/roundtrip-a.wasm` +
  `dist/roundtrip-b.wasm`. Seed B stresses edges (wrapping maxima, empty
  strings/lists, none/err sides, U+10FFFF).
- `stub/` — exports-only chain terminator. A symmetric callee keeps its own
  imports, so pairings compose as caller ∘ (callee ∘ stub); the stub is never
  actually called.
- `test.sh` — build + compose (wac plug) + run (wasmtime run --invoke).
  `./test.sh` = self-test both directions; `./test.sh CALLER.wasm CALLEE.wasm`
  swaps any implementation into either role.

Verified:
- Self-test passes both directions (rust-a -> rust-b, rust-b -> rust-a).
- Negative test: a deliberately wrong u8 transform is caught with
  `err(["u8-rt: want 11, got 12"])` and exit 1.

Proceeding to Stage 2 (lot:033iNRSfQ6bJV5pGM2v1Ei) without waiting, per your
instruction.
