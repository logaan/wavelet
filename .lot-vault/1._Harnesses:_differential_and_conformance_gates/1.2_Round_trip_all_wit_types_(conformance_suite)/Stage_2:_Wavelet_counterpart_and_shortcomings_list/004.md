---
status: info
update-id: lot:033iPURFTUgNZ5lFmKDcmq
info-at: 2026-07-02T08:57:15.338876+00:00
---
Stage 2 complete and ready for review: https://github.com/logaan/wavelet/pull/28
(draft, branch worktree-wit-roundtrip-wavelet, stacked on PR #27).

Deliverables:
- conformance/wavelet/src/runner.wvl — wavelet as CALLER; builds and runs the
  suite against both rust callees. Result: all 10 resource checks pass; 25
  values checks pass; 3 fail documenting one live backend bug (byte-width
  payload corruption on lift). Identical across rust-a and rust-b.
- conformance/wavelet/src/roundtrip.wvl — the CALLEE target, kept unbuilt as
  the acceptance test for stage 3.
- conformance/wavelet/README.md — roles, commands, current results.

The shortcomings list (every item probed in isolation; evidence on the step
2.3/2.5 updates) is now 11 child tasks under Stage 3
(lot:033iNRmqy5xh0UCmKb6G67), roughly priority-ordered:
 1. Byte-width payload lift corruption (correctness bug; the 3 red checks)
 2. char boundary trap + missing char<->int builtins
 3. f32 in the wasm backend
 4. Payload-less result ICE (emit.rs:2884) + unspellable result<_,e>/result<t>
 5. Flags in the backend (literals + flg)
 6. Variant/enum case constructors (+ enum synthesis nuance)
 7. Cross-package type references in signatures (gates the callee role)
 8. Dep type aliases in the backend
 9. Full u64 range (needs a design decision — will ask before starting)
10. Backend stdlib parity with the interpreter
11. User-declared resources (export the counter)

Per the agreed process I'll proceed into Stage 3 starting with #1 (the
correctness bug) unless you reprioritise.
