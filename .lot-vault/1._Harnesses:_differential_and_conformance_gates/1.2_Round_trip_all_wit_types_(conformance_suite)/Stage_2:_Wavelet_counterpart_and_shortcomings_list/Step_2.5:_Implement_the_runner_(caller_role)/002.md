---
status: work
update-id: lot:033iPOi1zKILBIXb1cG4m4
work-at: 2026-07-02T08:53:29.410092+00:00
---
Caller role WORKS end-to-end. conformance/wavelet/src/runner.wvl builds to
out/conformance-wavelet.wasm: imports roundtrip:suite/values+resources,
exports roundtrip:suite/runner with the dep's declared types (the local
`result(bool list(string))` spelling is ABI-compatible stand-in for
result<_, list<string>>, which is unspellable — '_' doesn't lex).
Composition: wac plug rust-callee --plug stub, then wac plug wavelet-runner
--plug that; wasmtime run --invoke 'run()' works (probe returned ok).

Failure collection had to be restructured twice for backend gaps: no
concat/push -> recursive collect() over a literal list of check() strings
with str-cat accumulation; no tup() -> Quote (1 2) tuple literals (work!);
no none() -> option-none check dropped.

Per-check isolation results (wavelet caller -> rust-a callee), bisect script
at ~/.claude/jobs/fd0efea3/tmp/bisect.sh:
- PASS: bool, s8, s16, s32, s64, u8, u16, u32, u64(<=2^53 seed), f64,
  string, list-string, list-list-u8, result-u32 ok, result-string-err err,
  result-u32-string ok+err, tuple-rt (Quote), tuple-nested-rt (Quote),
  point, awkward, no-params, no-result, no-params-no-result, multi-param,
  ALL TEN resource checks (constructor/next/value/static sum/make/bump
  borrow/take own/counter-round-trip/counter-to-point).
- RUNTIME TRAP: char-rt (lowering or lifting a char at the boundary traps —
  wasm unreachable).
- WRONG RESULT (check reports failure, no trap): list-u8-rt ([1 2 3] in,
  expected [2 3 4] not eq); option-u8-rt some(7)->some(8) not eq;
  result-tuple-direction-rt ok(Quote (1 2))->ok(Quote (2 3)) not eq.
  (Unclear yet whether value corruption or backend eq weakness on these
  shapes; needs one diagnostic: e.g. return to-string of the response.)
