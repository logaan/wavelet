---
status: work
update-id: lot:033iyyi4CT5HcuT7jGlgRJ
work-at: 2026-07-03T09:03:20.145446+00:00
---
5.2 started on goal-5-abi-backend (bec1f74, 59523e1). Landed so far:

1. Foundation: check.rs now exports a per-node static-type table
   (NodeId -> Type, check::node_types_with_imports); emit_core_module computes
   it on the exact (overload-rewritten) arena it emits, with the same import
   signatures the build path checks with.
2. Unboxed scalar expressions: literals, arithmetic, comparisons, eq/not/neg,
   and If conditions with statically-known scalar operand types now compile
   unboxed (i64/f64/i32 on the wasm stack), boxing once at the seam to the
   remaining boxed world. Scalar kinds mirror the interpreter's value
   variants — every int type is the one i64 Value::Int domain (u64 full-range
   residue from 3.4 therefore still open, unchanged), floats are f64 (f32 is
   boundary-only, matching the interpreter).

Remaining for this Thing: typed wasm locals for Let/Match bindings; typed
internal function signatures (flat params instead of box pointers) plus the
export-wrapper fast path that stops boxing scalar boundary args; WitTy::F32
at the boundary (the retired f32 child's plan, rides the conformance queue);
the u64-full-range decision. Parity pinned by new tests/typed_scalars.rs
(overflow/div-0 traps, f64-widened int compares at the 2^53 edge, wrapping
neg, NaN order trap, NaN eq, char codepoint order); full cargo test green.
