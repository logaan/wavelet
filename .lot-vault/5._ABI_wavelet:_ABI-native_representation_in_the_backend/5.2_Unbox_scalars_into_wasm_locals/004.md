---
status: work
update-id: lot:033izzz7dxTU8EuvzDXWO5
work-at: 2026-07-03T09:44:55.315772+00:00
---
Typed internal function signatures landed (c317817) — the biggest remaining
5.2 lever. Defs carry a representation signature: declared-scalar params are
unboxed i64/f64/i32 slots; scalar-typed bodies return unboxed; control forms
(If/Do/Let/Match) thread the typed result through tail position and same-repr
tail calls stay return_call (million-deep recursion test pins constant
stack). Export wrappers convert flat ABI values straight to typed params
(extend/promote) and typed results straight to flat form (wrap/demote) — a
fully-typed scalar def now runs box-free end to end. Match bare-name binders
over scalar scrutinees bind typed locals. Uniform closure wrappers box/unbox
at their seam so defs-as-values still work.

5.2 remaining: deep Match binds (list/record element binders — representation
comes with 5.3/5.5 canonical layout, where element types exist), and the
u64-full-range residue (needs its own decision; unchanged). Full suite +
differential + conformance matrix green.
