---
status: work
update-id: lot:033kpnrk70OAILdIKgFLOt
work-at: 2026-07-06T15:02:14.713563+00:00
---
Session 10 CHECKPOINT (branch goal-5-abi-backend; pushed, origin tip ad21c2e,
2 commits over base aa063fe; full cargo test green [35 suites, 0 failures] +
full conformance matrix green with the release compiler: wavelet caller ->
run()/run-values()/run-resources() = ok vs rust-a AND rust-b, resources callee
PASS both. Never pushed main; worked only in the worktree; all edits via
Bash/python patches since Edit/Write are blocked "parent not isolated").

=== 5.6 REMAINDER OPENED (per orchestrator mandate) — two verified slices ===

Sessions 8/9 declined to open this block as "not background-safe"; session 10
opened it deliberately using the prescribed verification discipline
(instrumented builds proving each new path fires + output unchanged, then
instrumentation removed and the pre-instrumentation state committed first;
oracle/differential/conformance gates green every slice). Both commits are
pure behavior-preserving representation optimizations -> NO CHANGELOG entry
(not user-visible), NO regen-examples (differential harness compares
dynamically; examples run the interpreter only), NO grammar/docs impact.

COMMIT baf59e3 (5.4 residue item 2 — bare nullary-case pattern -> disc matcher):
  A bare `none` (or a DefType case registered payload-less) matched against a
  canonical Repr::Mem variant scrutinee previously fell through pattern_top_mem
  (src/emit.rs) to the `_` arm, which reboxed the whole variant and ran the
  boxed string-compare matcher. Added an arm routing it to pattern_mem_var as a
  one-element pattern slice `&[pat]`: the `(0, None)` arm matches on the numeric
  discriminant alone — provably equivalent to the boxed matcher's "TAG_VAR +
  name eq + payload absent" for a payload-less case (verified against the boxed
  `pattern` at emit.rs:3018 and the interpreter). Gated on
  `ty.variant_cases().is_some()` so a bare nullary-case Sym over a NON-variant
  scrutinee still reboxes (correct). INSTRUMENTATION: env-gated eprintln in the
  new arm fired `name=none` when compiling backend_variants `get`
  (`Match vr/find(b) [((some x) x) (none -1)]`, vr/find dep-born canonical
  option); get(true)=7, get(false)=-1 unchanged. This is exactly the path the
  existing test `option_cases_match_like_the_oracle` documented as "bare `none`
  falls back to the rebuilt-box matcher" — now takes the disc fast path.
  NOTE: the identical `_`-arm rebox still exists in pattern_mem_field (emit.rs
  ~2886) for NESTED nullary-case fields (`{f: none}`); left for a follow-up
  because it is niche and needs a new component test to exercise (see docket).

COMMIT ad21c2e (5.6 item 1 — first type-indexed compound builtin: `len`):
  When `len`'s operand is statically a canonical Repr::Mem LIST, read the length
  word of its (ptr, len) area directly (I32Load ma(4,2)) via
  `node_mem`+`expr_mem`, instead of reboxing and calling len_raw. KEY ORACLE
  FINDING that scopes the slice: only LISTS qualify. A string's `len` is
  `s.chars().count()` (builtins.rs:348) — the CHAR count, NOT the byte length
  the canonical (ptr, len) word stores — so strings MUST keep boxed len_raw; a
  tuple's canonical layout is inline fields with no len word, so tuples too.
  Gated `matches!(mem_tys[mt], WitTy::List(_))`. INSTRUMENTATION: env-gated
  eprintln in the canonical branch fired for the new test `count = len(lv/nums())`
  (dep-born canonical list(s32)); result S64(3) unchanged. tests/backend_lists.rs
  gains the `count` export + `len_over_canonical_list_reads_the_length_word`.

=== NOT STARTED / DELIBERATELY NOT LANDED (rest of 5.6 remainder) ===

The other item-1 compound builtins are NOT single-read slices like `len`; each
needs recursive/allocating structural machinery over canonical layouts and is
genuinely part of the larger box-retirement, so none were opened half-done:
  - `eq`/`compare` over canonical: structural, field-order-observant, recursive
    (records compare field-by-field at offsets; lists by length+elements) —
    needs a generated per-type or runtime canonical-structural comparator.
  - `str-cat` over canonical strings: must allocate a new buffer and memcpy both
    (ptr,len) operands; result representation (box vs canonical (ptr,len)) is a
    design choice.
  - `head`/`tail` over canonical lists: head must materialize element[0] at its
    natural repr; tail must allocate a new (ptr,len) list minus the first
    element (stride copy).
  - float/char to-string (session 9's deferred to-string tail) — untouched;
    Rust {f:?} shortest-round-trip float formatting + {c:?} char escaping are
    infeasible to hand-write in wasm and not exercised by any non-float/char
    documented example (differential SKIP still exactly the 2 float entries).

TAG_* CONSUMER RETIREMENT (item 2) — NOT STARTED. TAG_STR/LIST/REC/VAR/TUP
(+FLG/CHAR/CELL) consumers still live; retiring the box representation is the
multi-session overhaul the eq/compare/head/tail/str-cat canonical work rides
into. TAG_VAR retirement rides here.

5.1 EVACUATION GLUE + FUNCTOR/RESOURCE OPT-OUT REMOVAL (item 3) — NOT STARTED;
rides the retirement.

5.4 RESIDUES (item 4):
  - dep Qsym ctor calls in ctor_parts: INVESTIGATED, NOT reachable cleanly. A
    Node::Qsym head in `call` (emit.rs:2429) ALWAYS routes to dep_call (an
    imported function call); the interpreter (interp.rs:89) resolves `alias/name`
    by plain env lookup — there is NO case-constructor resolution for qualified
    names in either the backend or the oracle, and no test/example/docs uses
    `t/east(5)`-style qualified case construction. So this residue does not
    "ride along" as prescribed; it would require a language-design decision on
    whether imported DefType cases are exposed as qualified constructors before
    any codegen. Recommend dropping it or escalating to Logan, not chasing it.
  - bare-Sym case pattern opt: DONE at top level (baf59e3); nested-field variant
    (pattern_mem_field) remains (see baf59e3 note above).
  - large types (>256 cases / >32 flags): per-type disc/bitset sizing still
    unimplemented; canonical gates (ctor_admissible emit.rs:4839 and the
    variant gates) hard-refuse >256, store/load/pattern assume 1-byte disc.
    Not reachable by any current test; unchanged.

5.7 (Do+If stdlib split), 5.8 (defunctionalization), strict-flip check, 5.11
(boundary no-ops), 5.12 (values callee) — all NOT STARTED. Session 9's
five-stage scoping finding for 5.7 (If/Do wired across reader/interp/check/wit/
emit; needs regen-examples + three grammars + docs; dedicated keystone) still
stands — do NOT open mid-budget.

VERIFICATION SUMMARY: cargo test 35/35 green after each slice; differential
harness (every_documented_example_agrees_with_the_compiled_artifact) green;
rebuilt the RELEASE compiler and ran the full conformance matrix on this exact
tree (wavelet caller run/run-values/run-resources -> ok vs rust-a AND rust-b;
resources callee PASS vs both). Restored the build-touched
conformance/wavelet/wit/wavelet.wit side-effect; tree clean.

NEXT SESSION (ordered docket for session 11):
  (1) 5.6 remainder — the STRUCTURAL compound builtins. Best done as a block
      with the TAG retirement: build a canonical-structural comparator (drives
      eq/compare over records/lists/variants/strings), then head/tail/str-cat
      over canonical lists/strings, then retire TAG_* consumers + 5.1 evacuation
      glue + functor opt-out. Each slice: instrument the new path, prove it fires
      + output unchanged, remove instrumentation, commit green, re-run
      differential + conformance. `len` (ad21c2e) is the template for the
      node_mem+expr_mem gating pattern; the bare-case arm (baf59e3) for the
      pattern side.
  (2) The two small ride-alongs if opening the retirement: pattern_mem_field
      nested nullary-case arm (mirror baf59e3, add a nested-field component
      test); float/char to-string if the layout work makes them cheaper.
  (3) 5.7 Do+If stdlib split as a dedicated keystone (five-stage finding).
  (4) 5.8 defunctionalization. (5) strict-flip check. (6) 5.11. (7) 5.12 values.

PROCESS: worktree
/Users/logaan/code/personal/rust/wavelet/.claude/worktrees/goal-5-abi-backend
throughout; Edit/Write blocked so all edits via Bash/python patches; no PR
opened (orchestrator integrates the branch and runs the full suite).
