---
status: work
update-id: lot:033j3yvSMIK1mUGJLpzYEH
work-at: 2026-07-03T12:27:13.104861+00:00
---
Session 3 continued: two more commits, all green (308 tests + conformance
matrix against both rust callees):

- bad296a: defs carry Mem RESULT signatures. A static prediction walk
  (predict_body_mem — record literals, Mem-bound names, If/Do/Let/the,
  dep calls; it mirrors let_form's binding decisions exactly via a
  simulated scope, MemLookup::Sim) fixes the result repr before bodies are
  emitted. The export wrapper gained the 5.3 payoff: when the def's layout
  equals the boundary type, the retptr path returns the def's canonical
  pointer directly — an exported record result is constructed exactly
  once, in place, zero boxes, zero copies (verified firing for annotated
  defs; s64-laid literal fields against narrow boundary fields keep the
  rebox path). The uniform closure wrapper and internal-call seams rebox
  faithfully.
- 196fe7a: dep-call record results are BORN canonical — the import's
  retptr area is already the value in declared field order (identical to
  the interpreter's boundary lift), so Let/Match consumers skip the box
  lift entirely and destructure the import's result by offset. Pinned by a
  composed two-component test.

REMAINING on 5.3 (recorded residue for the next session):
1. Tuples: wit mapping + gate plumbing exist, but no native construction
   (the language has no evaluated tuple literal; tuples arise from Quote/
   bundling/boundary lifts), no pattern_mem tuple destructure, and the
   dep-born gate is records-only. The natural driver is dep-born tuples +
   a pattern_mem_tup mirroring pattern_mem_rec; needs an executable test
   source for tuple values (the conformance rust callees produce them).
2. Mem def PARAMS: sound only with an all-call-sites eligibility check
   (box->mem at a call seam can normalize field order and diverge from the
   oracle — the order-observability constraint recorded earlier). Export
   record params could go canonical wrapper-side without that analysis.
3. Records inside variants/options/lists (mem_field_ok refuses them) ride
   5.4/5.5; flags fields are order-observable too (Value::Flg is a Vec) —
   treat like records when 5.4 lands.
4. Match clause results are excluded from body prediction (only If/Do/Let
   thread Mem results today).
5. TAG_REC/TAG_TUP still exist as the seam representation; they retire
   only once every producer/consumer is canonical (with 5.5/5.4), which is
   also when the 5.1 evacuation glue and functor opt-out removal land.
