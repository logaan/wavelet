---
status: work
update-id: lot:033j4mkYhC723j34OnHbnT
work-at: 2026-07-03T12:59:57.857031+00:00
---
Session 4: the tuple half landed (1261b79, 314 tests green + conformance
matrix green against both rust callees). Tuples join the Repr::Mem overlay:

- dep-born canonical tuples (dep_result_mem_ty/can_mem_as/node_mem_ty admit
  non-empty tuples; dep_call's want_mem path was already type-generic).
- pattern_mem_tup destructures canonical tuple scrutinees element-wise at
  despec offsets; length mismatch = static clause failure; nested
  records/tuples take interior pointers (mem_field_binding extended).
- ORACLE-PARITY BONUS: over a statically-tuple Mem scrutinee, a Sym-headed
  tuple pattern destructures element-wise like the interpreter (the boxed
  matcher's variant-only reading — the 5.9 residue — is fixed on the
  canonical path; the boxed path's residue still rides 5.4).
- Export retptr fast path verified for tuples (layout-equality gate was
  already type-generic): a def forwarding a dep tuple crosses two
  boundaries with zero boxes/copies.
- No construction side, by design: the language has no evaluated
  tuple-literal spelling, so dep-born results are the only producers.

tests/backend_tuples.rs (6 wac-composed tests) pins it; each fails against
the previous emitter.

Residue unchanged otherwise: Mem def params (all-call-sites analysis),
Match clause results excluded from body prediction, records/tuples inside
variants/options/lists (5.4/5.5), TAG_REC/TAG_TUP retirement with 5.5/5.4
+ 5.1 evacuation glue.
