---
status: work
update-id: lot:033j3eFQ6cpGVpfHrnDp51
work-at: 2026-07-03T12:13:37.694136+00:00
---
5.3 underway on goal-5-abi-backend (session 3). Three commits landed, all
green (305 tests + live conformance matrix):

- 14fa3d5: Option<Scalar> threading generalized into a Repr enum
  (Boxed | Scalar | Mem) across FnSig/Binding/control-form wants/export
  wrappers — the extension point the checkpoint called for. Binding stays
  Copy: Mem carries an index into an Emitter-interned WitTy table (MemTy),
  answering the recorded Copy-vs-Clone question.
- f207367: canonical-layout record BINDINGS. A Let-bound record literal
  whose construction is provably faithful to its static type lives as a
  pointer to despec-computed canonical layout (no TAG_REC box; field names
  compile-time only). Scalar fields store unboxed at WIT width; nested
  records construct in place; strings via the store_to_mem seam; boxed
  consumers rebuild via load_from_mem at reference/capture/result seams.
  KEY SEMANTIC CONSTRAINT discovered and honoured: record field order is
  observable (Value::Rec eq/to-string are positional, and the backend's
  eq_raw matches), so the gate (can_mem_as/mem_field_ok) only admits
  literals whose order matches the layout's, with losslessly-storable
  fields (exact static int ranges for narrow widths; f32 fields refused —
  demotion would lose the f64 the oracle keeps; IntLit/FloatLit fields lay
  out as s64/f64, the interpreter's full value domains).
- 260709e: Match DESTRUCTURES canonical scrutinees at despec offsets —
  scalar binders load typed locals, nested record binders take interior
  pointers (headerless, per 5.1), statically-absent pattern fields branch
  the clause out; any other pattern reboxes only what it inspects and
  delegates to the uniform matcher. Field access is now a constant-offset
  load end to end for Let+Match, the record's core consumption path.

tests/backend_records.rs pins the faithfulness properties; oracle answers
were verified against the interpreter directly.

Still open on 5.3: def result/param representation signatures (Mem results
for defs whose body is provably faithful + the export retptr fast path —
next), tuples (no literal spelling in eval position; they arrive Mem-born
from boundary lifts, coupled with 5.5/5.11), records inside variants/lists
(rides 5.4/5.5), and the 5.1 evacuation glue once value construction is
canonical end to end.
