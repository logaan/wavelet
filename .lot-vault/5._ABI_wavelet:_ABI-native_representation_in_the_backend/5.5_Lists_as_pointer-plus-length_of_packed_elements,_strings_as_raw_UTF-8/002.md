---
status: work
update-id: lot:033j5AZLE64OFVarEoT6FT
work-at: 2026-07-03T13:15:37.131501+00:00
---
Session 4: two 5.5 slices landed on goal-5-abi-backend (016b4f6, 0ae2135;
328 tests green + conformance matrix green against both rust callees),
following the Repr::Mem overlay recipe (boxed fallback always sound):

1. DEP-BORN lists and strings (016b4f6): a dep call whose declared result
   is a list or string joins the canonical-area path — the retptr area
   already carries the canonical (ptr, len) pair (packed elements at
   canonical stride / raw UTF-8), so Mem-wanting consumers take the area
   pointer with no lift and no boxes. pattern_mem_lst destructures
   canonical lists: RUNTIME length check (a value property, unlike tuple
   arity), then element sub-patterns at stride offsets; records inside
   lists destructure in place via interior pointers. String/list fields
   inside canonical records/tuples bind as interior (ptr, len) pointers.
   Exports forwarding dep lists/strings take the retptr fast path.

2. CONSTRUCTION (0ae2135): a list literal whose elements all store
   losslessly at the static element type packs canonically (elements at
   stride into a fresh buffer; the value is its (ptr, len) pair). List
   order is positional — no order-normalization hazard, so the gate is
   just per-element lossless storability (exact ranges; order-faithful
   record elements; recursive nested lists; string elements point into
   their interned boxes zero-copy). Record literals accept list fields;
   record elements pack inside list buffers (the records-inside-lists
   direction of 5.3's residue collapses). List-literal def bodies predict
   Mem results and export through the fast path.

tests/backend_lists.rs pins both slices (8 wac-composed + 6 single
-component tests); compile-time instrumentation confirmed every new path
fires (these slices are representation-only, so tests pin faithfulness).

REMAINING on 5.5:
- Strings/lists as the UNIVERSAL representation (retiring TAG_STR box
  headers and TAG_LIST boxed-pointer arrays everywhere) waits for 5.4 +
  5.6 (type-indexed builtins) — until then boxes remain the seam/fallback
  representation and literal strings stay interned boxes.
- Mem-bound names as record/list fields in literal construction (the
  gate admits literals only; a bound name would need a mem-to-mem copy).
- list<f32> stays boxed (demotion would lose the oracle's f64).
- Builtins (len/head/tail/nth/eq) still consume boxes via the rebox seam;
  type-indexed compiled forms ride 5.6.
