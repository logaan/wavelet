---
status: work
update-id: lot:033kvZoG1ef2DMUqossDAk
work-at: 2026-07-06T18:57:29.756843+00:00
---
Implementation begun (session 14, branch goal-5-abi-backend, tip f5ea328).
Landed per this Thing's update-003 design: Phase D arrow types in the checker
(8c492ad — Fn literals / bare single-sig def refs / indirect applications
statically typed; gradual stays Unknown); the boundary rule + oracle parity
(80b18b2 — exported function-typed results are a deliberate compile error with
this design's diagnostic, identical from check_roots and WIT synthesis via
InferredWit::Func); the zero-capture devirtualization (5c635b1 — Let-bound
bare-def applies compile to direct calls, instrumentation-proven); docs
(f5ea328 — type-system.mdx "Function values" section).

Remaining: Fn-literal (capture) devirtualization and the multi-case
variant+dispatch with the naming scheme — the exact positional-reservation
execution plan is in the goal-5 Thing's session-14 checkpoint
(lot:033kvZMvv5HhRdg0nah7gu). Note for the strict-flip expectation in this
design: arrow types proved necessary but NOT sufficient for the 3.9 gate — the
17 backlog examples are authored with untyped params, so they need either
expected-arrow-directed param inference + typed higher-order builtin sigs, or
typed example rewrites (decision escalated in the checkpoint).
