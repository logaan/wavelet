---
status: work
update-id: lot:033kwNy9X5qbb1P1MdQ5nx
work-at: 2026-07-06T19:30:27.734356+00:00
---
Session 15 (branch goal-5-abi-backend, tip 679f56a): Fn-literal CAPTURE
devirtualization LANDED (83c11d4), completing the devirtualization half of this
Thing's design item 1 (single-case / bare-def zero-capture landed session 14;
now inline Fn-literal Let inits with captures too). An inline `Fn` literal bound
directly as a `Let` init whose checked type is a concrete arrow is lambda-lifted
to a top-level core function; the apply compiles to a direct `call` (captures
pushed verbatim as leading typed params, then boxed value args) instead of a
closure box + TAG_FN funcref dispatch. Executed exactly on the positional-
reservation plan in the goal-5 session-14 checkpoint (scan_let_lambdas pre-scan
-> reserve after the overload block / before exports -> compile_known_lambda
stashes during def emission -> push in reservation order between overload and
export bodies, position == reserved index asserted). Boxed closure still emitted
for non-apply uses; macro-component path keeps the boxed path. cargo test 36
suites 0 failures; full conformance matrix green.

REMAINING on this Thing: the MULTI-CASE variant+dispatch (distinct function
values into one binding) + the deterministic naming scheme. DELIBERATELY
DEFERRED to the 5b evacuation session rather than built standalone — see the
goal-5 session-15 checkpoint for the reasoning: without TAG_FN retirement it
adds a parallel representation with zero observable change and no simplification
(retirement is gated on it PLUS total typing), so it belongs WITH retirement,
not before it. The immediately-paying devirtualizations (zero-capture, single-
case, capture) are all done.
