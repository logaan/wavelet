---
status: work
update-id: lot:033j0cTttxJR1bwlCnOwtb
work-at: 2026-07-03T10:10:13.371904+00:00
---
Session 2 checkpoint (branch goal-5-abi-backend, 2 new commits over the
merged session-1 work: c317817, 82f939c; pushed; 297 tests green, full
conformance matrix green at tip). Vault re-read first: no new answers from
Logan (5.8 design review silent -> implementation may proceed on the
recorded shape; 4.5 spelling still open -> callee-role counter still gated;
2.7/2.8/2.1 proposals unanswered -> If stays core, proposals untouched).

Landed this session:

- 5.2 is functionally COMPLETE for the pre-canonical-layout world (c317817):
  internal defs carry representation signatures (declared-scalar params =
  unboxed i64/f64/i32 slots; scalar-inferred results return unboxed);
  If/Do/Let/Match thread typed results through tail position; same-repr tail
  calls stay return_call (million-deep recursion test); export wrappers
  convert flat ABI values directly to/from typed reprs — a fully-typed
  scalar def runs box-free end to end. Match bare-name binders bind typed
  locals. Remaining 5.2 crumbs ride 5.3/5.5 (deep binder reprs) and the u64
  decision.
- 5.9 pattern-form coverage complete + 5.10 DONE (82f939c): char/flags
  literal patterns; Qsym pattern error parity; the backend pattern match is
  now exhaustive over reader forms. Import argument bundling works
  end-to-end (checker learned the §4.2 bundle shape it used to preempt;
  dep_call lowers the bundled tuple; conformance gained a bundled tuple-rt
  check, green). 5.9 keeps one recorded residue: Sym-headed tuple patterns
  read as variant-only; type-directed fix scheduled with 5.4.
- 5.1: the persistent-region design question is RESOLVED on the Thing
  (evacuate-at-post-return two-space plan); implementation is mechanically
  coupled to 5.3/5.5 by design.

Next session should start 5.3 (records/tuples canonical layout) fresh — it
is the largest remaining block and needs headroom: TAG_REC/TAG_TUP
replacement ripples through patterns, eq/to-string, rec accessors, the
boundary store/load (which then collapse per 5.11), and the 5.1 evacuation
glue. Then 5.5, 5.4 variants (+ the 5.9 residue), 5.6 compound builtins,
5.7, 5.8 (design is posted and unobjected), strict flip check, 5.11, 5.12
callee role (needs 4.5).
