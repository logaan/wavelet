---
status: work
update-id: lot:033kpFRqS0BD3x2m6tubpV
work-at: 2026-07-06T14:39:37.496018+00:00
---
Session 9 progress — 5.6 to-string slices A+B landed on goal-5-abi-backend
(pushed). Full cargo test green (35 suites, 0 failures) after each slice.

COMMITS over base e54154b:
- 84bbaae emit: quote and escape strings in backend to-string (5.6). The boxed
  to_str helper's TAG_STR arm now surrounds the value with `"` and applies
  Rust-debug escaping of " \ \n \t \r (other bytes incl. UTF-8 continuation pass
  through), matching print_value. Removed 18 now-agreeing TOSTR_STRING entries
  from tests/differential.rs SKIP; dropped the now-unused TOSTR_STRING const.
- bfd76be emit: print compound values in backend to-string (5.6). to_str now
  recurses over compound box layouts to match print_value: lists [a, b], tuples
  (a, b), records {k: v}, variants name / name(payload), flags {a, b}, cells
  cell(v), with nested quoting. New module-level to_str_seq helper emits the
  join loop for the sequence-shaped tags (list/tuple/flags). Removed 21 more
  now-agreeing entries from SKIP (incl. all the form-valued results: sf-quote,
  sf-quasi, sf-splice, sf-unquote, values-quote-days, syntax-quote-call, etc.).

DIFFERENTIAL SKIP now down to 2 TOSTR_TRAP entries, both FLOAT results (sf-def,
std-div-float). Floats and chars are the only to-string cases still trapping:
float formatting to match Rust's {f:?} (shortest round-trip) is infeasible to
hand-write in wasm and char needs UTF-8 encoding + {c:?} escaping; neither is
exercised by a non-float documented example, so deferred. The BUILTIN and
PI/FLAGS SKIP reasons are untouched (different gaps: missing stdlib builtins,
unbound pi, flags-literal).

MECHANISM NOTE for session 10: tests/differential.rs compares the backend's
to-string(Do[..]) output DYNAMICALLY against the interpreter's eval_snippet
(print_value), not against stored expected values, with a ratchet: when a
SKIP-listed example starts agreeing the test FAILS and lists it, so you must
remove exactly those from SKIP. No regen-examples needed for a pure backend
to_str change (examples.json / tests/examples.rs run the interpreter only).
Session 8's regen-examples concern does NOT apply to this path.

IMPORTANT — these two slices are the to-string family of 5.6 only. The larger
5.6 body remains: the compound builtins (eq/compare/len/head/tail/str-cat) still
consume BOXES via rebox (correct today), the TAG_* representation is NOT retired,
5.1 evacuation glue + functor opt-out removal NOT done, and the 5.4 residues NOT
folded in. CHANGELOG Unreleased Fixed entry added for the to-string change.

Next: run the conformance matrix with the release compiler to confirm the ABI is
unaffected, then continue 5.6 (type-indexed compound ops / TAG retirement) or move
to 5.7 (Do+If stdlib split) per docket if 5.6's box-retirement is too large for
the remaining budget.
