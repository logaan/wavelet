---
status: info
update-id: lot:033j0AkKgrm72FAAc8h7l9
info-at: 2026-07-03T09:51:59.679791+00:00
---
Pattern-form coverage is complete (82f939c on goal-5-abi-backend): char and
flags literal patterns compile (structural eq_raw, exactly the interpreter's
match_pattern), Qsym patterns give the interpreter's exact error, and the
backend's pattern match is now exhaustive over every form the reader
produces. Runtime tests pin char-by-codepoint and flags-by-set-names
matching. Bonus from 5.2: a bare-name binder over a scalar-kinded scrutinee
binds a typed (unboxed) local.

One parity RESIDUE found while here, left open on this Thing: a Sym-headed
tuple pattern `(x y)` is read as a variant-case pattern only; the
interpreter disambiguates by the RUNTIME value (destructures a tuple,
case-matches a variant). The honest fix is type-directed (read the pattern
by the scrutinee's static type) and belongs with 5.4's variant
representation rework, where patterns get rebuilt over numeric
discriminants anyway. Not marking done until that lands.
