---
status: work
update-id: lot:033kqLDp9zYnoceXMYt3KS
work-at: 2026-07-06T15:24:10.069929+00:00
---
Session 11 progress — 5.6 structural comparator (item 1) LANDED on
goal-5-abi-backend (pushed, tip 4fa9699 over base 131f83b). Full cargo test
green (35 suites, 0 failures) incl differential.

COMMIT 4fa9699 (emit: type-indexed structural eq over canonical layouts):
When both `eq` operands are the SAME statically-canonical Mem MemTy and the
type is eligible, compare canonical reps field-by-field via a new recursive
comparator `emit_mem_eq` instead of rebox+eq_raw. Arms: scalars (width loads +
Eq), records/tuples (AND of every field at its offset), variants/option/result
/enum (1-byte disc compare, then active-case payload via emit_variant_payload_eq
chain mirroring load_variant_chain), Str (byte-len eq then emit_bytes_eq memcmp
loop), List (elem-count eq then emit_list_eq loop recursing per element at
elem_size stride). Gate mirrors the `len` slice: node_mem(a)==node_mem(b) &&
mem_eq_eligible(ty). ELIGIBILITY excludes Flags (canonical bitset is order-free
but Value::Flg eq is Vec-order-sensitive) and Handle (Rc-identity, not the
opaque i32) — both stay on boxed eq_raw. Field-order hazard neutralized by
can_mem_as requiring record-literal fields in declaration order (a reordered
literal never reaches the same MemTy -> boxed -> correct false), pinned by the
existing backend_records diff-order test.

VERIFICATION: env-gated instrumentation (WLT_EQ_INSTR) confirmed the fast path
fires for kind=record/option/list/tuple; output unchanged; removed before
commit. New runtime tests: backend_tuples::eq_over_canonical_tuples_is_
structural (dep-vs-dep tuple) exercises the tuple arm; backend_variants::eq_
over_canonical_variants_is_structural (option payloads via new vr/opt dep
export) exercises same-disc/payload-equal(true), same-disc/payload-differ
(false), disc-differ(false). All existing eq-over-canonical tests stay green.
Pure codegen — no CHANGELOG, no regen-examples, no grammar/docs impact.

NEXT (this session): item 2 head/tail/str-cat over canonical lists/strings,
then TAG_* retirement. Release conformance matrix next to confirm ABI unaffected.
