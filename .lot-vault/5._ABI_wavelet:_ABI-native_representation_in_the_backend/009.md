---
status: work
update-id: lot:033jDT714U4OIHP3T9EUXs
work-at: 2026-07-03T18:53:47.464106+00:00
---
Session 6 checkpoint (branch goal-5-abi-backend, pushed; origin tip af427f3,
6 commits over base b2639ea; 352 tests green + full conformance matrix green
[wavelet caller -> rust-a AND rust-b, run()/run-values()/run-resources() all
ok]). Note: origin/main advanced to f7cae89 during the session (other
processes' vault commits); my branch is still based on b2639ea and its diff is
src/docs/tests only, so it should integrate cleanly. I never pushed main.

LANDED: both 2.1 cleanups, fully (semantics/oracle first, then backend, each
labelled), with tests + docs + CHANGELOG + examples regen + conformance:
- feef76a interp: drop list-payload positional param binding (proposal 1).
- 83387f6 emit: backend mirror (closure convention bundles as TAG_TUP so a
  lone list no longer spreads).
- d20463c check: reject float literals as Match patterns (proposal 2) + interp.
- b001946 emit: backend mirror (Dec pattern arm rejects).
- 47806f9 test: tests/cleanups_2_1.rs (10 regressions).
- af427f3 docs: CHANGELOG entries.
Downstream: no lexer/grammar change; LSP inherits the float-pattern diagnostic
from the checker; docs prose (evaluation.mdx, pattern-matching.mdx) + wasm
playground artifact regenerated.

ATTEMPTED then REVERTED (nothing committed): 5.4 canonical flags fields in
records. Findings for the next session (this is a bigger slice than the
store/load seams suggest):
- store_to_mem / load_from_mem / lift_flags ALREADY handle flags at the
  boundary (bitset <-> declaration-ordered Value::Flg).
- Blocker A: emit.rs wit_of_check_type has NO T::Flags arm, so ANY record with
  a flags field -> None -> the whole record boxes. (This is why flags fields
  block canonical records today.)
- Blocker B (the deep one): the checker's infer Node::Flg arm types a flags
  literal as its OWN narrow set (Type::Flags(literal_members)), not the
  declared flags type. I tried widening Node::Flg to consult `expected` +
  adding the T::Flags arm, but instrumentation proved the construction gate
  still received decl = the LITERAL's members, not the declared perms set —
  i.e. the field WitTy had the literal's bit positions, not the declaration's.
  That yields an internally-inconsistent canonical layout (wrong flags bit
  positions); the round-trip test passed only by luck (the boundary re-lowers
  / normalises order), masking corruption that would bite if the canonical
  flags value were consumed INTERNALLY (pattern match / field read).
- Correct approach next time: make the flags field's construction site see the
  DECLARED member set (full bit positions). Verify the def BODY record is
  checked against its declared result type so exp_fields threads the declared
  flags type down to the literal; confirm node_types[field] ends up as the
  declared flags type, not the literal's. THEN gate + construct. Pin it with a
  DISCRIMINATING test, not a round-trip: e.g. perms {a b c d}, a literal {a c}
  (non-adjacent members), assert the boundary lifts [a c] not [a b] — round
  trip alone is insufficient because the ABI bitset normalises order. The
  faithfulness gate logic itself was right: admit a flags literal only when its
  names are a duplicate-free subsequence of the declared names in declaration
  order (reordered/duplicated -> stays boxed).

Other 5.4 residue re-verified against emit.rs (line refs approx, base b2639ea):
1. Dep-case ctor calls via Qsym (t/east(5)) stay boxed - ctor_parts (~4715)
   matches only Node::Sym heads at items[0], not Node::Qsym; needs alias->case
   resolution against the target variant's cases.
2. Bare-Sym case patterns (none / nullary case, unwrapped) rebox -
   pattern_top_mem (~2636 bind arm / ~2661 rebox arm) sends them to the boxed
   `_` arm; pattern_mem_var (~2774) already handles the (0,None) disc case, so
   routing a bare nullary-case Sym there (as a 1-element slice) is a small
   optimization. BEHAVIOR-PRESERVING (rebox already gives correct results), so
   it needs instrumentation to confirm the disc path fires, not a diff test.
   Verify none/option/enum case indices resolve in ty.variant_cases().
3. TAG_VAR retirement + boxed matcher string-compare removal - rides 5.6.
4. Large types (>256 cases / >32 flags): sizing NOT implemented; canonical
   gates hard-refuse (>256), store/load/pattern assume 1-byte disc.

NEXT SESSION (docket order, 2.1 now done): (1) 5.4 remainder [flags fields per
the findings above; dep Qsym ctors; bare-Sym case pattern opt; large-types
sizing]; (2) 5.6 compound builtins (+TAG retirement +5.1 evacuation glue
+functor opt-out removal); (3) 5.7 stdlib split (Do + If per 2.8 option 1);
(4) 5.8 defunctionalization; (5) strict-flip check; (6) 5.11. EXCLUDES 5.12
and DefResource.

PROCESS HAZARD (bit me this session, recovered): a fresh Bash call defaults to
the worktree cwd, but `cd /Users/logaan/code/personal/rust/wavelet` lands in
the MAIN checkout (branch main), NOT the worktree. I accidentally committed the
2.1 work to main; recovered by cherry-picking onto goal-5-abi-backend in its
worktree and resetting main. ALWAYS operate in
/Users/logaan/code/personal/rust/wavelet/.claude/worktrees/goal-5-abi-backend.
