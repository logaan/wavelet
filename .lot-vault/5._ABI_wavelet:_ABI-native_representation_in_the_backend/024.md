---
status: work
update-id: lot:033kr9GvkEo8bqS7uRb7L1
work-at: 2026-07-06T15:57:03.729071+00:00
---
Session 12 CHECKPOINT (branch goal-5-abi-backend; pushed, origin tip 9e08caf, 2
commits over base 4359a45; full cargo test green [35 suites, 0 failures] incl
differential + full conformance matrix green with the RELEASE compiler: wavelet
caller -> run()/run-values()/run-resources() = ok vs rust-a AND rust-b, resources
callee PASS both. Never pushed main; worked only in the worktree; all src edits via
python patches since Edit/Write are blocked "parent not isolated"; tree clean,
build-touched conformance/wavelet/wit/wavelet.wit restored, out/out-res removed).

=== DELIVERABLE 1 (mandatory, done FIRST): the retirement slice-plan ===

Posted as this Thing's update lot:033kqn1tqSUxvlzHAste89 BEFORE any code. It
decomposes the "TAG_* consumer retirement + 5.1 evacuation glue + functor/resource
opt-out" monolith into an ordered, individually-green slice queue, and records the
HONEST ORDERING CONSTRAINT the study surfaced (this is the finding the brief asked
for, not a deferral):

  WHOLESALE TAG_* deletion is gated on TWO not-yet-built pieces and therefore is
  NOT a goal-5 slice:
  (1) TOTAL TYPING (goal 3). While the checker is gradual, expr() (emit.rs:1544)
      MUST keep producing boxes: it is the fallback for every Unknown-typed value
      and for node kinds with only a boxed producer (literals in untyped context,
      list_box/rec_box/flg_box, closures). A box CONSUMER (the boxed `pattern`
      string-compare matcher, eq_raw/len_raw/head_h/tail_h/strcat2/to_str, the
      boundary lower) cannot be DELETED while any producer feeding it can still be
      boxed. You can only SHORTCUT specific (producer-canonical -> consumer-
      canonical) paths PAST the box; the consumers finally retire when total typing
      removes the last boxed producer — a goal-3 exit, not a goal-5 slice.
  (2) 5.8 DEFUNCTIONALIZATION. TAG_FN + the TAG_TUP arg-bundle convention riding on
      it (payload_box/def_wrapper_slot/fn_form, session 6) can't retire until
      closures become WIT variants.

  So the queue is a stream of canonical-path shortcuts (each shrinks reboxing along
  one proven path, box path stays as the gradual fallback), plus the 5.1 evacuation
  glue (a resource-STATE-persistence concern, largely orthogonal to TAG retirement,
  with its own dependency). The plan update has the full per-slice detail
  (verification, blast radius, exact code sites, dependents) for slices 1-5.

=== DELIVERABLE 2: first slices of the plan LANDED (2 commits) ===

COMMIT b21a3bf (SLICE 1 — pattern_mem_field nested nullary-case arm):
  A bare `none` / payload-less DefType case as a NESTED field pattern over a
  canonical record scrutinee previously fell through pattern_mem_field's `_` arm
  (emit.rs:2898), reboxing the field via load_from_mem and running the boxed
  string-compare matcher. Added an arm mirroring pattern_top_mem (baf59e3): route
  it to pattern_mem_var(&[pat]) — the disc-only (0,None) match — gated on
  tf.variant_cases().is_some() && nullary case. Behavior-preserving TAG_VAR
  consumer shrink.
  EVIDENCE: WLT_NESTNULL_INSTR instrumentation confirmed the new arm fires
  (name=none, val field at off=4) compiling the new `unbox` export; results
  unchanged (unbox(true)=5, unbox(false)=-1). New discriminating test
  backend_variants::nested_nullary_field_pattern_matches_like_the_oracle (dep
  returns a record {tag: s32 val: option(s32)}, Match on {val: none}/{val: (some x)}).

COMMIT 9e08caf (SLICE 2 — canonical zero-copy `tail` over canonical lists):
  Route (tail x) over a statically-canonical list through expr_mem/Repr::Mem
  instead of reboxing + tail_h. Result = a fresh (ptr,len) pair whose data pointer
  is the operand's advanced one element (elem_size) and whose length is one less,
  SHARING the operand's packed element buffer (values immutable + arena not reset
  mid-call -> share unobservable). No element copy, no TAG_LIST result box. Empty
  operand traps, like tail_h / the oracle's tail of empty.
    - can_mem_as (emit.rs Tup arm): a `tail` head over a canonical-list operand
      whose Mem layout equals the result list type is Mem-eligible (tail preserves
      the list type). predict_body_mem reaches it via its existing `_ => node_mem_ty`
      arm -> an exported def body `tail(lst)` returns canonically via the fast path.
    - expr_mem: new `tail` arm builds the shared (ptr,len).
  EVIDENCE: WLT_TAIL_INSTR confirmed the canonical arm fires 4x (count2/firsttail/
  rest/mtail) with outputs unchanged. New test backend_lists::
  tail_over_canonical_list_is_zero_copy: len(tail)=2, head(tail)=2, Match on tail
  =5, and an EXPORTED list(s32) tail result = [2,3] (retptr fast path). This is the
  first item-2 result-side canonical path — a box result is genuinely avoided when
  the tail is consumed canonically (feeds the len(ad21c2e)/head(d9c2bf5) slices,
  Match, or an export).

Both commits: pure behavior-preserving codegen -> NO CHANGELOG, NO regen-examples,
NO grammar/docs/LSP impact. Instrument -> prove path fires + output unchanged ->
remove instrumentation -> commit pre-instrumentation state -> cargo test +
differential green -> full release conformance matrix (after the ABI-touching tail
slice AND at session end).

=== PLAN SLICES REMAINING (the queue is plan update lot:033kqn1tqSUxvlzHAste89) ===

SLICE 3 (canonical str-cat result) — ANALYZED, DELIBERATELY DEFERRED as LOW-VALUE.
  str-cat operands ALREADY rebox zero-copy (a canonical string reboxes to a TAG_STR
  that wraps the same (ptr,len) without copying bytes — session 11's finding), so
  the only thing a canonical str-cat result buys is avoiding the TAG_STR RESULT box
  when the concat is consumed canonically / exported. That is a real but marginal
  win, and it costs a variadic buffer-alloc + memcpy fold plus an ABI-touching
  export path (conformance re-run). My own plan said "land only if slice 2 proves
  the wiring cheap; otherwise defer as low-value." The wiring pattern IS cheap, but
  the memcpy body + variadic handling + marginal payoff make it a poor
  budget-tail slice; session 11 independently concluded str-cat "buys ~nothing."
  Recommend folding it into the slice-5 canonical-result/evacuation session, or
  skipping until the box representation is actually being retired.

SLICE 4 (further canonical compound builtins) — FINDING: NOT APPLICABLE as a
  box-consumer shrink. The backend's builtin set (BUILTINS, emit.rs:6508) is a
  FIXED list: eq not lt le gt ge add sub mul div rem neg len head tail str-cat
  upper lower to-string to-char some ok err cell-* form-kind rec-key rec-val gensym
  expand. get / empty / contains / reverse / push / concat / map / filter / fold /
  range / zip / split / join are NOT backend builtins — they compile as stdlib
  wavelet functions via internal_call, so there is no boxed builtin arm to shortcut
  (adding one would be a NEW backend feature, not a retirement slice). Drop slice 4
  from the retirement queue. (len/head/eq/tail are the only compound builtins with
  a canonical path, and all four now have one.)

SLICE 5 (5.1 evacuation glue + functor/resource opt-out removal) — the DEDICATED-
  SESSION block, NOT opened (correctly) mid-budget. It has a REAL DEPENDENCY spelled
  out in the plan: the 5.1 design (5.1 Thing update lot:033j0CfiXzfgP5nzT8dmjW,
  evacuate-at-post-return) assumes CANONICAL resource state, but resource state is
  STILL BOXED today (emit_set_resource keeps a TAG_CELL/TAG_LIST graph; user New
  cells are boxes). So the evacuation copy is either (5a) a bespoke box-graph
  relocator (works today, wide blast radius, pointer-rewriting) OR (5b) first
  canonicalize resource state then flat static-size memcpy (cleaner, aligns with
  goal 5, but "canonicalize resource state" is its own sub-project). RECOMMEND 5b as
  a dedicated session. Site: emit.rs:7285-7325 (post-return + the opt-out guard
  `if info.functors.is_empty() && info.resources.is_empty()`), emit_set_resource
  (~6957), user New cell emission, arena-floor globals (7311/7558). Gate: multi-call
  resource test (mutate then read across >2 export calls forcing a reset between) +
  resources callee both directions with the reset ENABLED for resource components.

GATED / OUT-OF-SCOPE (recorded so nobody re-derives): wholesale TAG_* deletion + the
  boxed `pattern` TAG_VAR string-compare deletion (gated on TOTAL TYPING / goal 3);
  TAG_FN + TAG_TUP arg-bundle (gated on 5.8); float/char to-string (not cheaper,
  untestable via differential); dep-Qsym ctor residue (escalated to Logan, session
  10).

=== ORDERED DOCKET for session 13 ===
  (1) DECISION POINT for the orchestrator/Logan: the retirement queue has now been
      decomposed and its first two slices consumed. The remaining retirement work is
      either marginal (slice 3 str-cat) or genuinely dependency-gated (slice 5
      evacuation needs canonical resource state; wholesale deletion needs total
      typing/goal-3 + 5.8). So the highest-leverage next keystone is very likely NOT
      more 5.6 retirement but one of its blockers: 5.7 (Do+If stdlib split, the next
      dedicated keystone per the standing five-stage finding) or 5.8
      (defunctionalization, which unblocks TAG_FN/TAG_TUP), or the total-typing push
      in goal 3 (which unblocks the wholesale consumer deletion).
  (2) If continuing 5.6: slice 5 (evacuation) as a DEDICATED session via path 5b
      (canonicalize resource state -> flat memcpy evacuation -> remove the opt-out).
      Optionally slice 3 (str-cat) folded in, given its result-side win only
      materializes alongside the canonical-result plumbing this session touches.
  (3) 5.7 Do+If stdlib split (session 9 five-stage finding: If/Do wired across
      reader/interp/check/wit/emit; needs regen-examples + three grammars + docs).
  (4) 5.8 defunctionalization (design on lot:033iYvSRtJneYx1zERPfFM update 003).
  (5) strict-flip check. (6) 5.11 boundary no-ops. (7) 5.12 values callee.

PROCESS: worktree
/Users/logaan/code/personal/rust/wavelet/.claude/worktrees/goal-5-abi-backend
throughout; Edit/Write blocked so all src edits via python patches; no PR opened
(orchestrator integrates the branch and runs the full suite). No subagents spawned.
