---
status: work
update-id: lot:033izad6Xk2QoI0HW8a0Dd
work-at: 2026-07-03T09:28:15.469916+00:00
---
Session 1 checkpoint (branch goal-5-abi-backend, 9 commits over main 1beb034,
pushed; every commit green under the full cargo test suite AND the live
conformance matrix). State per child:

**Landed:**
- Foundation: check.rs exports a per-node type table; the emitter consumes it
  (bec1f74). This is the spine of everything type-directed.
- 5.1: DECIDED (option 1, arena-per-call, headerless — Logan delegated) and
  implemented via canonical post-return per export (3ba7b51). One rider open:
  functor components opt out until resource state gets its persistent region
  (comes with 5.3/5.5 value-construction rework); then 5.1 closes.
- 5.2 (partial): unboxed scalar expressions + typed If conditions (59523e1),
  typed wasm locals for scalar Let bindings (9dfbb0b), f32 at the boundary
  (5b7cc86). Remaining: typed INTERNAL FUNCTION SIGNATURES (flat scalar
  params/results instead of box pointers — the single biggest lever left in
  5.2, next session's first target), Match-binding typed locals, u64 range.
- 5.4 (partial): flags values are the interpreter's Value::Flg end-to-end
  (96c372e); fixed a latent lift parity bug (record-of-bools). Remaining: the
  headline variant work (numeric discriminants; Match on ints not strings)
  and per-type sizing for >256 cases / >32 members.
- 5.6/5.6.1 (partial): typed builtins share drift-proof semantic cores with
  the boxed path; to-* conversions compile (849b81e). 5.6.1's per-width-ops
  framing deliberately narrowed to one checked-i64 core — recorded there why
  (per-width wrapping would diverge from the oracle).
- 5.12: the caller-role matrix is COMPLETE — runner.wlt now carries every
  check the suite defines (f32-rt, every-primitive-rt, permissions-rt,
  option-none) and passes against both rust callees.

**Not started:** 5.3 (canonical records/tuples), 5.5 (packed lists / raw
strings), 5.7/5.7.1 (stdlib split + monomorphization; Do move rides here),
5.8 (design POSTED for review on the Thing — implementation follows the
recorded shape after 5.2-5.6), 5.9, 5.10, 5.11 (the payoff step, last).

**Strict flip (3.9/3.10):** NOT executed — the 17-example backlog needs
function types, which arrive with 5.8.

**Awaiting Logan (untouched, per instructions):** 2.1 proposals, 2.7, 2.8
(If-to-stdlib), 4.5 resource spelling (gates 5.8.1 and the conformance
callee role's counter resource).

Recommended resume order: typed def signatures (5.2) -> 5.3 records/tuples ->
5.5 lists/strings -> 5.4 variants -> 5.6 compound builtins -> 5.7 -> 5.8 ->
5.9/5.10 -> 5.11 -> 5.12 callee role + strict flip.
