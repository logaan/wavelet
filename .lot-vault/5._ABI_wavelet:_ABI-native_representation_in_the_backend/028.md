---
status: work
update-id: lot:033kvZMvv5HhRdg0nah7gu
work-at: 2026-07-06T18:57:12.377387+00:00
---
=== SESSION 14 CHECKPOINT (goal-5-abi-backend, pushed, tip f5ea328; 4 commits
over base 132efae; never touched origin/main; no PR; no subagents) ===

KEYSTONE: 5.8 defunctionalization — the checker foundation, the boundary rule
(oracle parity), and the first backend devirtualization slice are LANDED green.
The Fn-literal lifting and multi-case variant+dispatch remain, with the exact
execution plan recorded below.

=== COMMITS (each committed green: cargo test 36 suites 0 failures incl the
differential harness) ===
- 8c492ad  5.8: Phase D arrow types in the checker (Type::Fn). Concrete-arrow
  Fn literals / bare single-sig def refs / indirect applications statically
  typed (check_indirect_apply: arity + arg types + result). fully_concrete
  gate; gradual stays Unknown. type_to_wit(Fn)=None; exhaustiveness treats
  arrows as non-enumerable. +6 type_system tests, +1 strict_mode positive.
- 80b18b2  5.8: boundary rule. Exported def whose inferred result contains a
  function type = deliberate compile error ("function types cannot cross
  component boundaries yet; import the combinator as a macro so it expands
  into this component instead"), identical from check_roots (runner/interp —
  oracle parity) and WIT synthesis (InferredWit::Func bridge). CHANGELOG
  [Unreleased] Added covers A+B. +3 tests.
- 5c635b1  5.8: backend devirtualization, zero-capture case. Binding.known_fn
  (interned via Emitter::known_fn_names): Let {f: inc} f(41) compiles to
  direct internal_call, not TAG_TUP payload + TAG_FN call_indirect; boxed
  closure still built for value uses; shadowing/local-rebinds stay indirect.
  WLT_DEVIRT_INSTR (removed pre-commit) proved fires/non-fires exactly per
  design; new tests/backend_functions.rs (component + oracle cross-check).
- f5ea328  docs: "Function values" section in type-system.mdx (arrow types,
  gradual fallback, boundary rule, macro imports as the cross-component path).

=== VERIFICATION ===
- cargo test: 36 suites, 0 failures, after every slice (differential incl).
- Release conformance matrix after the codegen slice (release binary, this
  tree): wavelet caller -> rust-a AND rust-b: run()/run-values()/
  run-resources() all ok; resources callee PASS rust-a AND rust-b.
  conformance/wavelet/wit/wavelet.wit restored; out/out-res removed; tree clean.
- regen-examples NOT needed: no documented example changes behaviour (locked
  suite green). Grammars: no lexer/token changes -> untouched (incl the
  wavelet.nvim submodule, unmoved). LSP: no API breakage, 4/4 tests green,
  diagnostics flow through the shared checker automatically.

=== STRICT-FLIP CHECK: NOT FLIPPED — backlog 17 of 86, unchanged, with WHY ===
Arrow types are necessary but NOT sufficient. Per-example:
- map-square, std-map, std-filter, std-fold, std-apply, eval-apply-list,
  eval-twice, sf-fn-adder, eval-fn-scalar, eval-fn-by-name, eval-fn-by-order,
  tail-count-down, tail-sum-to (13): authored with UNTYPED Fn/def params, so
  no concrete arrow ever forms. Clearing them needs EITHER (i) checker work:
  typed higher-order builtin signatures (map/filter/fold/apply as parametric
  arrows) PLUS expected-type-directed param inference (push the expected arrow
  onto an untyped Fn literal's params — bidirectional into fn-MACRO), OR (ii)
  rewriting the documented examples with typed params — a pedagogy decision
  for Logan (the examples currently TEACH the untyped style).
- pm-describe, pm-none (2): `none`/untyped Match binders yield Unknown-typed
  clause bodies (Option(Unknown) payload binds v: Unknown).
- std-cells (1): cell-new/get/set need a static cell type (recorded in 3.9's
  own status as category b).
- macro-trylet (1): expansion contains an option-typed hole pattern (3.9
  category c; re-derive at flip time).
The 3.9 option-1 gate (examples + conformance green under strict) therefore
still fails structurally; per the brief this is recorded rather than forced.
DECISION NEEDED (Logan/orchestrator): path (i) checker inference vs path (ii)
example rewrites — (i) keeps the docs' untyped pedagogy working under strict
and is the only path that doesn't change what the docs teach; (ii) is smaller
but makes every doc example spell types.

=== REMAINING 5.8 SURFACE — execution plan for the next slice ===
1. Fn-literal (capture) devirtualization — the positional-reservation design,
   derived from emit.rs's index discipline this session (function indices are
   STRICTLY pre-reserved via take() in emission order; bodies pushed
   positionally; imports finalized before bodies; closure_bodies live at
   closure_base = n_imports + bodies.len(), unknowable mid-body — so direct
   calls to lifted lambdas need reserved indices):
   (a) PRE-SCAN before index assignment: walk def bodies for fn-MACRO nodes
       whose node_types entry is a concrete Type::Fn and that appear as Let
       binding inits; reserve one take() index per node AFTER all other
       reservations (internal/export/overload/resource blocks), keyed by
       NodeId in a HashMap<NodeId, u32>.
   (b) let_form: on such an init, record KnownLambda { reserved_idx,
       capture_bindings } beside known_fn; compile the lifted body AT THIS
       POINT (captures bound at their CURRENT Binding reprs as leading typed
       params — sound because locals are single-assignment and the fact is
       scope-carried; fn params boxed, result boxed first cut); stash the
       finished Function in HashMap<NodeId, Function>; still emit fn_form's
       boxed closure for value uses.
   (c) assembly: push stashed bodies in reservation order at the reserved
       positions (both Emitter assembly paths — gate path 2 (macro
       components) OFF via an Emitter flag to avoid dangling reservations).
   (d) apply site: push capture locals at their reprs, evaluate args boxed,
       Call(reserved_idx) / ReturnCall for tail.
   Verification: WLT instrumentation + a backend_functions case with captures
   (Let {by: 5} Let {g: Fn {n: s32} add(n by)} g(1)) vs oracle; conformance.
2. Multi-case variant+dispatch (distinct function values into one binding /
   lists of functions) + the deterministic naming scheme from the design
   (fn-<wit-spelling>; case = def name / <owner>-<k>): only AFTER (1), since
   its cases reuse the lifted functions. WIT-internal only (boundary rule
   keeps variants out of synthesized WIT).
3. TAG_FN/TAG_TUP arg-bundle retirement stays gated on (1)+(2) AND total
   typing (session-12 plan) — unchanged.

=== NOT STARTED / OUT OF SCOPE (per brief) ===
5.11 boundary no-ops (budget went to 5.8 + verification); 5b evacuation
session (canonical resource state); 5.12 values callee; goals 6/7.

=== ORDERED DOCKET for session 15 ===
(1) 5.8 remainder: Fn-literal lifting per the recorded plan above, then
    multi-case variant+dispatch.
(2) Strict-flip path decision from Logan (checker inference vs example
    rewrites), then execute it; flip = 3.9 option-1 gate + delete gradual.
(3) 5b evacuation session via path 5b (canonicalize resource state, str-cat
    folded in) — per the session-12 slice-plan.
(4) 5.11 boundary no-ops. (5) 5.12 values conformance callee. Then goals 6/7.

PROCESS: worktree /Users/logaan/code/personal/rust/wavelet/.claude/worktrees/
goal-5-abi-backend throughout; base was fast-forwarded to 132efae first;
Edit/Write blocked (parent not isolated) so all src edits via python patches;
two environment watchdog crashes mid-session — work was committed promptly
after each and nothing was lost.
