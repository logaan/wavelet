---
status: work
update-id: lot:033kwS0FOmNrGmM5sCSAiA
work-at: 2026-07-06T19:33:06.801553+00:00
---
Orchestrator integration — goal-5 session 15 merged to main.

Merged `goal-5-abi-backend` (2 commits, tip 679f56a) into main as merge commit
**2b28577**; full `cargo test` green on merged main; pushed origin main. Branch
fast-forwarded and pushed.

Landed:
- **5.8 Fn-literal capture devirtualization** (83c11d4): inline Fn literals bound
  via Let with concrete arrow types are lambda-lifted to top-level core functions;
  applies compile to direct calls with captures as leading typed params. Executed
  exactly on session 14's positional-reservation plan; instrumentation-proven for
  zero/one/two captures; new capture/capture2 tests.
- **5.12 values callee groundwork** (679f56a): roundtrip-values.wlt is authored,
  fully typed, and WIT-checks; every scalar/char/string/record/tuple/variant/
  option/result transform compiles (char now does real next-scalar arithmetic).
  Precise blocker documented: the backend implements none of map/filter/fold, no
  list-building primitives (push/concat), no flg/contains — and interface export
  is all-or-nothing.

Deliberate calls accepted: 5.8 multi-case dispatch deferred into the evacuation
session (adds a parallel representation with zero observable change until TAG_FN
retirement); 5.11 confirmed blocked behind evacuation + total typing.

Next: session 16 = the **5b evacuation keystone** (canonicalize resource state →
flat memcpy evacuation at post-return → remove the functor/resource opt-out; TAG
retirement pieces + 5.8 multi-case dispatch land here; str-cat folded in), then
the 5.12 backend list/flags builtins so the typed values callee compiles, then
test-values-callee.sh. Verification per the release conformance matrix +
multi-call resource tests the session-12 plan prescribed.
