---
status: work
update-id: lot:033iyCcAY8FvMuuXwXWutK
work-at: 2026-07-03T08:31:43.577739+00:00
---
Starting goal-5 execution on branch goal-5-abi-backend (worktree off main
1beb034, the goal-4 merge). Recon done: emit.rs (9.1k lines) is fully boxed
(TAG_* uniform boxes, one bump allocator, lazily-cached value-def globals,
set-functor resource state as heap pointers); emit.rs consults check.rs not at
all, so no per-node static types reach codegen today — exporting a NodeId->Type
side table from the checker is the foundation everything else builds on.

Order of attack (per the recorded updates): 5.1 decision recorded (option 1,
arena-per-call — see that Thing; Logan delegated); then the type-table
foundation; then arena-per-call + persistent region; then layouts/unboxing
(5.2/5.3/5.4/5.5) with box<->typed coercion seams so every commit stays green
against the differential harness; then 5.6/5.7/5.8 per dependency order, with
the 5.8 design posted for review before implementation. 5.12 gates the end.
This will span multiple sessions; everything commits incrementally.
