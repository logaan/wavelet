---
status: work
update-id: lot:033j3ze6bWN2V04h8AtZER
work-at: 2026-07-03T12:27:41.502997+00:00
---
Session 3 checkpoint (branch goal-5-abi-backend, 5 new commits over the
merged session-2 tip 82f939c: 14fa3d5, f207367, 260709e, bad296a, 196fe7a;
pushed; 308 tests green + live conformance matrix green at tip). Vault
re-read first: no new answers from Logan (4.5 still open -> 5.12 callee
role still gated; 2.8 unanswered -> If stays core; 5.8 design still
unobjected -> proceed-by-default stands; 2.1 untouched).

This session was all 5.3 (records canonical layout), landed as an overlay
in the 5.2 style — Repr::Mem alongside Boxed/Scalar, boxed fallback always
sound — rather than a big-bang TAG_REC replacement:

- Repr enum refactor (the recorded extension point; Binding stays Copy via
  an interned-WitTy MemTy index — the Copy-vs-Clone question is answered).
- Canonical record BINDINGS (Let), in-place nested construction, unboxed
  scalar field stores at WIT width.
- KEY constraint honoured throughout: record field order is OBSERVABLE
  (Value::Rec eq/to-string are positional; backend eq_raw matches), so
  only provably-faithful constructions go canonical: literals whose order
  matches the layout, boundary lifts (declared order both worlds), and
  compositions thereof. box->mem coercion is deliberately NOT emitted
  anywhere it could normalize order.
- Match DESTRUCTURES canonical scrutinees at despec offsets (typed scalar
  binders, interior-pointer nested-record binders, static absent-field
  clause failure; everything else reboxes only what it inspects).
- Defs carry Mem result signatures via a pre-emission prediction walk;
  the export retptr fast path returns the def's canonical pointer
  directly when layouts agree (exported record results: built once, in
  place, no boxes).
- Dep-call record results are BORN canonical (the retptr area IS the
  value; declared order = oracle's lift order).

tests/backend_records.rs (11 tests) pins faithfulness; oracle answers
verified in the interpreter repl; instrumentation confirmed each new path
actually fires. No CHANGELOG entry (internal representation only, no
user-visible change); no regen-examples needed (differential corpus over
examples.json green, unchanged); no downstream surfaces touched (no
lexer/semantics change).

5.3 residue is recorded on the 5.3 Thing (tuples; Mem def params need an
all-call-sites analysis; records-in-variants/lists ride 5.4/5.5; Match
excluded from body prediction; TAG boxes retire with 5.5/5.4 + the 5.1
evacuation glue).

NEXT SESSION: (1) finish 5.3's tuple half (pattern_mem_tup + dep-born
tuples; test source = conformance rust callees or Quote-typed... see 5.3
Thing), then (2) 5.5 lists ptr+len / strings raw UTF-8 — note 5.5's
list/string canonical forms slot straight into the existing mem_field_ok
gate and mem_field_into/mem_field_binding seams, so 5.3's residue items 1
and 3 partially collapse into it; then 5.4 variants (+ 5.9 Sym-headed
tuple-pattern residue + flags order handling), 5.6 compound builtins, 5.7,
5.8 (design unobjected), strict-flip check, 5.11, 5.12 callee role (needs
4.5). Strict flip NOT executed this session (still blocked on 5.8's
function types clearing the 17-example backlog).
