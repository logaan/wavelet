---
status: work
update-id: lot:033jD0pxv1MSRD2OrepYSV
work-at: 2026-07-03T18:35:12.476182+00:00
---
Session 6 progress (part 1) — the two 2.1 cleanups are DONE, on branch
goal-5-abi-backend, pushed (origin now at af427f3, 6 commits over the base
b2639ea). 352 tests green + full conformance matrix green (wavelet caller ->
both rust callees, run()/run-values()/run-resources() all ok).

Commits (semantics/oracle first, then backend, each labelled 2.1):
- feef76a interp: drop list-payload positional param binding (proposal 1).
  bind_params: a single list arg no longer spreads across n params (a list is
  one value); positional multi-binding is the tuple's job, by-name the
  record's. Dropped the empty-list/flags/record "no arguments" acceptances —
  only the empty tuple () means no args. Threaded the consequences: builtins
  fold bundles (acc,elem) as a tuple; runner invokes the run entry with ();
  docs std-apply now feeds a record payload; examples.json + playground wasm
  regenerated; evaluation.mdx prose updated.
- 83387f6 emit: backend mirror. The indirect closure convention bundled >=2
  args as a TAG_LIST payload, indistinguishable from a lone list value, so it
  spread. payload_box now bundles as TAG_TUP (matching the interpreter's
  Value::Tup bundle) and both wrappers (def_wrapper_slot, fn_form) unpack a
  TAG_TUP; a lone list traps instead of spreading. Same layout, tag only.
- d20463c check: reject float literals as Match patterns (proposal 2).
  check.rs reject_float_patterns walks each clause pattern (recursing into
  tuple/list/record) and errors on any Dec. interp.rs match_pattern refuses
  Dec too (faithful standalone oracle). pattern-matching.mdx prose updated.
- b001946 emit: backend mirror — Dec split into its own erroring pattern arm.
- 47806f9 test: tests/cleanups_2_1.rs (10 regressions over eval_snippet).
- af427f3 docs: CHANGELOG Unreleased entries for both changes.

Downstream surfaces checked: no lexer token-class change (float/list/tuple
syntax unchanged), so the three grammars need no edit; LSP inherits the new
float-pattern diagnostic from the checker automatically. Docs prose +
CHANGELOG done. examples.json + docs/src/wasm regenerated and committed.

PROCESS NOTE (hazard for next session): a fresh Bash call's cwd defaults to
the worktree, but `cd /Users/logaan/code/personal/rust/wavelet` lands in the
MAIN checkout (branch main), not the worktree. I initially cd'd to main and
committed there by mistake; recovered by cherry-picking the 6 commits onto
goal-5-abi-backend in its worktree and hard-resetting main back to b1bad94
(the orchestrator's vault note, left intact). Always cd to
/Users/logaan/code/personal/rust/wavelet/.claude/worktrees/goal-5-abi-backend
(or run without cd) for this branch.

STILL TO DO this session (docket order): 5.4 remainder (bare-Sym case
patterns, flags Mem fields, dep Qsym ctor calls, then large-types sizing) ->
5.6 compound builtins -> 5.7 stdlib split -> 5.8 -> strict-flip check ->
5.11. Continuing now with the 5.4 remainder.
