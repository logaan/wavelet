---
status: work
update-id: lot:033koVShvXyf3rRwhZ8xUd
work-at: 2026-07-06T14:09:24.095876+00:00
---
Session 8 progress (docket item 1 DONE) — result-type threading + Option-A int
widening landed on goal-5-abi-backend (commit 7040ad2 over base 438afd7,
pushed). 378 tests green + full conformance matrix green (wavelet caller ->
rust-a AND rust-b: run()/run-values()/run-resources() all ok; resources callee
PASS both).

LANDED (7040ad2 check: thread declared export result type into bodies +
Option-A int widening):
- check_roots now collects each Export's declared result: type (as_export_result
  helper, export_results map) and threads it into the matching def's body as the
  expected type — previously bodies were checked with expected=None. Bare and
  Fn-with-params exported defs both thread. Only concrete (non-Unknown) results
  thread.
- compatible_widening (new, used ONLY at the check() boundary + resolve_overload
  step-2 result filter; args_match stays strict compatible to avoid overload
  ambiguity regressions):
  * int_widens/int_class: same-signedness narrower->wider (s32->s64) admitted;
    mixed signedness (s32<->u32) and narrowing (s32->s16) rejected.
  * payload-less result-arm dropping: an ok(0)-style body flows into a declared
    payload-less `result` because the ABI drops the payload (the wasi:cli/run
    `run: func() -> result` shape). A PRESENT declared arm still checks strictly.
- Return-type-directed overload resolution now applies in export bodies too; the
  interpreter agrees because resolve_overloads shares the same check_roots walk.

REGRESSION FIXED en route: the CLI scaffold template (src/scaffold.rs, `Def run
Fn {} Do [... ok(0)]` against `result: result`) newly failed to typecheck under
threading; the payload-less result-arm drop is exactly the legitimate boundary
coercion that makes it (and every wasi:cli/run component) sound. No template
edit needed.

TESTS: tests/result_threading.rs (8 discriminating cases) pins the policy.

DOWNSTREAM: CHANGELOG Unreleased Changed entry added. docs prose added
(type-system.mdx "Declared result types"). No examples.json regen needed
(documented examples run via eval_snippet which rejects file-level Export forms;
tests/examples.rs green). No lexer/token-class change -> grammars untouched;
LSP inherits the checker diagnostic.

NEXT (continuing this session): docket item 2 = 5.6 compound builtins.
