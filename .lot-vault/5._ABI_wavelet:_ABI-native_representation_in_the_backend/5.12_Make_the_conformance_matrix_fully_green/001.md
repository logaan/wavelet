---
status: note
task-id: lot:033iNRmqy5xh0UCmKb6G67
update-id: lot:033iNRmqy5xh0UCn2vPqpz
note-at: 2026-07-02T07:34:01.001983+00:00
---
# 5.12 Make the conformance matrix fully green

Work through the shortcomings list produced by Stage 2 until every WIT feature
round-trips both ways against both Rust seed files.

Process (agreed in the parent Thing's interview):

- One child task per shortcoming, created here once the Stage 2 list exists.
- One PR per shortcoming fix; interpreter first (it is the semantics oracle),
  then the wasm backend, then regen examples/docs/grammars per CLAUDE.md.
- Tractable fixes proceed autonomously; anything needing a language-design
  decision gets an info update asking for direction before work starts on it.

Known-in-advance candidates (from dev-notes/gaps.typ): constructing user
variant/enum cases in compiled code, user-declared resource types,
variant/enum > 256 cases, flags > 32 members.

Done when: the full matrix (wavelet <-> rust-a/rust-b, both directions, both
seeds) passes with zero failures.
