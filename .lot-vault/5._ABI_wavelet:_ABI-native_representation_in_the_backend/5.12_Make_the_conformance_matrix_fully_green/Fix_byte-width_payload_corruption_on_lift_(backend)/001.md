---
status: note
task-id: lot:033iPTotXfIqWXZNViDg8k
update-id: lot:033iPTotXfIqWXZOlYN4ap
note-at: 2026-07-02T08:56:50.944031+00:00
---
# Fix byte-width payload corruption on lift (backend)

The one live CORRECTNESS bug the matrix found, and the top priority: compiled
wavelet corrupts byte-width payloads when lifting values from an imported
call. Evidence (wavelet caller -> rust callee, both seeds): head of
list-u8-rt([1 2 3]) response lifts as 65794 (want 2); option-u8-rt(some(7))
payload lifts as 0 (want 8); result-tuple-direction-rt ok((1,2)) mismatches
(u8 pair in a result payload). eq itself is sound (eq(some(8) some(8)) true).
Suspect element-stride/offset handling for sub-4-byte types in the generic
lift. Acceptance: the three failing checks in conformance/wavelet/src/
runner.wvl (PR #28) flip green.
