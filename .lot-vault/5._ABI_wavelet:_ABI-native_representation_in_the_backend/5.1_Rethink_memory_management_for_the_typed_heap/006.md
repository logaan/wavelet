---
status: work
update-id: lot:033iydLIdA8IRDFUo6BcHe
work-at: 2026-07-03T08:49:17.557945+00:00
---
Implementation of the decided option 1 has landed on goal-5-abi-backend
(3ba7b51): every export now emits a canonical `cabi_post_<export>` companion
that wit-component wires as the export's post-return; it resets the heap
pointer to a new immutable arena-floor global (the heap base) and zeroes the
value-def cache globals so value defs recompute per call. wasmtime invokes
post-return automatically, so the reset is live in all hosts. Verified: probe
component shows `(canon lift ... (post-return ...))`; new tests/arena_reset.rs
pins 50-call value-def correctness and compound-arg integrity across resets;
full cargo test green.

Remaining on this Thing: the persistent region. Components with functor
instantiations currently OPT OUT of the reset (their set-resource cell + list
live on the same heap and must survive calls); they move to an explicit
persistent allocator when 5.3/5.5 rebuild value construction, at which point
the opt-out is removed and this Thing can close.
