---
status: work
update-id: lot:033jEVzQBTpdFXU0JX1QuK
work-at: 2026-07-03T19:36:25.823780+00:00
---
Session 7 checkpoint (branch goal-5-abi-backend; pushed, origin tip aa4613c, 2
commits over base c9e6a08; 353 tests green [+1 new discriminating test] + full
conformance matrix green: wavelet caller -> rust-a AND rust-b,
run()/run-values()/run-resources() all ok). Never pushed main; worked only in
the worktree.

COMMITS:
- fe8d633 emit: canonical flags fields in records (5.4).
- aa4613c docs: CHANGELOG entry for the flags-field alignment fix.

LANDED -- 5.4 canonical FLAGS FIELDS in records (the item session 6 attempted +
reverted). Full detail on the 5.4 Thing (lot:033iWTZoQYJK01IjTkjC87 update
033jEVAuFBjVzmqO0tCCpR). Summary:
- Blocker A fixed (wit_of_check_type T::Flags arm); Blocker B addressed by
  check's Node::Flg consulting `expected` (a flags literal takes its declared
  flags type where the record has an expected type -- annotation/typed-arg/
  field) + flags_members helper.
- mem_field_ok Flags gate (duplicate-free subsequence in declaration order;
  helper flags_is_ordered_subseq) + mem_field_into Flags construction (declared
  positions, canonical width).
- LATENT USER-VISIBLE BUG FIXED (CHANGELOG Fixed): flags_align hardcoded 4 ->
  canonical ABI 1/2/4 by member count; store_to_mem flags width matched. These
  corrupted ANY record/tuple with a flags field crossing a boundary
  (standalone flags unaffected).
- Discriminating test in tests/backend_flags.rs (declared positions +
  alignment, via `The entry {...}`; declared-position construction confirmed by
  instrumentation).

KEY DECISION FOR LOGAN (linchpin -- gates the export fast-path payoff for
canonical records/variants/flags built in BARE exported def bodies, and should
be resolved before 5.6): check_roots checks Fn bodies with expected=None, so a
declared result type does not reach a bare body's construction sites. Threading
it (I implemented + reverted) unlocks the fast path BUT is entangled with:
  1. Int-widening -- the checker's unify rejects an s32 body for an s64-declared
     result (broke 8 backend_lists tests). Should the boundary allow safe int
     widening? Programs rely on it being unchecked today.
  2. Return-type-directed overload resolution for export bodies (runtime
     behavior change).
  3. Possible new sub-expression type errors.
The localized flags work is CORRECT for all currently-reachable paths (5.6
cross-origin canonical compound ops not implemented yet, so the literal-vs-
declared layout divergence cannot manifest). Decide + land threading BEFORE 5.6.

5.4 RESIDUE (deferred, behavior-preserving OPTIMIZATIONS -- boxed fallback
correct; not landed in a bg session because the findings prescribe
instrumentation not diff tests, i.e. untestable codegen that could silently
regress): (1) dep Qsym ctor calls (ctor_parts ~4731 Node::Sym only); (2)
bare-Sym case pattern opt (pattern_top_mem ~2641 -> route to pattern_mem_var
~2774); (3) large types >256 cases/>32 flags (per-type sizing unimplemented);
(4) 5.9 Sym-headed-tuple residue on the boxed path.

NOT STARTED this session: 5.6 (compound builtins -- HUGE; still TAG_REC/TAG_TUP/
TAG_VAR/TAG_LIST/TAG_STR consumers; enables 5.1 evacuation glue + functor
opt-out removal + 5.11); 5.7 (stdlib split, Do+If move per 2.8 option 1 --
user-visible); 5.8 (defunctionalization on the posted shape); strict-flip
(gated on 5.8 clearing tests/strict_mode.rs::strict_backlog); 5.11 (boundary
no-ops). EXCLUDED per brief: 5.12, DefResource.

NEXT SESSION (ordered): (1) Get Logan's decision on declared-result-type
threading (int-widening) and land it -- unblocks the export fast-path for
canonical records/variants/flags from bare bodies, and is a precondition for
5.6's cross-origin canonical compound ops being sound. (2) 5.6 compound builtins
(type-indexed eq/compare/to-string/len/head/tail/str-cat over canonical layouts;
retire TAG_* consumers; 5.1 evacuation glue; functor opt-out removal). (3) 5.7
stdlib split. (4) 5.8. (5) strict-flip check. (6) 5.11. 5.4 residues are
optional opts, best done alongside 5.6's TAG retirement.

PROCESS: worktree
/Users/logaan/code/personal/rust/wavelet/.claude/worktrees/goal-5-abi-backend
throughout; Edit/Write blocked (parent not isolated) so all edits via
Bash/python patches; no PR opened (per Logan, orchestrator integrates).
