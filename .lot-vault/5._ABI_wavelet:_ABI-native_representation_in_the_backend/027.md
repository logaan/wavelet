---
status: work
update-id: lot:033kvSnFoH2RTkdbTnNwvN
work-at: 2026-07-06T18:52:53.082936+00:00
---
Session 14 mid-session progress (branch goal-5-abi-backend, pushed, tip 5c635b1,
3 commits over base 132efae). 5.8 defunctionalization underway per the design on
lot:033iYvSRtJneYx1zERPfFM update 003.

LANDED (each green: cargo test all suites incl differential; conformance matrix
after the codegen slice):

- 8c492ad  5.8 SLICE A — Phase D arrow types in the checker (Type::Fn).
  Fn literals with fully-concrete params+result carry (P...) -> R; a bare
  reference to a single-sig concrete-arrow Def types the same way (zero-capture
  case; absorbs "qualified symbols as bare values" for local defs); indirect
  application of a function-typed local checks arity+args and yields R
  (check_indirect_apply). Gradual values stay Unknown exactly as before.
  type_to_wit(Fn)=None (no boundary spelling); Match exhaustiveness treats
  arrows as non-enumerable. 6 new type_system tests + strict_mode positive test.

- 80b18b2  5.8 SLICE B — boundary rule + oracle parity (design items 3+4).
  An exported def whose inferred result contains a function type is a
  DELIBERATE compile error ("function types cannot cross component boundaries
  yet; import the combinator as a macro so it expands into this component
  instead"), raised identically by check_roots (runner/interp path — oracle
  parity) and WIT synthesis (new InferredWit::Func in the checker bridge).
  Previously: build failed with generic "cannot infer result type" while
  `wavelet run` silently allowed the escape. Gradual (Unknown) results keep the
  existing cannot-infer path (recorded gap: an UNTYPED escaping closure still
  runs under the interpreter but fails the build — pre-existing gradual hole,
  closes at the strict flip). CHANGELOG updated for A+B.

- 5c635b1  5.8 SLICE C — backend devirtualization, zero-capture case (design
  item 1's "single-case variant devirtualizes to a direct call", for bare-def
  bindings). Binding grows known_fn (interned def name; Copy preserved via
  index into Emitter::known_fn_names). Let {f: inc} f(41) compiles to the
  direct internal_call path instead of TAG_TUP payload + TAG_FN call_indirect;
  the boxed closure value is still built for non-apply uses; shadowing and
  local-to-local rebinding fall back indirect (scope-carried Binding = free).
  WLT_DEVIRT_INSTR (removed pre-commit) proved: direct+shadowed applies
  devirtualize, rebinding/Fn-literals stay indirect, outputs oracle-equal.
  New tests/backend_functions.rs (component + oracle cross-check).

VERIFICATION: cargo test 36 suites 0 failures after each slice (incl the
differential harness). Release conformance matrix after slice C: wavelet
caller -> rust-a AND rust-b run()/run-values()/run-resources() all ok;
resources callee PASS both. conformance/wavelet/wit/wavelet.wit restored,
out/out-res removed, tree clean. NO regen-examples needed (no documented
example changes behaviour — suite green against locked JSON).

STRICT-FLIP CHECK (early finding, full analysis at checkpoint): backlog still
17 of 86 — arrow types are NECESSARY but not SUFFICIENT. The 17 examples are
authored with UNTYPED Fn params (map-square, std-map/filter/fold/apply,
eval-*, tail-*), so no arrow forms; clearing them needs (i) expected-type-
directed param inference pushing a builtin's arrow expectation onto untyped Fn
literals + typed higher-order builtin sigs (map/filter/fold/apply), or (ii)
rewriting the documented examples typed (a pedagogy call for Logan); plus a
static cell type (std-cells) and the macro-trylet expansion hole. 3.9's own
status update anticipated exactly this ("blocked on function types" was
necessary-condition language). NOT flipping this session.

REMAINING 5.8 SURFACE (recorded): Fn-literal devirtualization with captures
(typed lifted function; attempting next if budget allows) and the multi-case
variant+dispatch generation (distinct function values flowing into one
binding) with the deterministic naming scheme — the latter is a dedicated
follow-up slice; TAG_FN/TAG_TUP retirement stays gated on it plus total typing.
