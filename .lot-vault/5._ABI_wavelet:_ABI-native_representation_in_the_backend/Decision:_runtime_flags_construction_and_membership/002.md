---
status: info
update-id: lot:033kyaEFtAlZHYtD0Fv5cK
info-at: 2026-07-06T21:00:00.957070+00:00
---
Orchestrator follow-up — option (b) has been VERIFIED, zero new surface needed.

A scoped session rewrote permissions-rt as a Match over the 16 declaration-order
flags literals (each arm's result ascribed `The permissions`, required because the
imported flags type's members aren't resolvable at the literal site) and verified:
the interpreter computes the complement correctly on both seeds, and the backend
type-checks + lowers the function. `flg`/`contains` are NOT needed for 5.12.

So this decision is effectively answered by verification: no new language surface
is required for the conformance goal. The options in this write-up remain live
only if you independently want ergonomic runtime flags ops (the Match encoding is
verbose). If you're happy with the zero-surface outcome, no action is needed here.

The values callee remains blocked by two OTHER findings (verified during the same
session): tuple VALUE construction has no language surface at all (`tup(...)` is
unbound in the oracle; bare parens are call forms — a new decision Thing is being
written), and backend `drop` is a plain oracle-following backend gap (being fixed
in the backend-coverage session). Details: goal-5 Thing update lot:033kyXijRu9Vq9mLw2j4SV.
