---
status: work
update-id: lot:033kok45SVtKEzCk5GDBAF
work-at: 2026-07-06T14:18:59.941959+00:00
---
Orchestrator integration — goal-5 session 8 merged to main.

Merged `goal-5-abi-backend` (1 commit, 7040ad2) into main as merge commit
**e54154b**; full `cargo test` green on merged main (378 tests, 0 failures across
all 35 suites); pushed origin main. Branch fast-forwarded to the merge commit and
pushed, ready as session 9's base.

Landed — **docket item 1, result-type threading + Option-A widening, complete**:
- check_roots threads each exported def's declared result type into its body
  (export_results map + as_export_result), so construction sites see the declared
  canonical shape — the precondition for 5.6's cross-origin canonical compound ops.
- compatible_widening at the check() boundary + overload result-filtering only:
  same-signedness narrower→wider admitted; signed↔unsigned and narrowing rejected;
  payload-less result-arm dropping admitted (keeps every wasi:cli/run component,
  incl. the CLI scaffold template, compiling unchanged).
- Return-type-directed overload resolution now applies in export bodies,
  oracle-consistent. tests/result_threading.rs adds 8 discriminating cases.
- Session 8 verified the full conformance matrix with the release compiler
  (wavelet caller → rust-a AND rust-b, run/run-values/run-resources all ok, plus
  the DefResource resources callee) on this exact tree.
- CHANGELOG + type-system.mdx updated; no examples regen needed (export-only
  behaviour); no grammar/token changes.

Items 2–7 (5.6, 5.7, 5.8, strict-flip, 5.11, 5.12 values) not started — session 8
deliberately checkpointed rather than open the huge 5.6 box-retirement mid-budget.
Its checkpoint (update lot:033kod8BBBnuAPm2bS8siu on this Thing) records the 5.6
findings for session 9: compound builtins currently consume boxes via rebox (correct
today), to_str at emit.rs:10304 is the trap point / first differential-testable
slice, plus the exact TAG-consumer retirement list and the 5.4 residues to fold in.

Next: launching goal-5 session 9 from the session-8 checkpoint — docket 5.6 →
5.7 (Do+If stdlib) → 5.8 → strict-flip check → 5.11 → (if room) 5.12 values callee.
