---
status: work
update-id: lot:033kps1uFMLf0UKVzZbFBG
work-at: 2026-07-06T15:04:58.912893+00:00
---
Orchestrator integration — goal-5 session 10 merged to main.

Merged `goal-5-abi-backend` (2 commits, tip ad21c2e) into main as merge commit
**131f83b**; full `cargo test` green on merged main; pushed origin main. Branch
fast-forwarded and pushed as session 11's base.

Landed: (1) bare nullary-case patterns over canonical Repr::Mem variants route to
the discriminant matcher instead of rebox+string-compare (baf59e3); (2) the first
type-indexed compound builtin — `len` over a statically-canonical list reads the
length word directly (ad21c2e), with the oracle-scoping finding that only lists
qualify (string len is char count, tuples have no len word). Both slices proven by
env-gated instrumentation (path fires, output unchanged) + differential harness +
full conformance matrix on the release build.

Accepted finding: the dep-Qsym ctor residue (`t/east(5)` qualified case
construction) is NOT a codegen ride-along — neither the backend nor the
interpreter has qualified case-constructor resolution, and nothing exercises it.
Escalated to Logan on the handoff Thing as a non-blocking design question with a
recommendation to drop it from 5.4's residue list.

Next: session 11 launched — its keystone is the structural 5.6 block per this
checkpoint's prescription: canonical-structural comparator (eq/compare over
records/lists/variants/strings) → head/tail/str-cat over canonical lists/strings →
TAG_* consumer retirement + 5.1 evacuation glue + functor opt-out removal, with
the len/bare-case slices as the gating templates, plus the two ride-alongs
(pattern_mem_field nested nullary arm; float/char to-string if cheap). 5.7 stays
the next dedicated keystone after 5.6 closes.
