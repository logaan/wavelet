---
status: work
update-id: lot:033kw8bVAQza5wCZH2CTCC
work-at: 2026-07-06T19:20:21.822610+00:00
---
Session 15 progress (branch goal-5-abi-backend, pushed, tip 83c11d4, 1 commit
over base 0dd05b5). 5.8 Fn-literal capture devirtualization LANDED.

- 83c11d4  5.8: devirtualize applies through Fn-literal bindings (capture case).
  An inline `Fn` literal bound directly as a `Let` init whose checked type is a
  concrete arrow is lambda-lifted to a top-level core function; an apply through
  the binding compiles to a direct `call` instead of allocating a closure box
  and dispatching through the TAG_FN funcref table. The lifted function takes
  the lambda's captures (every visible local, name-sorted exactly as fn_form
  orders them) as leading TYPED params followed by its boxed value params; the
  apply pushes the capture locals verbatim (sound: single-assignment,
  lexically scoped) then the boxed args. The boxed closure fn_form builds is
  still emitted for non-apply uses.

  Implemented on the recorded positional-reservation plan (session-14
  checkpoint): scan_let_lambdas pre-scans each internal def body and reserves
  one core-fn index per qualifying node RIGHT AFTER the overload block and
  before the export wrappers; compile_known_lambda lifts + stashes the body
  during that def's emission; the stashed bodies are pushed into em.bodies in
  reservation order between the overload and export bodies (position == reserved
  index, debug-asserted). The macro-component path makes no reservations, so
  let_form there keeps the boxed closure (no flag needed - lambda_reserved is
  simply empty). Binding grew known_lambda (index into Emitter::known_lambdas),
  Copy preserved. WLT_DEVIRT_INSTR (removed pre-commit) proved zero/one/two-
  capture lambdas all lift + apply direct, outputs oracle-equal.

  backend_functions.rs gained `capture` (1 int capture -> 6) and `capture2`
  (2 int captures -> 31) component cases + oracle cross-checks; the pre-existing
  `lam` zero-capture case now runs through the lifted path (was boxed).

VERIFICATION: cargo test full suite 0 failures (incl differential harness +
locked examples). Release conformance matrix on this tree: wavelet caller ->
rust-a AND rust-b run()/run-values()/run-resources() all ok; resources callee
PASS rust-a AND rust-b. conformance/wavelet/wit/wavelet.wit restored, out/out-res
removed, tree clean. No CHANGELOG entry (internal codegen only, no user-visible
change); no regen-examples (no documented example behaviour changed); no
downstream surfaces (no lexer/semantics change).

NEXT in this session: assessing the multi-case variant+dispatch half of 5.8 and
the 5.11/5.12 docket; findings to follow.
