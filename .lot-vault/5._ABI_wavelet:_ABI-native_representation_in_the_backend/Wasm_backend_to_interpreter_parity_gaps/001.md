---
status: note
task-id: lot:033iXMOcUXfyRMZpdIS8FQ
update-id: lot:033iXMOcUXfyRMZqfiHPFY
note-at: 2026-07-02T14:17:57.305153+00:00
---
# Wasm backend to interpreter parity gaps

The interpreter (`interp.rs`) is the semantics oracle; the wasm backend
(`emit.rs`) is validated against it and is explicitly a v0 subset. These are
the constructs the backend still rejects with an honest "not supported by
the wasm backend yet" rather than miscompiling — each is a place the
compiler trails the interpreter.

Two items from this list were already migrated earlier under
`Round_trip_all_wit_types/Stage_3...` and are not repeated here: garbage
collection (see the top-level "Implement garbage collection" Thing), flag
literal lowering (see "Support flags in the wasm backend"), and constructing
user variant/enum cases in compiled code (see "Bind variant and enum case
constructors").
