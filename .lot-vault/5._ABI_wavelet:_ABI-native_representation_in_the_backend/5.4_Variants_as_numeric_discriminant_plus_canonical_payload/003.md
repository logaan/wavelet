---
status: work
update-id: lot:033j5QKuSQ23HWypPdz3Xs
work-at: 2026-07-03T13:25:58.891229+00:00
---
Session 4: the headline's first half landed (6d88015; 336 tests green +
conformance matrix green against both rust callees). Dep-born canonical
variants + discriminant-based Match, in the established Repr::Mem overlay:

- A dep call whose declared result is an option/result/variant is BORN
  canonical: the retptr area carries the numeric discriminant + payload at
  the canonical offset. Gates (dep_result_mem_ty / can_mem_as /
  node_mem_ty) admit variant shapes; wit_of_check_type learned two-arm
  results (elided/_-arm results use the checker's Unit and stay boxed).
- pattern_mem_var: a Sym-headed case pattern over a canonical variant
  scrutinee resolves the case name to its discriminant INDEX at compile
  time and matches with one integer comparison — no runtime case-name
  strings. Payloads destructure in place (typed scalar binders, interior
  record/tuple/list pointers); statically-absent cases and unsatisfiable
  arities branch out; several sub-patterns spread a tuple payload
  element-wise (the one-sub-pattern-binds-whole-payload rule verified
  against the interpreter repl).
- Export retptr fast path verified for option results (fwd test).
- Instrumentation confirmed every case pattern in the test corpus
  compiles through the disc path.

STILL OPEN on 5.4:
1. Variant CONSTRUCTION stays boxed: case-constructor calls (some/ok/err/
   local/dep cases) still build TAG_VAR boxes; canonical construction +
   mem_field_ok admission (variants inside records/lists) is the next
   slice. Faithfulness note: a case constructor's value is name+payload —
   no order hazard — so construction can go canonical wherever the
   payload stores losslessly.
2. Bare-Sym case tests (none / payload-less case names) take the sound
   rebuilt-box fallback, not the disc path.
3. TAG_VAR retirement + Match-on-strings removal for the BOXED path (the
   uniform matcher still string-compares); rides construction + 5.6.
4. Large types (>256 cases / >32 flags members): unchanged, still the
   recorded child.
5. The 5.9 Sym-headed-tuple-pattern residue on the BOXED path remains
   (fixed on the canonical path for statically-known tuples/variants).
6. Flags stay Value::Flg boxes internally (order-observable Vec); i32
   bitset is boundary-only. Mem flags fields ride the construction slice.
