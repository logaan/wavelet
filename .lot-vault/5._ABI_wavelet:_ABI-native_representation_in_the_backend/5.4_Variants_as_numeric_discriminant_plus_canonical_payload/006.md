---
status: work
update-id: lot:033jEVAuFBjVzmqO0tCCpR
work-at: 2026-07-03T19:35:53.694168+00:00
---
Session 7: canonical FLAGS FIELDS in records landed (branch goal-5-abi-backend;
commits fe8d633 + aa4613c over base c9e6a08; 353 tests green [+1 new
discriminating test], full conformance matrix green: wavelet caller -> rust-a
AND rust-b, run()/run-values()/run-resources() all ok).

LANDED (fe8d633 emit + aa4613c CHANGELOG):
- Blocker A: emit wit_of_check_type learned a T::Flags arm, so a record with a
  flags field no longer maps to None -> boxed.
- Blocker B (partial, see NOTE): check's Node::Flg now consults `expected` -- a
  flags literal in a context expecting a declared flags type takes THAT type
  (its full declared member set), so the canonical layout uses the
  DECLARATION's bit positions, not the literal's. Added Checker::flags_members
  helper. This threads correctly wherever the record has an expected type: a
  `The rec {...}` annotation, a typed call argument, or a record field.
- emit mem_field_ok Flags gate: admit a flags literal ONLY when its members are
  a duplicate-free subsequence of the declared members in DECLARATION ORDER
  (helper flags_is_ordered_subseq). The canonical bitset rebuilds in
  declaration order at the boundary, while the oracle's Value::Flg is
  order-observable (eq value.rs:81, pattern interp.rs:552, to-string
  value.rs:250 all source-order), so a reordered/duplicated literal must stay
  boxed.
- emit mem_field_into Flags arm: writes the compile-time-constant bitset
  (declared positions) at the canonical width.
- LATENT BUG FIXED (user-visible, CHANGELOG Fixed): flags_align was hardcoded 4;
  the canonical ABI aligns flags to 1/2/4 by member count. store_to_mem's flags
  arm always stored 4 bytes. Both corrupted the layout of ANY record/tuple with
  a flags field crossing a component boundary (standalone flags unaffected --
  offset 0). Now fixed and matching flags_size + load_from_mem widths.
- Test: tests/backend_flags.rs canonical_flags_field_uses_declared_bit_positions_and_alignment
  -- a record {lead: u8 perms: access trail: u32} built via `The entry {...}`
  with the non-adjacent literal {read exec} over access{read write exec admin}
  lifts {read exec} with trail at the right offset. Alignment is
  boundary-observable; declared-position construction confirmed by
  instrumentation (mem_field_into saw the full 4-member declared set).

NOTE / DECISION NEEDED (linchpin, gates the export fast-path payoff): a BARE
exported def body is NOT checked against its declared result type (check_roots
passes expected=None), so `Def f Fn {} {perms: {read exec}}` gets the LITERAL's
2-member flags layout (structural), which mismatches the declared type; the
export then re-lowers from the declared type (correct output, NOT canonical
fast-path). I implemented threading the declared result type (parse Export
result types, check each Fn body against it) and it made the flags field take
declared positions AND the layout match declared -- BUT it broke 8
backend_lists tests: `Def sum Fn {} Match ... add(a b)` declares result s64 with
an s32 body, and the checker's unify does not widen s32->s64 ("type mismatch:
expected S64, got S32"). match-MACRO threads `expected` into every arm
(check.rs:1316), so the rejection is intrinsic. This is entangled with THREE
language-semantics questions for Logan:
  1. Should the boundary/checker allow SAFE int widening (s32 body for an
     s64-declared result)? Programs currently rely on this being unchecked.
  2. Threading also makes overload resolution return-type-directed for export
     bodies (a runtime behavior change).
  3. New sub-expression type errors could surface.
I REVERTED the threading and kept the localized flags work, which is CORRECT for
all currently-reachable paths: cross-origin canonical compound ops (5.6) are not
implemented yet, so the literal-vs-declared layout divergence cannot manifest
(a literal-layout record is self-consistent + oracle-faithful in source order,
and the boundary re-lowers set-preservingly). The threading should be decided +
landed BEFORE 5.6 makes cross-origin canonical compound ops reachable.

5.4 RESIDUE still open (behavior-preserving optimizations; boxed fallback
already correct; the findings themselves prescribe instrumentation not diff
tests -- deliberately NOT landed in a background session to avoid untestable
codegen that could silently regress pattern/ctor logic):
  1. Dep-case ctor calls via Qsym (t/east(5)) stay boxed: ctor_parts
     (emit.rs ~4731) matches only Node::Sym heads, not Node::Qsym; needs
     alias->case resolution against the target variant's cases.
  2. Bare-Sym case patterns (none / nullary case) rebox: pattern_top_mem
     (emit.rs ~2641) routes them to the boxed `_` arm; pattern_mem_var
     (~2774) already handles the (0,None) disc case, so route a bare
     nullary-case Sym there as a 1-element slice.
  3. Large types (>256 cases / >32 flags): per-type disc/bitset sizing still
     unimplemented; canonical gates hard-refuse >256, store/load/pattern assume
     1-byte disc. (flags_size already handles >32; store/load error >32.)
  4. 5.9 Sym-headed-tuple residue on the boxed path.
