---
status: work
update-id: lot:033j5t3miJqKQQ9jpZL00H
work-at: 2026-07-03T13:44:51.576753+00:00
---
Session-5 exit recon (session was stopped by Logan before doing any work; branch unchanged at 986a9a4 = merged main f7cae89; nothing to integrate). Verified 5.4 open items against emit.rs, for the next session to start from:

1. Dep-case ctor calls via Qsym (`t/east(5)`) still boxed - `ctor_parts` (emit.rs:4715) only matches `Node::Sym` heads, not `Node::Qsym`.
2. Bare-Sym case tests still rebox - `pattern_top_mem` (emit.rs:2627) sends a bare case-name pattern (`none`/nullary case) down the `_` reboxing arm; `pattern_mem_var` (emit.rs:2768) already handles the `(0, None)` disc case, so this is a small dispatch addition - but verify oracle semantics for a bare case name over a payload-carrying case against the boxed `pattern` at emit.rs:2975 first.
3. TAG_VAR retirement + boxed matcher string-compare removal - rides 5.6.
4. Large types (>256 cases / >32 flags): child lot:033iXNIjkaYb9h6VxPvopG is "done" only as a DESIGN decision (folded into 5.4); per-type discriminant/bitset sizing is NOT implemented - canonical gates at emit.rs:4167/4176/4191 still hard-refuse, and store/load/pattern all assume 1-byte disc.
5. 5.9 Sym-headed-tuple residue remains on the boxed path.
6. Flags fields in Mem records - `mem_field_ok` (emit.rs:4875) still returns false for `WitTy::Flags`.

Recommended next-session start (per the session-4 checkpoint order): items 2 and 6 are the most self-contained single-commit wins; item 1 next; large-type sizing is bounded but threads store/load/pattern together. Then 5.6 compound builtins. Commit after each step; cargo test green per commit; commit before any instrumentation (git checkout hazard).
