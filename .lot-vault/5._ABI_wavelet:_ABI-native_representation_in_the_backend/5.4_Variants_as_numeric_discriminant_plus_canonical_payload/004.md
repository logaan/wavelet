---
status: work
update-id: lot:033j5gmC7tbkKUoKFTvZgX
work-at: 2026-07-03T13:36:47.184403+00:00
---
Session 4, second slice (986a9a4; 342 tests green + conformance matrix
green both callees, pushed): canonical case-constructor CONSTRUCTION.

- A ctor form builds disc+payload directly in canonical layout, with the
  interpreter's bundling rule (1 arg = payload; >=2 bundle as a tuple
  payload, element-wise in place). some/ok/err + local DefType cases +
  bare none/nullary-case Syms.
- KEY insight recorded: a variant's value is name+payload — no field-order
  hazard — so box->mem IS faithful for variants (unlike records).
  mem_var_into therefore has a sound boxed fallback (evaluate boxed,
  store_to_mem), used wherever ctor resolution at emission differs from
  the gate's view (e.g. shadowing invisible to the prediction walk's Sim
  scopes), and expr_mem stores boxed variant bindings canonically at the
  seam. This makes optimistic gating safe.
- ctor_parts mirrors expr's routing exactly (local bindings shadow all;
  builtins some/ok/err outrank defs; other heads need local_cases; defs/
  value globals shadow cases); pinned by a shadowing test verified
  against the interpreter repl.
- Variants nest canonically inside records/lists (mem_field_ok admits
  variant-shaped fields; fallback covers non-ctor values). Ctor def
  bodies predict Mem results; exports take the retptr fast path.
- Instrumentation: every ctor site in the test corpus compiles through
  the in-place build.

Updated STILL OPEN list: (1) dep-case ctor calls via Qsym (t/east(5))
stay boxed; (2) bare-Sym case TESTS in patterns still rebox (disc path
is Tup-patterns only); (3) TAG_VAR retirement + the boxed matcher's
string-compare removal ride 5.6/strict; (4) large types (>256/>32)
child unchanged — canonical gates refuse >256-case variants; (5) 5.9
Sym-headed-tuple residue on the BOXED path; (6) flags fields in Mem
records (order-observable literal gate, declaration-order only).
