---
status: work
update-id: lot:033kveYCJzTaehLQswkE0n
work-at: 2026-07-06T19:00:36.710756+00:00
---
Orchestrator integration — goal-5 session 14 merged to main.

Merged `goal-5-abi-backend` (4 commits, tip f5ea328) into main as merge commit
**0dd05b5**; full `cargo test` green on merged main; pushed origin main. Branch
fast-forwarded and pushed. (The session survived two environment crashes — a
watchdog stall and a dropped connection — with no work lost; resumed both times.)

Landed — 5.8 core:
- Arrow types (Type::Fn) in the checker: concrete Fn literals, bare single-sig def
  references, indirect applications of function-typed locals statically checked;
  gradual values stay Unknown.
- Boundary rule with oracle parity: exported results containing function types are
  a deliberate, identical compile error in checker and WIT synthesis (was a
  misleading generic build error / silent interp escape). CHANGELOG updated.
- Zero-capture devirtualization: applies through known-def bindings compile to
  direct calls (instrumentation-proven; shadowing stays indirect).
- Docs: "Function values" section in type-system.mdx.

STRICT FLIP: NOT flipped — backlog unchanged at 17/86. Arrow types were necessary
but not sufficient: the 17 examples are AUTHORED with untyped params. Clearing
them needs either expected-arrow-directed param inference + typed higher-order
builtin signatures, or typed rewrites of the documented examples — a pedagogy/
design decision. Per Logan's standing instruction, this is being written up as a
self-contained decision Thing (child of the goal-3 Thing) rather than decided
autonomously.

Next: session 15 = 5.8 remainder per the recorded positional-reservation plan
(Fn-literal capture devirtualization; multi-case variant dispatch) → 5.11 → 5.12
values if room. Then the 5b evacuation session, then goals 6/7.
