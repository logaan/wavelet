---
status: work
update-id: lot:033kyF4FZ3MRj5r0OqxDZc
work-at: 2026-07-06T20:46:06.486567+00:00
---
goal-6 session 1 — CHECKPOINT (branch goal-6-retire-interp, tip 9746e64)

Session 1 was primarily discovery + verification: the tree is materially further
along than the plan notes assumed, so the honest, high-value output is an
accurate state map, the true blocker, and a coverage-gated docket — not a
manufactured code slice. One correct in-lane commit landed (a doc fix finishing
Step 1's documentation).

## Commits (pushed to origin/goal-6-retire-interp)
- 9746e64 docs(expand): describe the landed compiled-macro path. Corrects the
  stale `expand.rs` module comment ("Local macros expand via
  Interp::expand_once") to the reality: native build/run/expand expand local +
  foreign macros as compiled wasm via macrodep::FileExpander; Interp::expand_once
  is now only the docs-playground fallback + the F4 oracle. Doc-only; `cargo
  check` clean.

## Test / conformance status
- `cargo test`: GREEN — 37 suites, 0 failures (full clean run), exit 0. Includes
  both F4 harnesses (differential.rs, macro_differential.rs) and the locked
  examples.
- Conformance matrix: NOT re-run, and intentionally so — this session changed no
  `src/`/emit/build code (only one doc comment), so the release conformance
  status is definitionally identical to base 98b1e59, which goal-5 session 16
  verified green (wavelet caller vs rust-a AND rust-b ok; resources callee PASS
  both). Tree clean; conformance/wavelet/wit/wavelet.wit untouched.

## Findings (the corrected state map)
1. **F4 (differential harness, the re-sequenced step 1.1) is DONE and green** —
   execution half (differential.rs: interp vs compiled-component to-string) and
   expansion half (macro_differential.rs: interp expand_file(None) vs compiled
   strategy-B FileExpander). The "land F4 before any backend change" gate is met.
2. **Step 1 (6.1 compiled macros) is SUBSTANTIALLY LANDED in production.**
   build.rs / runner.rs / main.rs all build a FileExpander and route local
   DefMacro sites through a compiled `wavelet:meta/macros` component. Compiled
   Quote/Quasi/Unquote/Splice/gensym/form-introspection — the plan's "riskiest
   single piece" — are implemented and oracle-verified. 6.1.1–6.1.8 effectively
   complete (see #3); 6.1.9 (file-local *functions* at expand time) is future
   work. The Interp left in expand.rs is the playground-only (None) path — the
   intended, out-of-scope surface — and cannot go to zero without breaking the
   playground.
3. **6.1.8 (expand-inside-macro) is resolved in code** (commit 02fef25): a
   guest-internal one-step expander `mc_expand_step` emitted into every macro
   component, oracle-verified by macro_differential's expand_inside_macro_agrees.
   Recommended it be marked done on its Thing (lot:033iXPVQNRNNMG4SVXW1fC). No
   "reject" diagnostic needed.
4. **F2 (native box reader) is likely unnecessary.** differential.rs proves the
   cleaner path: the guest renders its result with the backend `to-string` and
   returns a WIT string that host.rs marshals natively. A compiler-backed REPL
   should print via to-string→string, not by walking boxes in memory. Fold this
   into Step 2's design; drop the box-reader task unless a value needs printing
   that to-string cannot render.
5. **The true blocker for Step 2 (REPL) and the run rider is backend coverage,
   not goal-6 codegen.** differential.rs SKIP list is the evidence: `to-string`
   traps on float/char; missing stdlib builtins apply/abs/to-u32/to-u8/map (via
   range)/reverse/get/split/zip/read; `pi` unbound; flag literals rejected on the
   emit path. Therefore:
   - A fully-compiled REPL would regress (no `map`, no float/char printing).
   - `wavelet run` is NOT blocked by the float/char to-string trap (it discards
     the result, prints nothing — the README "WASM!" is stale; there are no IO
     builtins), but IS blocked because programs using an unimplemented builtin
     fail to compile, and it additionally needs build+**compose**+run for the
     multi-file/import case (examples/main.wlt imports examples/shout.wlt). A
     straight flip regresses; a compile-else-interp hybrid keeps the interpreter
     (doesn't retire it) and is throwaway once coverage lands.
   Closing coverage is goal-5 backend/stdlib work (5.6/5.7) plus the pending
   Logan decision on float & char to-string in the backend
   (lot:033kswCdhpahhwjWsuqDOb) — out of this session's lane (goal-5 dormant /
   decision-blocked; must not touch its codegen). This is the "overlap trap" the
   session brief flagged: route around, keep on interpreter, record — done.

## NOT started (deliberately, blocked as above)
- Step 2 (6.4) compiler-backed REPL — repl.rs still Interp::evals per line.
- run rider (6.5) — runner.rs expands compiled but EXECUTES via Interp.
- 6.6 retire Interp from remaining CLI paths — gated on Steps 2/run landing.

## Ordered docket for session 2
1. GATE CHECK first: re-read differential.rs SKIP. Step 2 + run flip only become
   non-regressing once (a) the float/char to-string decision lands + is
   implemented in emit, and (b) the missing stdlib builtins compile (map via
   range, apply, abs, to-u8/u32, reverse, get, split, zip, read, pi). If the SKIP
   list is still large, Step 2/run remain blocked — do NOT flip; keep recording.
2. When the gate clears, build **Step 2 (REPL)** on the proven machinery:
   accumulate-and-recompile (session accumulator of Def/DefMacro/DefType/Import;
   bare expr → synthetic `Def __eval Fn {} <expr>` exported), reuse
   differential.rs's wrap → build → HostComponent → to-string→string path
   (skip F2). Definition lines compile-to-validate + commit-on-success; macro
   lines flow through the already-landed FileExpander. Add a REPL-session
   differential test (interp REPL vs compiled REPL) to lock it.
3. **run rider (6.5)** after Step 2: repoint runner.rs at build + compose + a
   core-module/component runner (host.rs is capability-free single-component —
   multi-file run needs wac-style compose). Add a run-differential harness (a
   corpus of run programs, interp vs compiled exit-status/error) before flipping,
   since there is no run test corpus today.
4. Then **6.6**: confirm the only remaining Interp callers on CLI surfaces are
   gone (playground + F4 oracle excepted); update lib.rs exports.
5. CHANGELOG [Unreleased] when repl/run behaviour actually changes; downstream
   surfaces per CLAUDE.md if anything user-facing moves.

## Process notes
- Worktree throughout; base 98b1e59. Edit/Write blocked (parent not isolated) —
  the one src edit went through a python patch. Committed + pushed. Branch green.
- No environment crashes. Vault churn from the concurrent decision-docs agent on
  main is expected and ignored.
