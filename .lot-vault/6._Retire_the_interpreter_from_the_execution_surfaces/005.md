---
status: work
update-id: lot:033kyJKHtHrBnwz2LuMqai
work-at: 2026-07-06T20:48:54.421147+00:00
---
Orchestrator integration — goal-6 session 1 merged to main (59b0acb).

Session 1 was discovery + verification and it materially re-scoped goal 6:
- F4 (differential harness, the "land first" prerequisite) is DONE — both
  execution (tests/differential.rs) and expansion (tests/macro_differential.rs)
  harnesses exist and are green.
- Step 1 (compiled macros incl. Quote/Quasi/Unquote/Splice/gensym — the plan's
  riskiest piece) is ALREADY LANDED in production and oracle-verified; the
  remaining Interp in expand.rs is only the docs-playground fallback (out of
  scope) + the oracle itself. 6.1.8 recommended marked done.
- F2 (native box reader) likely unnecessary (to-string→WIT-string supersedes).
- THE REAL GATE for Step 2 (REPL) / 6.5 (run) / 6.6 (Interp retirement) is
  backend coverage: the differential SKIP list (missing builtins like
  apply/abs/split/zip/reverse/get/read/pi; float/char to-string; flag literals).
  A flip today would regress those surfaces.

ORCHESTRATOR NEXT STEP: a dedicated **backend-coverage session** to implement the
missing SKIP-list builtins that are pure oracle-following work (no decisions
needed) on the goal-5 branch once the values-callee verify session frees it.
The two decision-gated entries stay excluded: float/char to-string (decision #3)
and flag literals-at-runtime (decision #5). After coverage closes, goal-6
session 2 attempts the REPL/run flip with the SKIP-list residue explicitly
carved out.
