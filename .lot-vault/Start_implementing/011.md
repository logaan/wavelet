---
status: work
update-id: lot:033j5pPipQ8YjtBLyd2ew8
work-at: 2026-07-03T13:42:27.794228+00:00
---
# Coordination handoff (orchestrator session ending; successor starts here)

## Where the plan stands

- **Goals 0, 1: done** (before this session).
- **Goal 2 (shrink/settle core): done.** Merged + pushed (merge a08e29c). Decisions recorded and Things retired: 2.2 all seven drops, 2.3 defunctionalization, 2.4 trims, 2.5 (no renames), 2.6 (.wvl -> .wlt implemented).
- **Goal 3 (total type system): done.** Merged + pushed (08b2982). 3.1-3.14 landed; `--strict` exists but the DEFAULT FLIP IS NOT DONE - gated on 5.8 function types clearing the 17-example backlog (`tests/strict_mode.rs::strict_backlog`); execute per 3.9/3.10 recorded plans when it clears.
- **Goal 4 (full WIT surface): done except 4.5.** Merged + pushed (1beb034). 4.5 DefResource is mid-decision (see below); user-defined functor templates deferred behind it (recorded on 4.6).
- **Goal 5 (ABI backend): in progress, ~sessions 1-4 merged** (latest merge f7cae89, all green). Branch `goal-5-abi-backend`, worktree `.claude/worktrees/goal-5-abi-backend`. **Session 5 is RUNNING now** (Opus agent): docket = 5.4 remainder, 5.6 compound builtins + TAG_* retirement + 5.1 evacuation glue, 5.7 stdlib split (Do AND If - Logan answered 2.8 option 1), 5.8 defunctionalization (design posted, unobjected), strict flip check, 5.11, 5.12-callee only when 4.5 design consolidates. Authoritative per-child state = latest checkpoint updates on goal Thing lot:033iWOnrwIwk9KIjngGGD1.
- **Goals 6 (retire interpreter) and 7 (build/CLI/tooling): not started.** Goal 6 goal Thing lot:033iXPUXOpyoLFMXHiK0s1; goal 7 lot:033iXOA8Svj1eWC0ph47es. Goal 8 is unscheduled/parked - Logan's decisions there are NOT pre-recorded.

## 4.5 DefResource decision state (interactive thread with Logan)

Settled, recorded as work updates on lot:033iXNJD5h2KcdEuOxFeOb: dedicated DefResource form; use-site spelling `counter/new(5)` / `counter/next(c)` (qualified Qsym, handle explicit); methods declare `self: counter` as ordinary typed first field, self binds the rep.
Awaiting Logan on four child notes: decision 3 own/borrow type positions (lot:033j5kGCR56ZRXqZcJoXOq), decision 4 destructor (lot:033j5kry0JxFkYeIVrUcGD), decision 5 statics/export (lot:033j5lUjxlZ78kkwA4cvVq), decision 6 observable semantics (lot:033j5lxbD1LPP8QOpF1Sr6).
When Logan has answered all four: post the consolidated `info` design update on 4.5 (complete design + consequences), then DefResource implementation can be scheduled, which unblocks 5.12 callee role and 5.8.1.

## Other decisions still awaiting Logan

- 2.1 proposals on lot:033i26jtolYsuD2hi5xwd1: drop list-payload positional binding (lands with 3.9/3.10); reject float literals as Match patterns (lands with 3.12).
- 2.7 grouping / meaning of (a) on lot:033ichoJaN3hdjhyPgjlVj.

## Workflow (Logan's instructions, proven over this session)

1. One subagent per goal; large goals run as sequential sessions (fresh agent per session once context runs out; each session ends with a checkpoint update on the goal Thing that the next session resumes from). **Logan wants all new subagents spawned with model: opus.** (Note: spawn-time override works; turns triggered by Logan messaging an agent from his UI run on the session default regardless.)
2. Subagents work in their own worktree/branch, commit after every step, push their branch to origin, never push origin/main, never open PRs.
3. Orchestrator integrates each finished session: `git fetch && git merge <branch>` on the shared checkout's main, run `cargo test` (must be fully green), `git push origin main`. This is Logan's explicit instruction for this plan and overrides the usual no-push-to-main rule.
4. Progress updates on the relevant vault Things as work proceeds (work/info/done); orchestrator posts integration updates on this Thing.

## Operational gotchas (all learned the hard way)

- `lot` CLI: ALWAYS `LOT_VAULT_PATH=/Users/logaan/code/personal/rust/wavelet/.lot-vault`; update bodies via stdin ONLY (`echo ... | lot update ...` or `< file`) - the `-- "body"` form hangs and wedges the vault lock (kill the stuck process to recover). `lot` commits to the shared checkout's main; that is expected.
- Subagent Edit/Write tools may be rejected ("parent bg session hasn't isolated") - agents fall back to Bash-applied patches inside their worktree; tell them this up front.
- `git checkout <file>` in the worktree discards uncommitted work (bit two sessions). Commit before instrumentation/cleanup.
- Agents stall (stream watchdog, no progress 600s). Work survives if committed. Resume via a message to the same agent; if its context is exhausted, spawn a fresh session agent from the goal-Thing checkpoint instead.
- Verification gates per CLAUDE.md: cargo test, differential harness, conformance suite (`conformance/`), `./scripts/regen-examples.sh` when example-visible behaviour changes, CHANGELOG [Unreleased], downstream surfaces (docs prose, Prism, wavelet.nvim submodule - commit AND push inside it + stage pointer, VS Code grammar, wavelet-lsp).

## Immediate next actions for the successor

1. When goal-5 session 5 reports: integrate (merge, full cargo test, push origin/main), post an integration update here, then launch session 6 from the goal-Thing checkpoint (Opus) if work remains; iterate until goal 5 closes.
2. Watch for Logan's answers on the 4.5 child notes; when all four land, post the consolidated info update on 4.5 and schedule DefResource implementation (then 5.12 callee + 5.8.1).
3. After goal 5: goals 6 then 7, same pattern (read goal Thing + children fully, brief per this workflow).
4. The strict flip belongs to whichever session lands 5.8 - do not let it fall through the cracks.
