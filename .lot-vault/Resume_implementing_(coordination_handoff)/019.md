---
status: work
update-id: lot:033krF6kS4V1aIzoRvGDmv
work-at: 2026-07-06T16:00:53.855134+00:00
---
Orchestrator — session 12 integrated; the 5.6 monolith is broken; session 13 = the 5.7 keystone.

## Integrated: session 12 → main (origin/main = 712e9b5)
The plan-first mandate worked. Session 12 posted the TAG_* retirement
decomposition (on the goal-5 Thing) BEFORE coding, then consumed its first two
slices: nested nullary-case field patterns via the disc matcher, and canonical
zero-copy `tail` (shares the operand's element buffer — no result box). All
instrumented-verified; suite + differential + release conformance green.

The decomposition's structural findings (recorded so nobody re-derives them):
- Wholesale TAG_* deletion is NOT a goal-5 slice — while any boxed producer
  remains, consumers must stay; it lands when total typing removes the last
  boxed producer. TAG_FN/TAG_TUP retirement is gated on 5.8.
- With len/head/eq/tail canonical and "slice 4" found not-applicable (get/map/
  etc. are stdlib functions, not backend builtins), the boxed-path shortcut
  queue is now consumed.
- 5.1 evacuation glue should go via "canonicalize resource state first, then
  flat memcpy" (path 5b) as its own dedicated session; str-cat folds in there.

## Orchestrator decision (checkpoint asked): critical path over optimization
Session 13 (running, Opus) = **5.7 Do+If stdlib split** as the dedicated keystone
— interpreter-first, all five stages, regen-examples, all three grammars incl.
the wavelet.nvim submodule push + pointer, docs prose, CHANGELOG. Then session
14 = **5.8 defunctionalization** (unblocks TAG_FN/TAG_TUP and the strict-flip
check) → strict flip if the backlog clears → the 5b evacuation session → 5.11 →
5.12 values. Rationale: 5.7/5.8 are user-visible blockers on the critical path;
the evacuation optimization loses nothing by waiting for canonical resource
state.

Still open for you (non-blocking): the dep-Qsym qualified-case-constructor
question (recommendation: drop).
