---
status: work
update-id: lot:033jGfWtnJ8OanERJyePNT
work-at: 2026-07-03T21:04:11.796846+00:00
---
Orchestrator status — DefResource interp/check/wit integrated; emit + 5.12 now running.

## Integrated: DefResource session 1 → main (origin/main = cafdd68)
The interpreter-first layers of user-declared resources (4.5) landed green and
merged: **read + interp + check + wit**, 366 tests (+13 new), CLI-verified
(constructor `counter(10)`, `counter/next` mutation, static alt-ctor
`counter/sum([1 2 3 4])`, identity equality, opaque `<counter>` to-string — all per
design). Full per-layer detail is on the 4.5 Thing checkpoint.

## Now running: DefResource session 2 (Opus, branch `defresource`)
The remaining backend: **emit.rs** (exported-resource intrinsics generalizing the
`set` machinery — session 1 left an exact line-referenced map) then the **5.12
conformance callee** (wavelet exports the counter and passes rust-a/rust-b
`run-resources()`). This is the acceptance test for both 4.5 and 5.12.

## One NON-BLOCKING convention question for you (no rush)
DefResource member results that flow through `cell-get`/constructor calls aren't
structurally inferable, so the conformance source ascribes them with `The` (e.g.
`next: Fn {self: counter} The u32 …`). This works and matches the target WIT.
Question for whenever you're back: is the `The`-annotation convention fine as-is, or
do you want dedicated sugar for member result types? Not blocking — session 2
proceeds with `The`.

## Pipeline
- (running) DefResource emit + 5.12 conformance.
- (blocked on your widening decision above) goal-5: result-type threading + safe
  int widening → 5.6 → 5.7 (Do+If stdlib) → 5.8 → strict-flip → 5.11.
- (later) goals 6 and 7.
