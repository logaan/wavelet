---
status: work
update-id: lot:033kycPM3yL5Xvw5fyu1Sd
work-at: 2026-07-06T21:01:26.884792+00:00
---
Orchestrator — decision #5 self-resolved by verification; the callee's REAL last
gap is tuples; backend-coverage session running.

## Zero-surface verify: flags SOLVED, premise partly corrected (main = 8f8149f)
permissions-rt is now written and VERIFIED with zero new language surface (Match
over the 16 declaration-order flags literals, arms ascribed `The permissions`) —
oracle computes the complement correctly on both seeds; backend lowers it.
`flg`/`contains` are NOT needed. I posted this as an info update on decision
Thing #5 (lot:033ky5FRo5Up9Id5G6cTmr): no action needed from you unless you
independently want ergonomic runtime flags ops.

The verify also corrected the record — the values callee's actual blockers are:
1. **Tuple VALUE construction does not exist in the language** (`tup(...)` is
   unbound in the oracle; bare parens are call forms; tuple patterns work,
   boundary lift/lower works). This is genuinely new decision territory —
   **decision Thing #6 is being written now** (spelling options: `tup` builtin /
   dedicated literal syntax incl. its collision with your 2.7 paren decision /
   leave boundary-only). Arguably a hole in goal 4's "full WIT surface" claim.
2. **Backend `drop`** — a plain oracle-following backend gap (interp builtin
   exists), being fixed now, no decision needed.

## Now running (parallel)
- **Backend-coverage session** (Opus, goal-5 branch): implementing every
  differential-SKIP builtin that is pure oracle-following work (apply, abs,
  split, zip, reverse, get, read, pi, drop, ...), using the harness ratchet as
  per-builtin acceptance. This is THE gate for goal-6's repl/run flip (session-1
  finding). Exclusions: float/char to-string (#3), tuples (#6).
- **Decision-docs agent**: Thing #6 (tuple construction).

Goal-6 session 2 (the surface flip attempt) launches after coverage closes.

Decision Things: #1 lot:033ksw3uOs10lLkuyHNe6c · #2 lot:033kswCYjPPamnfkbExrjF ·
#3 lot:033kswCdhpahhwjWsuqDOb · #4 lot:033kvrrxADAJMmX0ikCyFL ·
#5 lot:033ky5FRo5Up9Id5G6cTmr (RESOLVED by verification, FYI only) · #6 to follow.
