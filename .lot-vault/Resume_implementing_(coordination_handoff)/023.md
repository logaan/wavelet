---
status: work
update-id: lot:033kvso5CL1B6nv6CqhDZO
work-at: 2026-07-06T19:09:58.885172+00:00
---
Orchestrator — decision Thing #4 is ready: the strict-mode backlog.

**lot:033kvrrxADAJMmX0ikCyFL** (child of the goal-3 Thing). Empirical per-example
bucketing of all 17 backlog failures (each verified with `--strict` runs): 6 clear
with typed params, 2 need typed higher-order builtin signatures, 2 need a
record-return rewrite, and 7 need deeper checker/backend work (recursion result
inference, cell types, macro-trylet, capturing closures, lists of functions) —
which proves pure example rewrites (option b) cannot reach the flip on their own.
Recommendation: the hybrid (c) — expected-arrow-directed inference for the
mainstream higher-order cases so the docs keep their untyped teaching style, plus
targeted straggler work, much of which is already on 5.8's docket. The recorded
3.9/3.10 flip plans are summarized inside, with per-option verification gates.

All four decision Things now awaiting your answers:
1. lot:033ksw3uOs10lLkuyHNe6c — qualified case constructors
2. lot:033kswCYjPPamnfkbExrjF — DefResource tail
3. lot:033kswCdhpahhwjWsuqDOb — float/char to-string
4. lot:033kvrrxADAJMmX0ikCyFL — strict-mode backlog / the flip

Session 15 (5.8 remainder → 5.11 → 5.12 values if room) is still running; the
overnight queue continues.
