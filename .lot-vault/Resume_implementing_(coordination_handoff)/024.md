---
status: work
update-id: lot:033kwTT1o3OgUJgJrM4XXt
work-at: 2026-07-06T19:34:04.540639+00:00
---
Orchestrator — session 15 integrated; session 16 (the evacuation keystone) running.

## Integrated: session 15 → main (origin/main = 2b28577)
5.8's capture devirtualization landed (Fn literals bound via Let are lambda-lifted;
applies become direct calls with captures as leading params — executed exactly on
session 14's recorded plan, instrumentation-proven). Plus 5.12 groundwork: the
values callee (roundtrip-values.wlt) is authored and fully typed; its precise
blocker is that the backend lacks map/filter/fold, list-building (push/concat),
and flg/contains. Suite + differential + release conformance green.

Accepted deferrals: 5.8 multi-case dispatch and 5.11 both correctly wait on the
evacuation/retirement work (no observable change or nothing deletable until then).

## Now running: session 16 (Opus) — the 5b evacuation keystone
Canonicalize resource state → flat memcpy evacuation at post-return → remove the
functor/resource opt-out (gate: multi-call resource mutation test + resources
callee with reset enabled) → riders (5.8 multi-case dispatch, newly-possible TAG
retirement, canonical str-cat) → the 5.12 backend builtins so the typed values
callee compiles and test-values-callee passes both seeds → re-check 5.11. This may
take more than one session; green prefixes only.

After this docket clears, goal 5 closes → goal 6 (retire interpreter) → goal 7
(build/CLI/tooling) → the record-only code review (lot:033kscUZLMS3Ouvf2KdFeo).

Your four decision Things (answers whenever you're up):
1. lot:033ksw3uOs10lLkuyHNe6c — qualified case constructors
2. lot:033kswCYjPPamnfkbExrjF — DefResource tail
3. lot:033kswCdhpahhwjWsuqDOb — float/char to-string
4. lot:033kvrrxADAJMmX0ikCyFL — strict-mode backlog / the flip
