---
status: work
update-id: lot:033ky75pBoyXuAcYHRKOKr
work-at: 2026-07-06T20:40:52.018831+00:00
---
Orchestrator — decision Thing #5 ready, and its finding is being acted on.

**lot:033ky5FRo5Up9Id5G6cTmr** — runtime flags construction/membership (child of
the goal-5 Thing). Verified facts: `flg` isn't a builtin; `contains` is
string-only at runtime though the checker already types the flags form; the block
is purely oracle semantics (typing + WIT already pass for all 35 exports); and
any runtime constructor must normalize to declaration order (Value::Flg is an
order-sensitive Vec vs the ABI's declaration-ordered bitset). Options: add
builtins / membership-only / rewrite the test / defer — with the key finding that
permissions-rt's complement transform is LIKELY EXPRESSIBLE TODAY via Match over
static flags literals, i.e. zero new language surface.

**Acting on the recommendation's first step** (it needs no language change, so it
doesn't preempt your answer): a scoped session is now attempting the zero-surface
values callee on the dormant goal-5 branch — writable scope limited to
conformance/wavelet (no src/ changes allowed; it stops and reports if any would
be needed). Outcome either closes 5.12 outright or pins the exact blocker,
sharpening decision #5 either way. Goal-6 session 1 continues in parallel in its
own worktree.

All five decision Things: #1 lot:033ksw3uOs10lLkuyHNe6c (qualified ctors),
#2 lot:033kswCYjPPamnfkbExrjF (DefResource tail), #3 lot:033kswCdhpahhwjWsuqDOb
(float/char to-string), #4 lot:033kvrrxADAJMmX0ikCyFL (strict backlog),
#5 lot:033ky5FRo5Up9Id5G6cTmr (runtime flags).
