---
status: work
update-id: lot:033ktKjTR1tYsJveyUcV2I
work-at: 2026-07-06T17:26:05.431222+00:00
---
Orchestrator — the three decision Things are ready for you.

Each is self-contained (context with repl-verified code examples, the question,
options + tradeoffs + recommendation, and a per-option implementation plan): answer
on the Thing and hand it to any fresh Claude session to execute.

1. **lot:033ksw3uOs10lLkuyHNe6c** — Qualified case constructors (child of the 5.4
   Thing). Notable correction: bare `days(5)` AND import-alias-qualified
   `sh/circle(2.0)` case construction both already work (the latter just stays
   boxed in the backend); what doesn't exist is TYPE-qualified `ttl/days(5)`.
   Options: drop / adopt type-qualified ctors / narrow emit-only canonical fast
   path for the alias spelling. Recommended: drop, or the narrow fast path.

2. **lot:033kswCYjPPamnfkbExrjF** — DefResource tail (child of the 4.5 Thing).
   Findings: the playground example just needs authoring + the heavy regen
   (recommend do-now); the own-consumption `consumed` flag is DEAD PLUMBING (never
   set anywhere — not "modelled" as the design claimed); and there is a real
   ORACLE DIVERGENCE today — the backend runs user `Drop:` bodies (exported
   [dtor]) but the interpreter binds nothing. Firing-point options laid out
   (recommend teardown).

3. **lot:033kswCdhpahhwjWsuqDOb** — float/char to-string in the backend (child of
   the goal-5 Thing). Exact trap site + exact oracle format documented. Options:
   park until goal 6 / port Ryū / simpler format in both / permanent carve-out —
   plus the finding that CHAR is much cheaper than float and can be split out and
   done now regardless of the float call.

Meanwhile goal-5 session 14 (5.8 defunctionalization → strict-flip check) is still
running; the overnight queue continues as planned.
