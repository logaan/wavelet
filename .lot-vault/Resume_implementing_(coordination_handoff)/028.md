---
status: work
update-id: lot:033kyov38nnVgprhZbOZuV
work-at: 2026-07-06T21:09:40.247998+00:00
---
Orchestrator — decision Thing #6 ready: constructing tuple values in source.

**lot:033kynq2dZv0puz9xWOQKY** (child of the goal-5 Thing). Verified: parens are a
single Node::Tup meaning a CALL in eval position and a tuple only under Quote;
commas lex as whitespace so the WAVE `(1, "a")` spelling collides with call forms
AND your settled 2.7 answer ("(a) stays a zero-arg call") — while the printer
already emits tuples as `(1, "a")`, a reader/printer asymmetry worth knowing about.
The oracle mints Value::Tup via arg-bundling/boundary-lift/zip/Quote, and
`The tuple(u32 string) Quote (1 "a")` works today — but Quote is literal-only, so
computed tuples are inexpressible. Options: `tup` variadic builtin (recommended,
oracle-first) / dedicated literal syntax / no-surface / reuse Quote-Quasi.
Also flagged: docs values.mdx + roadmap.mdx currently OVERCLAIM tuple
construction — whichever option you pick includes a docs correction.

This is the LAST language gap for the 5.12 values callee (backend `drop` is in
the coverage session running now).

Complete decision-Thing index for your morning:
1. lot:033ksw3uOs10lLkuyHNe6c — qualified case constructors
2. lot:033kswCYjPPamnfkbExrjF — DefResource tail
3. lot:033kswCdhpahhwjWsuqDOb — float/char to-string
4. lot:033kvrrxADAJMmX0ikCyFL — strict backlog / the flip
5. lot:033ky5FRo5Up9Id5G6cTmr — runtime flags (RESOLVED by verification; FYI)
6. lot:033kynq2dZv0puz9xWOQKY — tuple value construction

Running: the backend-coverage session (differential-SKIP builtins + drop).
Next after it: goal-6 session 2 (repl/run flip attempt with residue carved out).
